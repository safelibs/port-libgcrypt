name: libgcrypt-validator-workflow
backend: codex
working_dir: .
max_bounces: 999
linear: true
yaml_source_mode: inline-only
verifier_encoding: explicit-phases
vars:
  GOAL: Validate the Rust libgcrypt safe port with the Safelibs validator and finalize evidence.
phases:
  - id: impl_p01_validator_checkout_invocation
    type: implement
    bounce_target: null
    prompt: |
      Role: Software engineer implementing phase `impl_p01_validator_checkout_invocation`.
      
      This prompt is self-contained; the agent sees only this prompt. Work from repository root `/home/yans/safelibs/pipeline/ports/port-libgcrypt`. Preserve unrelated user work and the existing dirty worktree state.
      
      Workflow Topology
      - Phase order: 1
      - Phase ID: `impl_p01_validator_checkout_invocation`
      - Phase type: `implement`
      - Fixed top-level `bounce_target`: null
      - Verifier phases, in structure order immediately after this implement phase: `check_p01_validator_checkout_invocation`
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      - Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or agent-guided `bounce_targets` lists.
      - Use only fixed `bounce_target` values from `.plan/workflow-structure.yaml`.
      
      Preexisting Inputs
      Consume or update the existing artifacts listed here and in the phase file content below. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `original/libgcrypt20-1.10.3/`
      - `safe/`
      - `safe/dist/`
      - `rust-toolchain.toml`
      - `safe/rust-toolchain.toml`
      - `safe/scripts/check-rust-toolchain.sh`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `.gitignore`
      - `https://github.com/safelibs/validator`
      
      New Outputs
      Produce or update the outputs named here and in the phase file content below. Keep outputs phase-scoped, avoid unrelated edits, and commit all phase work to git before yielding.
      
      - External checkout `validator/` cloned or fast-forwarded to the current
        upstream commit.
      - Root `.gitignore` entries for `/validator/`, `/validator-artifacts/`, and
        `/validator-local/`.
      - `safe/scripts/prepare-validator-local-port.py`.
      - `safe/scripts/run-validator-libgcrypt.sh`.
      - `safe/scripts/check-validator-port-evidence.py`.
      - `safe/scripts/validator-libgcrypt-skips.json` initialized with
        `schema_version: 1`, the current validator commit, and an empty `skips`
        array.
      - Verified or repaired Rust 1.85.1 toolchain pin consumed by all package build
        paths.
      - Verified safe package lock split for Ubuntu snapshot/base-image packages
        versus locally rebuilt safe `.deb` packages.
      - Initial `validator-report.md` with validator commit, README-derived
        invocation notes, libgcrypt support detection, original smoke result, safe
        port smoke result, and port-mode override-install evidence status. A failing
        safe port smoke result must be recorded for phase 2 triage instead of fixed
        or hidden in this setup phase.
      - Ignored generated directories `validator-local/` and `validator-artifacts/`.
      
      Git Requirement
      Commit work to git before yielding. Stage only files that are in scope for this phase. Do not commit unrelated dirty files or the nested `validator/` checkout.
      
      Phase File Content From `.plan/phases/01-validator-checkout-invocation.md`
      
      # Phase Name
      
      Validator checkout and local invocation harness
      
      # Implement Phase ID
      
      `impl_p01_validator_checkout_invocation`
      
      # Preexisting Inputs
      
      Consume these artifacts in place. If any listed generated artifact already exists, use it as phase input evidence instead of rediscovering or reimporting it.
      
      - Original source snapshot and safe implementation artifacts:
        `original/libgcrypt20-1.10.3/`, `safe/Cargo.toml`, `safe/Cargo.lock`,
        `safe/build.rs`, `safe/src/`, `safe/abi/`, `safe/cabi/`, and
        `safe/vendor/`.
      - Generated/package artifacts from prior phases: `safe/dist/*.deb`,
        `safe/dist/safe-debs.manifest.json`, and top-level package build outputs if
        present.
      - Existing test harnesses and scripts: `safe/scripts/*.sh`,
        `safe/tests/upstream/`, `safe/tests/compat/`, `safe/tests/dependents/`,
        and `safe/tests/regressions/`.
      - Existing metadata and documentation inputs: `dependents.json`,
        `relevant_cves.json`, `all_cves.json`, and `safe/docs/*.md`.
      - Dependent-image package locks and snapshot inputs:
        `safe/tests/dependents/metadata/install-packages.noble.lock`,
        `safe/tests/dependents/metadata/safe-debs.noble.lock`,
        `safe/tests/dependents/metadata/base-image.noble.digest`, and
        `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`.
      - `safe/` Rust port and package scripts.
      - Root `rust-toolchain.toml`, `safe/rust-toolchain.toml`, and
        `safe/scripts/check-rust-toolchain.sh`.
      - `safe/dist/` if it already exists; treat it as generated evidence from prior
        phases, but rebuild it for validator input after the phase tag points at
        `HEAD` and verify the manifest `phase_commit` and `phase_tag` match.
      - `safe/tests/dependents/metadata/install-packages.noble.lock` and
        `safe/tests/dependents/metadata/safe-debs.noble.lock`.
      - `original/libgcrypt20-1.10.3/`.
      - Existing `.gitignore`.
      - Existing `validator/` checkout if present.
      - Validator upstream URL `https://github.com/safelibs/validator`.
      
      # New Outputs
      
      - External checkout `validator/` cloned or fast-forwarded to the current
        upstream commit.
      - Root `.gitignore` entries for `/validator/`, `/validator-artifacts/`, and
        `/validator-local/`.
      - `safe/scripts/prepare-validator-local-port.py`.
      - `safe/scripts/run-validator-libgcrypt.sh`.
      - `safe/scripts/check-validator-port-evidence.py`.
      - `safe/scripts/validator-libgcrypt-skips.json` initialized with
        `schema_version: 1`, the current validator commit, and an empty `skips`
        array.
      - Verified or repaired Rust 1.85.1 toolchain pin consumed by all package build
        paths.
      - Verified safe package lock split for Ubuntu snapshot/base-image packages
        versus locally rebuilt safe `.deb` packages.
      - Initial `validator-report.md` with validator commit, README-derived
        invocation notes, libgcrypt support detection, original smoke result, safe
        port smoke result, and port-mode override-install evidence status. A failing
        safe port smoke result must be recorded for phase 2 triage instead of fixed
        or hidden in this setup phase.
      - Ignored generated directories `validator-local/` and `validator-artifacts/`.
      
      # File Changes
      
      - `.gitignore`
      - `rust-toolchain.toml` only if missing or inconsistent with
        `safe/rust-toolchain.toml`.
      - `safe/rust-toolchain.toml` only if missing or inconsistent with the root
        pin.
      - `safe/scripts/check-rust-toolchain.sh` only if it does not enforce Rust
        `1.85.1` before Cargo and Debian package builds.
      - `safe/tests/dependents/metadata/safe-debs.noble.lock` only if the local
        safe-deb policy is missing or ambiguous.
      - `safe/tests/dependents/validate-installed-packages.py` only if safe-image
        installed-package validation does not distinguish Ubuntu packages from local
        safe `.deb` packages.
      - `safe/scripts/prepare-validator-local-port.py`
      - `safe/scripts/run-validator-libgcrypt.sh`
      - `safe/scripts/check-validator-port-evidence.py`
      - `safe/scripts/validator-libgcrypt-skips.json`
      - `validator-report.md`
      
      # Implementation Details
      
      ## Workflow-Wide Requirements For This Phase
      
      - Consume the listed preexisting inputs in place. Existing source snapshots, metadata, tests, packages, validator checkouts, and prior artifacts are inputs, not rediscovery or regeneration tasks unless this phase explicitly rebuilds generated package/validator artifacts.
      - Keep `validator/`, `validator-local/`, and `validator-artifacts/` external to the port repository. Do not commit nested validator contents or generated validator artifacts.
      - Preserve the Rust toolchain contract: root `rust-toolchain.toml` and `safe/rust-toolchain.toml` must stay identical and pinned to Rust `1.85.1` with `profile = "minimal"` and `components = ["rustfmt"]`.
      - Run `safe/scripts/check-rust-toolchain.sh` before every Cargo, release-library, Debian package, or dependent safe-image build.
      - Build any package artifacts used as validator inputs from a clean phase commit after `phase/impl_p01_validator_checkout_invocation` points at `HEAD`. If tracked files or `validator-report.md` change after validation, amend the same phase commit, force-update the phase tag, rebuild packages, regenerate `validator-local`, and rerun the phase validator commands until the worktree is clean and artifacts match the final phase commit.
      - `validator-report.md` must identify the safe port by implement phase ID and `phase/impl_p01_validator_checkout_invocation`, not by embedding the literal hash of the final report commit.
      
      ## Phase-Specific Details
      
      - Clone/update `validator/`:
        - If `validator/.git` exists, verify `origin` is
          `https://github.com/safelibs/validator` or an equivalent GitHub URL, then
          run `git -C validator pull --ff-only`.
        - If `validator/` does not exist, run
          `git clone https://github.com/safelibs/validator validator`.
        - Record `git -C validator rev-parse HEAD` in `validator-report.md`.
      - Do not commit the nested validator checkout. Add ignore entries for
        `/validator/`, `/validator-artifacts/`, and `/validator-local/`.
      - Verify the Rust and package-lock contracts before creating validator
        adapters:
        - `rust-toolchain.toml` and `safe/rust-toolchain.toml` must be identical and
          pinned to Rust `1.85.1`.
        - `safe/scripts/check-rust-toolchain.sh` must reject Ubuntu 24.04's packaged
          Rust 1.75 and must be called by `safe/scripts/build-debs.sh`,
          `safe/debian/rules`, `safe/scripts/build-release-lib.sh`, and
          `safe/scripts/build-dependent-image.sh`.
        - `safe/scripts/check-dependent-metadata.sh` must pass, proving that
          `install-packages.noble.lock` remains the Ubuntu/base-image package
          closure and `safe-debs.noble.lock` remains the local safe-deb policy for
          `libgcrypt20` and `libgcrypt20-dev`.
      - Implement `safe/scripts/prepare-validator-local-port.py` as a deterministic
        adapter from the local safe `.deb` artifacts to validator's port override
        format:
        - Inputs: `--validator-dir`, `--dist`, and `--output-root`.
        - Read `safe/dist/safe-debs.manifest.json`.
        - Require manifest fields `phase_commit`, `phase_tag`,
          `toolchain.rustc_vv`, `toolchain.cargo_vv`, and package entries with
          `package_name`, `filename`, `architecture`, `version`,
          `source_package_name`, `source_version`, and `sha256`.
        - Require exactly the canonical safe packages `libgcrypt20` and
          `libgcrypt20-dev`.
        - Validate package names, versions, source package, source version,
          architecture, and file globs against
          `safe/tests/dependents/metadata/safe-debs.noble.lock`, while taking SHA256
          values from the freshly generated manifest rather than the committed lock.
        - Copy the `.deb` files to
          `validator-local/override-debs/libgcrypt/`.
        - Write `validator-local/proof/local-port-debs-lock.json` with
          `schema_version: 1`, `mode: "port"`, one `libraries` entry for
          `libgcrypt`, canonical packages `libgcrypt20` and `libgcrypt20-dev`,
          `repository` set to the GitHub `owner/name` from this port's `origin`
          remote (`safelibs/port-libgcrypt` in the current workspace),
          `release_tag` set to a deterministic local value such as
          `local-libgcrypt-safe`, `tag_ref` equal to
          `refs/tags/local-libgcrypt-safe`, `commit` equal to the current port HEAD,
          `debs` entries containing `package`, `filename`, `architecture`, `sha256`,
          `version`, and `size`, and an empty `unported_original_packages` list. The
          current validator accepts the standard `package`, `filename`,
          `architecture`, `sha256`, and `size` fields; the local `version` field is
          required for this port's independent override-install evidence checks.
        - Validate the copied files by hash before returning.
      - Implement `safe/scripts/run-validator-libgcrypt.sh` as the single local
        entrypoint for validator runs:
        - Inputs: `--validator-dir`, `--mode original|port`, `--artifact-root`,
          optional `--override-root`, optional `--port-lock`, optional `--case`,
          optional `--case-glob`, optional `--kind source|usage`, and
          optional `--record-casts`.
        - Also support a standalone `--self-test-timeout` mode. This mode must create a
          temporary synthetic fallback testcase with `@timeout: 1` and a temporary fake
          `docker` executable or equivalent internal harness that sleeps past the
          deadline, run the same timeout-enforcement code used by real fallback cases,
          and assert that the internal testcase run returns nonzero only after writing
          a failed result JSON with `exit_code: 124`, a log containing
          `testcase timed out after 1 seconds`, and no leftover temporary status
          directory or named container. The `--self-test-timeout` command itself must
          exit zero when those assertions pass. It must not modify `validator/`.
        - Load `safe/scripts/validator-libgcrypt-skips.json` before selecting cases.
          The file must contain `schema_version: 1`, top-level `validator_commit`,
          and a `skips` array. Fail closed if the top-level `validator_commit` does
          not exactly match `git -C validator rev-parse HEAD`. Reject the run on any
          skip entry whose own `validator_commit` does not exactly match the same
          commit.
          A skip entry may match only one testcase ID, must name the affected mode,
          must include a concise reason, and must point at the
          `validator-report.md` section documenting the validator defect.
        - Enumerate the selected libgcrypt testcase IDs before choosing an execution
          path. Active skips are skip entries whose validator commit equals the
          checked-out validator `HEAD`, whose mode equals the requested mode, and
          whose exact testcase ID is in the selected full-suite or focused set.
          Unknown testcase IDs in active skip entries are fatal. Inactive entries for
          a different validator commit are fatal because the file is intentionally
          commit-specific.
        - First try the official validator matrix path by running
          `python3 validator/tools/testcases.py --config validator/repositories.yml --tests-root validator/tests --list-summary --library libgcrypt`.
        - If the official path succeeds, no filter options were requested, and there
          are no active skips for the selected full-suite run, call `validator/test.sh`
          with
          `--config validator/repositories.yml`, `--tests-root validator/tests`,
          `--artifact-root <artifact-root>`, `--library libgcrypt`, and
          `--mode <mode>` for both original and port mode. In port mode, also pass
          `--override-deb-root <override-root>` and `--port-deb-lock <port-lock>` to
          `validator/test.sh`. Do not pass `--case`, `--case-glob`, or `--kind` to
          `validator/test.sh`; the current validator matrix runner does not support
          per-testcase filters.
        - If the official path succeeds but `--case`, `--case-glob`, or `--kind` was
          requested, use the direct Docker fallback for that focused run.
        - If the official path succeeds but active skips match the selected
          full-suite run, use the direct Docker fallback for that run. Do not call
          `validator/test.sh`, because it would execute the skipped testcase and would
          not emit the port-owned skip result.
        - If the official path fails because libgcrypt is missing from
          `repositories.yml` or `tools/inventory.py`, record that as a validator
          invocation bug in the run output and use the direct Docker fallback.
        - The direct Docker fallback must not edit validator files. It must build the
          existing libgcrypt Dockerfile with
          `docker build -t safelibs-validator-libgcrypt -f validator/tests/libgcrypt/Dockerfile validator/tests`,
          enumerate executable case scripts under
          `validator/tests/libgcrypt/tests/cases/source` and `usage`, parse the
          metadata header using validator-compatible rules for `# @testcase:`,
          `# @title:`, `# @description:`, `# @timeout:`, `# @tags:`, and usage-case
          `# @client:` directives, require the header testcase ID to match the script
          filename, and require `@timeout` to be an integer from `1` through `7200`.
        - For each selected fallback case in either mode, create a fresh per-case host
          status directory and a unique Docker container name derived from the mode,
          testcase ID, and wrapper process ID. Launch Docker through a subprocess,
          process group, or shell `timeout --kill-after` wrapper that enforces the
          exact parsed testcase timeout. The implementation must always remove the
          container with `docker rm -f <container-name>` in the timeout and cleanup
          paths, tolerate the container already being gone, and remove the host status
          directory before moving to the next case.
        - Run each selected original-mode fallback case as
          `docker run --rm --name <container-name> --mount type=bind,src=<absolute-status-dir>,dst=/validator/status --env VALIDATOR_STATUS_DIR=/validator/status --entrypoint /validator/tests/libgcrypt/docker-entrypoint.sh safelibs-validator-libgcrypt <case-id> -- /validator/tests/libgcrypt/tests/cases/<kind>/<file>.sh`,
          but only under the per-case timeout wrapper described above.
        - For selected port-mode fallback cases, resolve
          `<override-root>/libgcrypt` to an absolute path before launching Docker,
          require that it contains exactly the `libgcrypt20` and `libgcrypt20-dev`
          `.deb` files named by `--port-lock`, validate their sizes and SHA256 values,
          create a fresh per-case host status directory, and add both the status mount
          and override mount before the image name:
          `docker run --rm --name <container-name> --mount type=bind,src=<absolute-status-dir>,dst=/validator/status --mount type=bind,src=<absolute-override-root>/libgcrypt,dst=/override-debs,readonly --env VALIDATOR_STATUS_DIR=/validator/status --entrypoint /validator/tests/libgcrypt/docker-entrypoint.sh safelibs-validator-libgcrypt <case-id> -- /validator/tests/libgcrypt/tests/cases/<kind>/<file>.sh`.
          The port-mode Docker command is also valid only when launched under the
          per-case timeout wrapper described above.
        - If the per-case timeout fires in either mode, treat that testcase as failed
          with exit code `124`. The per-case log must contain
          `testcase timed out after <timeout> seconds`, the result JSON must include
          `status: "failed"`, `exit_code: 124`, and `error` equal to
          `testcase timed out after <timeout> seconds`, and the wrapper must continue
          running later selected cases before returning nonzero for the overall run.
        - After every port-mode fallback case, fail the case regardless of testcase
          exit status unless `<absolute-status-dir>/override-installed` exists and
          `<absolute-status-dir>/override-installed-packages.tsv` proves that
          `libgcrypt20` and `libgcrypt20-dev` were installed with package names,
          versions, architectures, and filenames matching
          `validator-local/proof/local-port-debs-lock.json`.
        - For fallback results, write JSON summaries and logs under the requested
          `--artifact-root` using the validator matrix artifact layout:
          original results at `results/libgcrypt/*.json`, port results at
          `port/results/libgcrypt/*.json`, and summaries at
          `results/libgcrypt/summary.json` or `port/results/libgcrypt/summary.json`.
          Port result JSONs must include `override_debs_installed: true`,
          `port_debs`, `override_installed_packages`, `port_repository`,
          `port_commit`, `port_release_tag`, `port_tag_ref`, and
          `unported_original_packages` for every non-skipped testcase.
        - After either the official path or fallback path finishes, inspect the
          generated result JSONs selected by `--case`, `--case-glob`, `--kind`, or the
          full libgcrypt suite. Return nonzero if any selected non-skipped result has
          `status` other than `passed`, even if `validator/test.sh` returned zero.
          Phase 1's safe smoke command and phase 2's safe candidate command
          intentionally append `|| true` so those failures can be recorded in
          `validator-report.md`; fix and final phases must rely on the wrapper's
          nonzero exit to reject unresolved validator failures.
        - When a skip entry matches, still write a per-case result JSON with
          `status: "skipped"`, the testcase ID, the exact validator commit, the
          matching skip reason, and the report section. Do not run that testcase, and
          do not suppress any other selected testcase. Mark the run summary with an
          `official_proof_eligible: false` or equivalent field whenever any skipped
          result JSON is written, so final verifiers can distinguish a validator
          proof/status limitation from an inventory-selection limitation.
        - In `--mode port`, require both `--override-root` and `--port-lock`, and
          validate the local lock before launching any case.
        - Artifact cleanup must be mode-scoped. An original-mode run may replace only
          the original result subtree for the selected libgcrypt cases, and a port-mode
          run may replace only the port result subtree for the selected libgcrypt
          cases. The wrapper must preserve sibling mode results, proof directories,
          and site directories when the same `--artifact-root` is reused, as in the
          final phase.
      - Implement `safe/scripts/check-validator-port-evidence.py` as an independent
        verifier for port-mode artifact roots:
        - Inputs: repeated `--artifact-root`, `--port-lock`, `--override-root`, and
          optional `--library` defaulting to `libgcrypt`.
        - Load the raw local port lock and require canonical packages `libgcrypt20`
          and `libgcrypt20-dev`, including `version`, `architecture`, `filename`,
          `sha256`, and `size`.
        - Validate that `--override-root/libgcrypt` contains exactly those `.deb`
          files and that their sizes and SHA256 values match the lock.
        - For each artifact root, inspect every testcase JSON under
          `port/results/libgcrypt/` except `summary.json`; require at least one
          testcase result.
        - For every non-skipped port testcase result, require
          `override_debs_installed: true`, require `port_debs` to match the lock's
          package, filename, architecture, SHA256, and size fields, and require
          `override_installed_packages` to match the lock's package, version,
          architecture, and filename fields in canonical package order.
        - Fail if an artifact root contains original-mode results only, missing port
          result JSONs, stale port commits, extra or missing override packages, or any
          port result that could have passed against Ubuntu's original packages.
      - Record the validator inspection defect only as context. Re-detect against the
        actual checked-out validator commit.
      - Commit only tracked port-side files and the report. End with one commit whose
        subject begins `impl_p01_validator_checkout_invocation:` and tag it
        `phase/impl_p01_validator_checkout_invocation`.
      
      # Verification Phases
      
      - Phase ID: `check_p01_validator_checkout_invocation`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p01_validator_checkout_invocation`
      - Purpose: software-tester verification that the validator checkout, ignore
        rules, local package override preparation, and smoke invocation path work.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p01_validator_checkout_invocation)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `test -d validator/.git`
        - `git -C validator rev-parse HEAD`
        - `bash -c 'case "$(git -C validator remote get-url origin)" in https://github.com/safelibs/validator|https://github.com/safelibs/validator.git|git@github.com:safelibs/validator.git) ;; *) exit 1 ;; esac'`
        - `python3 -c 'import json, subprocess; data=json.load(open("safe/scripts/validator-libgcrypt-skips.json")); vc=subprocess.check_output(["git","-C","validator","rev-parse","HEAD"], text=True).strip(); assert data.get("schema_version")==1; assert data.get("validator_commit")==vc; assert isinstance(data.get("skips"), list)'`
        - `make -C validator unit`
        - `make -C validator check-testcases`
        - `cmp -s rust-toolchain.toml safe/rust-toolchain.toml`
        - `safe/scripts/check-rust-toolchain.sh`
        - `safe/scripts/check-dependent-metadata.sh`
        - `safe/scripts/build-debs.sh`
        - `safe/scripts/check-deb-metadata.sh --dist safe/dist`
        - `python3 safe/scripts/prepare-validator-local-port.py --validator-dir validator --dist safe/dist --output-root validator-local`
        - `bash safe/scripts/run-validator-libgcrypt.sh --self-test-timeout`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode original --artifact-root validator-artifacts/p01-original-smoke --case aes-ctr-roundtrip`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p01-port-smoke --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case aes-ctr-roundtrip || true`
        - `test -d validator-artifacts/p01-original-smoke`
        - `test -d validator-artifacts/p01-port-smoke`
        - `python3 safe/scripts/check-validator-port-evidence.py --port-lock validator-local/proof/local-port-debs-lock.json --override-root validator-local/override-debs --artifact-root validator-artifacts/p01-port-smoke || true`
        - `bash -c 'test -z "$(git status --short)"'`
      
      # Success Criteria
      
      - The smoke source testcase `aes-ctr-roundtrip` passes in original mode. The
        safe port smoke run is non-gating for safe compatibility; any safe failure is
        preserved in `validator-artifacts/p01-port-smoke` and listed for phase 2
        triage.
      - The wrapper timeout self-test proves that the direct Docker fallback cannot
        hang forever: a synthetic one-second case times out, records failed result and
        log artifacts with exit code `124`, and cleans temporary status/container
        resources.
      - The port smoke evidence checker is run and its result is recorded. Missing
        override-install evidence is classified as a packaging/install setup issue for
        phase 2 triage and phase 3 repair, not as a phase 1 harness failure unless
        the wrapper itself failed to create artifacts.
      - `validator-report.md` records the validator commit, whether the official
        libgcrypt matrix path was available, and the exact smoke commands.
      - The port worktree remains clean even though `validator/` and artifacts exist.
      
      # Git Commit Requirement
      
      The implementer must commit work to git before yielding. The commit subject must start with `impl_p01_validator_checkout_invocation:`, and the implementer must force-update local tag `phase/impl_p01_validator_checkout_invocation` to that phase commit before yielding.
  - id: check_p01_validator_checkout_invocation
    type: check
    bounce_target: impl_p01_validator_checkout_invocation
    prompt: |
      Role: Software tester reviewer for check phase `check_p01_validator_checkout_invocation`.
      
      Review the current workspace for implement phase `impl_p01_validator_checkout_invocation`. Do not modify files. The fixed `bounce_target` for this check is `impl_p01_validator_checkout_invocation`.
      
      Workflow Topology
      - Phase order: 2
      - Phase ID: `check_p01_validator_checkout_invocation`
      - Phase type: `check`
      - Fixed top-level `bounce_target`: `impl_p01_validator_checkout_invocation`
      - This verifier is an explicit top-level `check` phase from `.plan/workflow-structure.yaml` and must remain in structure order immediately after the implement block it verifies.
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      
      Preexisting Inputs
      Use the existing artifacts listed here and in the phase file content below as review evidence. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `phase/impl_p01_validator_checkout_invocation`
      - `validator/`
      - `.gitignore`
      - `safe/scripts/prepare-validator-local-port.py`
      - `safe/scripts/run-validator-libgcrypt.sh`
      - `safe/scripts/check-validator-port-evidence.py`
      - `safe/scripts/validator-libgcrypt-skips.json`
      - `validator-report.md`
      - `safe/dist/safe-debs.manifest.json`
      - `validator-local/proof/local-port-debs-lock.json`
      - `validator-local/override-debs/libgcrypt/`
      - `validator-artifacts/p01-original-smoke/`
      - `validator-artifacts/p01-port-smoke/`
      
      Specific Things To Verify
      - Verify the implementation for `impl_p01_validator_checkout_invocation` satisfies the phase file `.plan/phases/01-validator-checkout-invocation.md` for `Validator checkout and local invocation harness`.
      - Verify the implementation consumed or updated existing artifacts instead of refetching, recollecting, rediscovering, or regenerating them from scratch.
      - Verify the implementation committed the phase work and updated the required phase tag when the phase file requires it.
      - Run every command listed in the verification block below unless a command is impossible in the current environment; if a command cannot be run, treat that as a failure unless the phase file explicitly permits a documented exception.
      - Verify the success criteria below and report any missing artifact, unexpected file modification, unsafe scope expansion, or validator evidence gap.
      
      Verification Block From `.plan/phases/01-validator-checkout-invocation.md`
      
      - Phase ID: `check_p01_validator_checkout_invocation`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p01_validator_checkout_invocation`
      - Purpose: software-tester verification that the validator checkout, ignore
        rules, local package override preparation, and smoke invocation path work.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p01_validator_checkout_invocation)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `test -d validator/.git`
        - `git -C validator rev-parse HEAD`
        - `bash -c 'case "$(git -C validator remote get-url origin)" in https://github.com/safelibs/validator|https://github.com/safelibs/validator.git|git@github.com:safelibs/validator.git) ;; *) exit 1 ;; esac'`
        - `python3 -c 'import json, subprocess; data=json.load(open("safe/scripts/validator-libgcrypt-skips.json")); vc=subprocess.check_output(["git","-C","validator","rev-parse","HEAD"], text=True).strip(); assert data.get("schema_version")==1; assert data.get("validator_commit")==vc; assert isinstance(data.get("skips"), list)'`
        - `make -C validator unit`
        - `make -C validator check-testcases`
        - `cmp -s rust-toolchain.toml safe/rust-toolchain.toml`
        - `safe/scripts/check-rust-toolchain.sh`
        - `safe/scripts/check-dependent-metadata.sh`
        - `safe/scripts/build-debs.sh`
        - `safe/scripts/check-deb-metadata.sh --dist safe/dist`
        - `python3 safe/scripts/prepare-validator-local-port.py --validator-dir validator --dist safe/dist --output-root validator-local`
        - `bash safe/scripts/run-validator-libgcrypt.sh --self-test-timeout`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode original --artifact-root validator-artifacts/p01-original-smoke --case aes-ctr-roundtrip`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p01-port-smoke --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case aes-ctr-roundtrip || true`
        - `test -d validator-artifacts/p01-original-smoke`
        - `test -d validator-artifacts/p01-port-smoke`
        - `python3 safe/scripts/check-validator-port-evidence.py --port-lock validator-local/proof/local-port-debs-lock.json --override-root validator-local/override-debs --artifact-root validator-artifacts/p01-port-smoke || true`
        - `bash -c 'test -z "$(git status --short)"'`
      
      Success Criteria From `.plan/phases/01-validator-checkout-invocation.md`
      
      - The smoke source testcase `aes-ctr-roundtrip` passes in original mode. The
        safe port smoke run is non-gating for safe compatibility; any safe failure is
        preserved in `validator-artifacts/p01-port-smoke` and listed for phase 2
        triage.
      - The wrapper timeout self-test proves that the direct Docker fallback cannot
        hang forever: a synthetic one-second case times out, records failed result and
        log artifacts with exit code `124`, and cleans temporary status/container
        resources.
      - The port smoke evidence checker is run and its result is recorded. Missing
        override-install evidence is classified as a packaging/install setup issue for
        phase 2 triage and phase 3 repair, not as a phase 1 harness failure unless
        the wrapper itself failed to create artifacts.
      - `validator-report.md` records the validator commit, whether the official
        libgcrypt matrix path was available, and the exact smoke commands.
      - The port worktree remains clean even though `validator/` and artifacts exist.
      
      Verdict Instruction
      End your response with exactly one of these lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl_p02_validator_baseline_triage
    type: implement
    bounce_target: null
    prompt: |
      Role: Software engineer implementing phase `impl_p02_validator_baseline_triage`.
      
      This prompt is self-contained; the agent sees only this prompt. Work from repository root `/home/yans/safelibs/pipeline/ports/port-libgcrypt`. Preserve unrelated user work and the existing dirty worktree state.
      
      Workflow Topology
      - Phase order: 3
      - Phase ID: `impl_p02_validator_baseline_triage`
      - Phase type: `implement`
      - Fixed top-level `bounce_target`: null
      - Verifier phases, in structure order immediately after this implement phase: `check_p02_validator_baseline_triage`
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      - Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or agent-guided `bounce_targets` lists.
      - Use only fixed `bounce_target` values from `.plan/workflow-structure.yaml`.
      
      Preexisting Inputs
      Consume or update the existing artifacts listed here and in the phase file content below. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `phase/impl_p01_validator_checkout_invocation`
      - `validator/`
      - `validator-report.md`
      - `safe/scripts/prepare-validator-local-port.py`
      - `safe/scripts/run-validator-libgcrypt.sh`
      - `safe/scripts/check-validator-port-evidence.py`
      - `safe/scripts/validator-libgcrypt-skips.json`
      - `safe/dist/`
      - `validator/tests/libgcrypt/`
      - `validator-artifacts/p01-original-smoke/`
      - `validator-artifacts/p01-port-smoke/`
      
      New Outputs
      Produce or update the outputs named here and in the phase file content below. Keep outputs phase-scoped, avoid unrelated edits, and commit all phase work to git before yielding.
      
      - Full original baseline artifacts under `validator-artifacts/p02-original/`.
      - Full safe candidate artifacts under `validator-artifacts/p02-port/`.
      - Updated `validator-report.md` with:
        - Validator commit and invocation mode.
        - Count of source, usage, and total libgcrypt cases discovered, including
          exact machine-readable lines `libgcrypt_source_cases: <integer>`,
          `libgcrypt_usage_cases: <integer>`, and
          `libgcrypt_total_cases: <integer>` for later proof thresholds.
        - Original baseline pass/fail status.
        - Safe candidate pass/fail status.
        - Port override installation evidence summary. If evidence is incomplete,
          classify the affected cases under `packaging-install`; phase 3 fixes those
          failures first.
        - Failure inventory grouped by class.
        - Any validator-side skip, such as current validator inventory not exposing
          libgcrypt through the official matrix path.
        - The fixed libgcrypt case counts that later phases must consume for proof
          thresholds and report checks instead of rerunning testcase discovery.
      
      Git Requirement
      Commit work to git before yielding. Stage only files that are in scope for this phase. Do not commit unrelated dirty files or the nested `validator/` checkout.
      
      Phase File Content From `.plan/phases/02-validator-baseline-triage.md`
      
      # Phase Name
      
      Baseline full validator run and failure triage
      
      # Implement Phase ID
      
      `impl_p02_validator_baseline_triage`
      
      # Preexisting Inputs
      
      Consume these artifacts in place. If any listed generated artifact already exists, use it as phase input evidence instead of rediscovering or reimporting it.
      
      - Original source snapshot and safe implementation artifacts:
        `original/libgcrypt20-1.10.3/`, `safe/Cargo.toml`, `safe/Cargo.lock`,
        `safe/build.rs`, `safe/src/`, `safe/abi/`, `safe/cabi/`, and
        `safe/vendor/`.
      - Generated/package artifacts from prior phases: `safe/dist/*.deb`,
        `safe/dist/safe-debs.manifest.json`, and top-level package build outputs if
        present.
      - Existing test harnesses and scripts: `safe/scripts/*.sh`,
        `safe/tests/upstream/`, `safe/tests/compat/`, `safe/tests/dependents/`,
        and `safe/tests/regressions/`.
      - Existing metadata and documentation inputs: `dependents.json`,
        `relevant_cves.json`, `all_cves.json`, and `safe/docs/*.md`.
      - Dependent-image package locks and snapshot inputs:
        `safe/tests/dependents/metadata/install-packages.noble.lock`,
        `safe/tests/dependents/metadata/safe-debs.noble.lock`,
        `safe/tests/dependents/metadata/base-image.noble.digest`, and
        `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`.
      - Phase 1 commit and tag.
      - `validator/` checkout.
      - `safe/scripts/prepare-validator-local-port.py`.
      - `safe/scripts/run-validator-libgcrypt.sh`.
      - `safe/scripts/check-validator-port-evidence.py`.
      - `safe/scripts/validator-libgcrypt-skips.json`.
      - `safe/dist/` and package build scripts.
      - `validator/tests/libgcrypt/` if present in the checked-out validator.
      
      # New Outputs
      
      - Full original baseline artifacts under `validator-artifacts/p02-original/`.
      - Full safe candidate artifacts under `validator-artifacts/p02-port/`.
      - Updated `validator-report.md` with:
        - Validator commit and invocation mode.
        - Count of source, usage, and total libgcrypt cases discovered, including
          exact machine-readable lines `libgcrypt_source_cases: <integer>`,
          `libgcrypt_usage_cases: <integer>`, and
          `libgcrypt_total_cases: <integer>` for later proof thresholds.
        - Original baseline pass/fail status.
        - Safe candidate pass/fail status.
        - Port override installation evidence summary. If evidence is incomplete,
          classify the affected cases under `packaging-install`; phase 3 fixes those
          failures first.
        - Failure inventory grouped by class.
        - Any validator-side skip, such as current validator inventory not exposing
          libgcrypt through the official matrix path.
        - The fixed libgcrypt case counts that later phases must consume for proof
          thresholds and report checks instead of rerunning testcase discovery.
      
      # File Changes
      
      - `validator-report.md`
      - `safe/scripts/run-validator-libgcrypt.sh` only if baseline use finds a
        wrapper bug.
      - `safe/scripts/prepare-validator-local-port.py` only if lock or override
        generation is incomplete.
      - `safe/scripts/check-validator-port-evidence.py` only if baseline use finds an
        evidence-checker bug.
      - `safe/scripts/validator-libgcrypt-skips.json` only if baseline identifies a
        concrete testcase execution bug that must be skipped before a complete triage
        run can finish; the skip must be exact-commit-specific and documented in
        `validator-report.md`.
      
      # Implementation Details
      
      ## Workflow-Wide Requirements For This Phase
      
      - Consume the listed preexisting inputs in place. Existing source snapshots, metadata, tests, packages, validator checkouts, and prior artifacts are inputs, not rediscovery or regeneration tasks unless this phase explicitly rebuilds generated package/validator artifacts.
      - Keep `validator/`, `validator-local/`, and `validator-artifacts/` external to the port repository. Do not commit nested validator contents or generated validator artifacts.
      - Preserve the Rust toolchain contract: root `rust-toolchain.toml` and `safe/rust-toolchain.toml` must stay identical and pinned to Rust `1.85.1` with `profile = "minimal"` and `components = ["rustfmt"]`.
      - Run `safe/scripts/check-rust-toolchain.sh` before every Cargo, release-library, Debian package, or dependent safe-image build.
      - Build any package artifacts used as validator inputs from a clean phase commit after `phase/impl_p02_validator_baseline_triage` points at `HEAD`. If tracked files or `validator-report.md` change after validation, amend the same phase commit, force-update the phase tag, rebuild packages, regenerate `validator-local`, and rerun the phase validator commands until the worktree is clean and artifacts match the final phase commit.
      - `validator-report.md` must identify the safe port by implement phase ID and `phase/impl_p02_validator_baseline_triage`, not by embedding the literal hash of the final report commit.
      
      ## Phase-Specific Details
      
      - Rebuild safe packages so the candidate run consumes current safe code.
      - Run the original Ubuntu libgcrypt validator suite first. Original failures
        indicate validator or environment problems, not safe regressions, and must be
        documented separately.
      - Run the safe candidate suite with local override packages prepared from
        `safe/dist`. A failing safe testcase is still useful triage data. A port-mode
        artifact root that lacks override-install evidence is invalid as a final safe
        validator result, but in this baseline phase it must be preserved and
        classified under `packaging-install` so the next phase can fix package setup
        before source or usage compatibility phases depend on rebuilt packages.
      - If the official validator matrix path works and the phase 2 original and port
        artifact roots contain no skipped testcase result JSONs, also generate
        validator proof artifacts for the selected libgcrypt run:
        - Original proof with at least the discovered source/usage/total counts.
        - Port proof with the same thresholds.
        - Use `--ports-root /home/yans/safelibs/pipeline/ports` if supported, so the
          proof can include unsafe counts.
      - If a validator testcase execution bug forces an active skip during phase 2,
        do not run proof generation for an artifact root containing `status:
        "skipped"`. Record `Official validator proof/status limitation` in
        `validator-report.md`, list the skipped testcase IDs and validator commit, and
        use the wrapper's full non-skipped per-case summary as the phase proof
        substitute.
      - If the official path is unavailable because the validator does not support
        libgcrypt in its inventory, use the direct Docker fallback from phase 1 and
        record that the official matrix/proof check was skipped due to validator
        inventory bug while all existing libgcrypt testcase scripts were still run.
      - Group safe candidate failures into these buckets:
        - `source-api`: the 5 source-facing cases, especially AES CTR, SHA-256,
          HMAC-SHA256, MPI arithmetic, and nonce generation.
        - `usage-symmetric-digest-random`: GPG `--print-md`, `--gen-random`,
          symmetric encryption/decryption, list-packets symmetric cases, cipher
          preference/list-config cases, and digest preference cases.
        - `usage-pubkey-keyring`: GPG key generation, import/export, fingerprint,
          sign/verify, recipient encryption/decryption, hidden recipient, trustdb,
          ownertrust, secret-key, keygrip, and revocation cases.
        - `packaging-install`: override `.deb` install, symbol/linker, headers,
          `libgcrypt-config`, pkg-config, Multi-Arch, and dependency issues.
        - `validator-bug`: only failures that are demonstrably in the validator
          harness rather than libgcrypt-safe.
      - Do not fix safe code in this phase except wrapper/report bugs. The phase is
        for a reproducible baseline and triage inventory.
      - Commit the updated report and any wrapper fixes. End with one commit whose
        subject begins `impl_p02_validator_baseline_triage:` and tag it
        `phase/impl_p02_validator_baseline_triage`.
      
      # Verification Phases
      
      - Phase ID: `check_p02_validator_baseline_triage`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p02_validator_baseline_triage`
      - Purpose: software-tester verification that a full original baseline and full
        safe candidate run were executed or that any validator-side skip is precisely
        documented.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p02_validator_baseline_triage)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p01_validator_checkout_invocation)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `bash -c 'if git diff --name-only HEAD^..HEAD | grep -E "^(validator|validator-local|validator-artifacts)(/|$)"; then exit 1; fi'`
        - `safe/scripts/check-rust-toolchain.sh`
        - `safe/scripts/build-debs.sh`
        - `safe/scripts/check-deb-metadata.sh --dist safe/dist`
        - `python3 safe/scripts/prepare-validator-local-port.py --validator-dir validator --dist safe/dist --output-root validator-local`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode original --artifact-root validator-artifacts/p02-original --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p02-port --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --record-casts || true`
        - `python3 safe/scripts/check-validator-port-evidence.py --port-lock validator-local/proof/local-port-debs-lock.json --override-root validator-local/override-debs --artifact-root validator-artifacts/p02-port || true`
        - `test -d validator-artifacts/p02-original`
        - `test -d validator-artifacts/p02-port`
        - `python3 -c 'import re, sys; from pathlib import Path; text=Path("validator-report.md").read_text(); required=["Validator commit","Original baseline","Safe candidate","Port override installation evidence","Failure inventory","libgcrypt_source_cases:","libgcrypt_usage_cases:","libgcrypt_total_cases:"]; missing=[item for item in required if item not in text]; counts={key: re.search(r"^"+key+r":\s*([1-9][0-9]*)\s*$", text, re.M) for key in ["libgcrypt_source_cases","libgcrypt_usage_cases","libgcrypt_total_cases"]}; missing += [key for key, match in counts.items() if match is None]; sys.exit("missing report fields: "+", ".join(missing) if missing else 0)'`
        - `bash -c 'test -z "$(git status --short)"'`
      
      # Success Criteria
      
      - The report has enough detail for phase 3 to fix every `packaging-install`
        blocker first and for later phases to select failure buckets without
        rerunning discovery.
      - Original baseline failures, if any, are separated from safe candidate
        failures.
      - Safe candidate artifact roots that lack override evidence are not accepted as
        passing validator results; they are explicitly inventoried as
        `packaging-install` failures for phase 3.
      - Safe candidate failures are not marked as validator bugs without a concrete
        reason.
      
      # Git Commit Requirement
      
      The implementer must commit work to git before yielding. The commit subject must start with `impl_p02_validator_baseline_triage:`, and the implementer must force-update local tag `phase/impl_p02_validator_baseline_triage` to that phase commit before yielding.
  - id: check_p02_validator_baseline_triage
    type: check
    bounce_target: impl_p02_validator_baseline_triage
    prompt: |
      Role: Software tester reviewer for check phase `check_p02_validator_baseline_triage`.
      
      Review the current workspace for implement phase `impl_p02_validator_baseline_triage`. Do not modify files. The fixed `bounce_target` for this check is `impl_p02_validator_baseline_triage`.
      
      Workflow Topology
      - Phase order: 4
      - Phase ID: `check_p02_validator_baseline_triage`
      - Phase type: `check`
      - Fixed top-level `bounce_target`: `impl_p02_validator_baseline_triage`
      - This verifier is an explicit top-level `check` phase from `.plan/workflow-structure.yaml` and must remain in structure order immediately after the implement block it verifies.
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      
      Preexisting Inputs
      Use the existing artifacts listed here and in the phase file content below as review evidence. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `phase/impl_p01_validator_checkout_invocation`
      - `phase/impl_p02_validator_baseline_triage`
      - `validator/`
      - `validator-report.md`
      - `safe/scripts/prepare-validator-local-port.py`
      - `safe/scripts/run-validator-libgcrypt.sh`
      - `safe/scripts/check-validator-port-evidence.py`
      - `safe/scripts/validator-libgcrypt-skips.json`
      - `safe/dist/safe-debs.manifest.json`
      - `validator-local/proof/local-port-debs-lock.json`
      - `validator-local/override-debs/libgcrypt/`
      - `validator-artifacts/p02-original/`
      - `validator-artifacts/p02-port/`
      
      Specific Things To Verify
      - Verify the implementation for `impl_p02_validator_baseline_triage` satisfies the phase file `.plan/phases/02-validator-baseline-triage.md` for `Baseline full validator run and failure triage`.
      - Verify the implementation consumed or updated existing artifacts instead of refetching, recollecting, rediscovering, or regenerating them from scratch.
      - Verify the implementation committed the phase work and updated the required phase tag when the phase file requires it.
      - Run every command listed in the verification block below unless a command is impossible in the current environment; if a command cannot be run, treat that as a failure unless the phase file explicitly permits a documented exception.
      - Verify the success criteria below and report any missing artifact, unexpected file modification, unsafe scope expansion, or validator evidence gap.
      
      Verification Block From `.plan/phases/02-validator-baseline-triage.md`
      
      - Phase ID: `check_p02_validator_baseline_triage`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p02_validator_baseline_triage`
      - Purpose: software-tester verification that a full original baseline and full
        safe candidate run were executed or that any validator-side skip is precisely
        documented.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p02_validator_baseline_triage)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p01_validator_checkout_invocation)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `bash -c 'if git diff --name-only HEAD^..HEAD | grep -E "^(validator|validator-local|validator-artifacts)(/|$)"; then exit 1; fi'`
        - `safe/scripts/check-rust-toolchain.sh`
        - `safe/scripts/build-debs.sh`
        - `safe/scripts/check-deb-metadata.sh --dist safe/dist`
        - `python3 safe/scripts/prepare-validator-local-port.py --validator-dir validator --dist safe/dist --output-root validator-local`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode original --artifact-root validator-artifacts/p02-original --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p02-port --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --record-casts || true`
        - `python3 safe/scripts/check-validator-port-evidence.py --port-lock validator-local/proof/local-port-debs-lock.json --override-root validator-local/override-debs --artifact-root validator-artifacts/p02-port || true`
        - `test -d validator-artifacts/p02-original`
        - `test -d validator-artifacts/p02-port`
        - `python3 -c 'import re, sys; from pathlib import Path; text=Path("validator-report.md").read_text(); required=["Validator commit","Original baseline","Safe candidate","Port override installation evidence","Failure inventory","libgcrypt_source_cases:","libgcrypt_usage_cases:","libgcrypt_total_cases:"]; missing=[item for item in required if item not in text]; counts={key: re.search(r"^"+key+r":\s*([1-9][0-9]*)\s*$", text, re.M) for key in ["libgcrypt_source_cases","libgcrypt_usage_cases","libgcrypt_total_cases"]}; missing += [key for key, match in counts.items() if match is None]; sys.exit("missing report fields: "+", ".join(missing) if missing else 0)'`
        - `bash -c 'test -z "$(git status --short)"'`
      
      Success Criteria From `.plan/phases/02-validator-baseline-triage.md`
      
      - The report has enough detail for phase 3 to fix every `packaging-install`
        blocker first and for later phases to select failure buckets without
        rerunning discovery.
      - Original baseline failures, if any, are separated from safe candidate
        failures.
      - Safe candidate artifact roots that lack override evidence are not accepted as
        passing validator results; they are explicitly inventoried as
        `packaging-install` failures for phase 3.
      - Safe candidate failures are not marked as validator bugs without a concrete
        reason.
      
      Verdict Instruction
      End your response with exactly one of these lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl_p03_fix_packaging_install
    type: implement
    bounce_target: null
    prompt: |
      Role: Software engineer implementing phase `impl_p03_fix_packaging_install`.
      
      This prompt is self-contained; the agent sees only this prompt. Work from repository root `/home/yans/safelibs/pipeline/ports/port-libgcrypt`. Preserve unrelated user work and the existing dirty worktree state.
      
      Workflow Topology
      - Phase order: 5
      - Phase ID: `impl_p03_fix_packaging_install`
      - Phase type: `implement`
      - Fixed top-level `bounce_target`: null
      - Verifier phases, in structure order immediately after this implement phase: `check_p03_packaging_install_software_tester`, then `check_p03_packaging_install_senior_tester`
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      - Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or agent-guided `bounce_targets` lists.
      - Use only fixed `bounce_target` values from `.plan/workflow-structure.yaml`.
      
      Preexisting Inputs
      Consume or update the existing artifacts listed here and in the phase file content below. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `phase/impl_p02_validator_baseline_triage`
      - `validator/`
      - `validator-report.md`
      - `validator-artifacts/p02-original/`
      - `validator-artifacts/p02-port/`
      - `safe/scripts/prepare-validator-local-port.py`
      - `safe/scripts/run-validator-libgcrypt.sh`
      - `safe/scripts/check-validator-port-evidence.py`
      - `safe/scripts/validator-libgcrypt-skips.json`
      - `safe/dist/`
      - `safe/debian/`
      - `safe/abi/`
      - `safe/cabi/`
      - `safe/build.rs`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/regressions/manifest.json`
      
      New Outputs
      Produce or update the outputs named here and in the phase file content below. Keep outputs phase-scoped, avoid unrelated edits, and commit all phase work to git before yielding.
      
      - `safe/scripts/check-validator-package-dev-probe.sh`, a gating clean-container
        package/development validation helper for local validator `.deb` inputs.
      - Safe-side package/install fixes for every phase 2 `packaging-install`
        failure, including missing override evidence, package install failures,
        development package metadata failures, header/linker failures,
        `libgcrypt-config` failures, pkg-config failures, Multi-Arch issues,
        dependency issues, and symbol/SONAME problems.
      - Minimal regression tests under `safe/tests/regressions/` for each fixed
        package/install failure that can be reproduced outside the validator.
      - Updated `safe/tests/regressions/manifest.json`.
      - Focused validator artifacts under `validator-artifacts/p03-port-source/` and
        `validator-artifacts/p03-port-usage-smoke/`.
      - Package/development probe artifacts under
        `validator-artifacts/p03-package-dev-probe/`, including the clean-container
        install log, compiler/link commands, `pkg-config` output,
        `libgcrypt-config` output, installed package metadata, and probe result.
      - Updated `validator-report.md` marking the `packaging-install` bucket fixed or
        clean, with artifact roots, package/development probe result, and
        evidence-check results.
      - If the phase 2 `packaging-install` bucket is empty, no package or regression
        changes are required; the phase still adds the package/development probe if it
        does not already exist, reruns the probe plus the focused source and usage
        smoke validator checks with port override evidence, updates
        `validator-report.md` to mark the bucket clean with no fixes needed, and
        creates the required phase commit and tag.
      
      Git Requirement
      Commit work to git before yielding. Stage only files that are in scope for this phase. Do not commit unrelated dirty files or the nested `validator/` checkout.
      
      Phase File Content From `.plan/phases/03-fix-packaging-install.md`
      
      # Phase Name
      
      Fix packaging and installation validator failures
      
      # Implement Phase ID
      
      `impl_p03_fix_packaging_install`
      
      # Preexisting Inputs
      
      Consume these artifacts in place. If any listed generated artifact already exists, use it as phase input evidence instead of rediscovering or reimporting it.
      
      - Original source snapshot and safe implementation artifacts:
        `original/libgcrypt20-1.10.3/`, `safe/Cargo.toml`, `safe/Cargo.lock`,
        `safe/build.rs`, `safe/src/`, `safe/abi/`, `safe/cabi/`, and
        `safe/vendor/`.
      - Generated/package artifacts from prior phases: `safe/dist/*.deb`,
        `safe/dist/safe-debs.manifest.json`, and top-level package build outputs if
        present.
      - Existing test harnesses and scripts: `safe/scripts/*.sh`,
        `safe/tests/upstream/`, `safe/tests/compat/`, `safe/tests/dependents/`,
        and `safe/tests/regressions/`.
      - Existing metadata and documentation inputs: `dependents.json`,
        `relevant_cves.json`, `all_cves.json`, and `safe/docs/*.md`.
      - Dependent-image package locks and snapshot inputs:
        `safe/tests/dependents/metadata/install-packages.noble.lock`,
        `safe/tests/dependents/metadata/safe-debs.noble.lock`,
        `safe/tests/dependents/metadata/base-image.noble.digest`, and
        `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`.
      - Phase 2 commit, baseline artifacts, failure inventory, and report.
      - Phase 1 validator wrapper, local port preparation script, evidence checker,
        and skip file.
      - `safe/dist/` and package build scripts.
      - `safe/debian/control`, `safe/debian/rules`,
        `safe/debian/libgcrypt20.install`, `safe/debian/libgcrypt20-dev.install`,
        `safe/debian/libgcrypt20.symbols`, `safe/debian/libgcrypt20.postinst`, and
        `safe/debian/clean-up-unmanaged-libraries`.
      - `safe/abi/gcrypt.h.in`, `safe/abi/libgcrypt.pc.in`,
        `safe/abi/libgcrypt-config.in`, `safe/abi/libgcrypt.vers`,
        `safe/abi/libgcrypt.m4`, and `safe/cabi/`.
      - `safe/tests/dependents/metadata/install-packages.noble.lock` and
        `safe/tests/dependents/metadata/safe-debs.noble.lock`.
      - `safe/tests/regressions/manifest.json`.
      - Phase 2 local port lock and override-root schema as generated by
        `safe/scripts/prepare-validator-local-port.py`.
      
      # New Outputs
      
      - `safe/scripts/check-validator-package-dev-probe.sh`, a gating clean-container
        package/development validation helper for local validator `.deb` inputs.
      - Safe-side package/install fixes for every phase 2 `packaging-install`
        failure, including missing override evidence, package install failures,
        development package metadata failures, header/linker failures,
        `libgcrypt-config` failures, pkg-config failures, Multi-Arch issues,
        dependency issues, and symbol/SONAME problems.
      - Minimal regression tests under `safe/tests/regressions/` for each fixed
        package/install failure that can be reproduced outside the validator.
      - Updated `safe/tests/regressions/manifest.json`.
      - Focused validator artifacts under `validator-artifacts/p03-port-source/` and
        `validator-artifacts/p03-port-usage-smoke/`.
      - Package/development probe artifacts under
        `validator-artifacts/p03-package-dev-probe/`, including the clean-container
        install log, compiler/link commands, `pkg-config` output,
        `libgcrypt-config` output, installed package metadata, and probe result.
      - Updated `validator-report.md` marking the `packaging-install` bucket fixed or
        clean, with artifact roots, package/development probe result, and
        evidence-check results.
      - If the phase 2 `packaging-install` bucket is empty, no package or regression
        changes are required; the phase still adds the package/development probe if it
        does not already exist, reruns the probe plus the focused source and usage
        smoke validator checks with port override evidence, updates
        `validator-report.md` to mark the bucket clean with no fixes needed, and
        creates the required phase commit and tag.
      
      # File Changes
      
      - `safe/debian/**` as required for package install, dependency, Multi-Arch,
        postinst, symbols, or installed-file layout failures.
      - `safe/abi/**` as required for generated headers, `libgcrypt.pc`,
        `libgcrypt-config`, version script, or aclocal metadata failures.
      - `safe/cabi/**` as required for exported C ABI forwarding or symbol failures.
      - `safe/build.rs` as required for generated package/development metadata.
      - `safe/scripts/build-debs.sh`, `safe/scripts/build-release-lib.sh`,
        `safe/scripts/check-deb-metadata.sh`,
        `safe/scripts/prepare-validator-local-port.py`, or
        `safe/scripts/check-validator-port-evidence.py` only if the package or
        evidence tooling is incomplete.
      - `safe/scripts/check-validator-package-dev-probe.sh`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock` only if the committed
        local safe-deb policy is missing a package metadata constraint required by
        the existing contract; do not add rebuilt `.deb` SHA256 values.
      - `safe/tests/dependents/validate-installed-packages.py` only if safe-image
        validation fails to distinguish Ubuntu packages from local safe packages.
      - `safe/tests/regressions/manifest.json`
      - `safe/tests/regressions/**`
      - `validator-report.md`
      
      # Implementation Details
      
      ## Workflow-Wide Requirements For This Phase
      
      - Consume the listed preexisting inputs in place. Existing source snapshots, metadata, tests, packages, validator checkouts, and prior artifacts are inputs, not rediscovery or regeneration tasks unless this phase explicitly rebuilds generated package/validator artifacts.
      - Keep `validator/`, `validator-local/`, and `validator-artifacts/` external to the port repository. Do not commit nested validator contents or generated validator artifacts.
      - Preserve the Rust toolchain contract: root `rust-toolchain.toml` and `safe/rust-toolchain.toml` must stay identical and pinned to Rust `1.85.1` with `profile = "minimal"` and `components = ["rustfmt"]`.
      - Run `safe/scripts/check-rust-toolchain.sh` before every Cargo, release-library, Debian package, or dependent safe-image build.
      - Build any package artifacts used as validator inputs from a clean phase commit after `phase/impl_p03_fix_packaging_install` points at `HEAD`. If tracked files or `validator-report.md` change after validation, amend the same phase commit, force-update the phase tag, rebuild packages, regenerate `validator-local`, and rerun the phase validator commands until the worktree is clean and artifacts match the final phase commit.
      - `validator-report.md` must identify the safe port by implement phase ID and `phase/impl_p03_fix_packaging_install`, not by embedding the literal hash of the final report commit.
      
      ## Phase-Specific Details
      
      - Consume the phase 2 failure inventory. Do not rerun full discovery in this
        phase; rerun only the focused package-gating validator checks listed above.
      - Fix all `packaging-install` failures before source API, symmetric/digest, or
        public-key phases run. Later phases must be able to assume that package
        installation, development metadata, and override evidence are already clean.
      - Treat these as `packaging-install` failures: missing
        `override_debs_installed`, missing or mismatched
        `override_installed_packages`, package install errors, absent runtime or dev
        package files, wrong package name/version/architecture/source metadata,
        missing `gcrypt.h`, missing or broken `libgcrypt.so` or
        `libgcrypt.so.20`, missing symbols, broken `libgcrypt-config`, broken
        `pkg-config libgcrypt`, Multi-Arch metadata problems, dependency problems,
        and package scripts that remove or replace the safe library incorrectly.
      - Add `safe/scripts/check-validator-package-dev-probe.sh` if it does not already
        exist. The script must accept `--dist`, `--override-root`, `--port-lock`,
        `--artifact-root`, and optional `--image` defaulting to `ubuntu:24.04`; fail
        unless `--override-root/libgcrypt` contains exactly the two `.deb` files named
        in the lock; validate their SHA256 and size before Docker starts; install the
        local `.deb` files in a clean container with the minimal compiler and
        development dependencies; reject any installed `libgcrypt20` or
        `libgcrypt20-dev` package whose package name, version, architecture, filename,
        size, or SHA256 does not match the lock and mounted files; compile and run a
        reduced package-surface C probe that includes `gcrypt.h` and calls only
        `gcry_check_version(NULL)`; compile and run the same package-surface probe
        once with direct linker inputs, once with
        `pkg-config --cflags --libs libgcrypt`, and once with
        `libgcrypt-config --cflags --libs`; record the container log, compiler
        commands, tool outputs, installed package metadata, and probe result under the
        requested artifact root; and exit nonzero on every package install, metadata,
        compile, link, or `gcry_check_version` runtime failure. The phase 3 probe must
        not call `gcry_md_hash_buffer`, `gcry_create_nonce`, cipher, MPI, public-key,
        or other source-behavior APIs as gating checks; those compatibility failures
        remain in the phase 4 or later validator buckets.
      - Do not fix source API behavior in this phase. If a source testcase fails after
        successfully installing the safe override packages and compiling/linking its
        probe, leave that testcase in the `source-api` bucket for phase 4.
      - Add a minimal regression before each safe-side package/install fix.
        Regression candidates include installing the locally built `.deb` files in a
        clean package context, checking `dpkg-query` package metadata, compiling and
        linking a reduced C program with `gcrypt.h`,
        `pkg-config --cflags --libs libgcrypt`,
        and `libgcrypt-config --cflags --libs`, and checking `gcry_check_version`
        through the installed shared library.
      - If phase 2 recorded zero `packaging-install` failures, do not invent a
        regression or package change. Rerun the package/development probe and focused
        phase 3 validator checks with override evidence, update
        `validator-report.md` with the clean bucket result, package/development probe
        result, and artifact roots, and make the required
        `impl_p03_fix_packaging_install:` phase commit. If the report was already
        exact and no tracked file changed, create the phase commit with
        `git commit --allow-empty` before tagging it.
      - If the validator itself has a packaging/install harness bug, document the
        exact defect in `validator-report.md`. Do not edit validator files. Use the
        testcase skip file only for an exact testcase execution bug; proof or
        inventory bugs are report-only skips.
      - Commit fixes and report updates. End with one commit whose subject begins
        `impl_p03_fix_packaging_install:` and tag it
        `phase/impl_p03_fix_packaging_install`.
      
      # Verification Phases
      
      - Phase ID: `check_p03_packaging_install_software_tester`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p03_fix_packaging_install`
      - Purpose: verify that package installation, development metadata, local
        override evidence, headers, linker inputs, and package metadata are fixed
        before source or usage compatibility phases depend on rebuilt packages.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p03_fix_packaging_install)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p02_validator_baseline_triage)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `bash -c 'if git diff --name-only HEAD^..HEAD | grep -E "^(validator|validator-local|validator-artifacts)(/|$)"; then exit 1; fi'`
        - `cmp -s rust-toolchain.toml safe/rust-toolchain.toml`
        - `safe/scripts/check-rust-toolchain.sh`
        - `safe/scripts/check-dependent-metadata.sh`
        - `cargo fmt --manifest-path safe/Cargo.toml -- --check`
        - `cargo build --manifest-path safe/Cargo.toml --release --locked --offline`
        - `safe/scripts/build-release-lib.sh`
        - `safe/scripts/check-abi.sh --all`
        - `safe/scripts/run-regression-tests.sh --all`
        - `safe/scripts/build-debs.sh`
        - `safe/scripts/check-deb-metadata.sh --dist safe/dist`
        - `python3 safe/scripts/prepare-validator-local-port.py --validator-dir validator --dist safe/dist --output-root validator-local`
        - `bash safe/scripts/check-validator-package-dev-probe.sh --dist safe/dist --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --artifact-root validator-artifacts/p03-package-dev-probe`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p03-port-source --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --kind source --record-casts || true`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p03-port-usage-smoke --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case usage-gpg-print-md --record-casts || true`
        - `python3 safe/scripts/check-validator-port-evidence.py --port-lock validator-local/proof/local-port-debs-lock.json --override-root validator-local/override-debs --artifact-root validator-artifacts/p03-port-source --artifact-root validator-artifacts/p03-port-usage-smoke`
        - `python3 -c 'import sys; from pathlib import Path; text=Path("validator-report.md").read_text(); required=["packaging-install","Port override installation evidence","Package/development probe","validator-artifacts/p03-package-dev-probe","phase/impl_p03_fix_packaging_install"]; missing=[item for item in required if item not in text]; sys.exit("missing report fields: "+", ".join(missing) if missing else 0)'`
        - `bash -c 'test -z "$(git status --short)"'`
      
      - Phase ID: `check_p03_packaging_install_senior_tester`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p03_fix_packaging_install`
      - Purpose: senior review that packaging/install fixes are safe-side changes,
        do not alter validator tests, and unblock all later port-mode validator runs.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p03_fix_packaging_install)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p02_validator_baseline_triage)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `git diff --stat HEAD^..HEAD`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `bash -c 'if git diff --name-only HEAD^..HEAD | grep -E "^(validator|validator-local|validator-artifacts)(/|$)"; then exit 1; fi'`
        - `test -x safe/scripts/check-validator-package-dev-probe.sh`
        - Review `validator-report.md`, `safe/debian/**`, `safe/abi/**`,
          `safe/cabi/**`, `safe/build.rs`, package scripts, the package/development
          probe, and new regression tests to confirm the phase only fixes package
          install, development metadata, override evidence, header, linker,
          pkg-config, `libgcrypt-config`, Multi-Arch, dependency, or symbol packaging
          failures.
      
      # Success Criteria
      
      - Package install, header, linker, `libgcrypt-config`, pkg-config, Multi-Arch,
        dependency, symbol/SONAME, and override-evidence failures from phase 2 are
        fixed or documented as exact validator bugs.
      - The clean-container package/development probe passes with local safe
        `libgcrypt20` and `libgcrypt20-dev` installed from the validator override root,
        and the probe artifacts show successful direct-header, pkg-config, and
        `libgcrypt-config` compile/link/run paths.
      - Focused source and usage smoke validator runs produce port-mode artifacts with
        verified safe override package installation evidence for every non-skipped
        testcase. Source or usage compatibility failures with complete install
        evidence remain assigned to phase 4 or later buckets rather than blocking
        this packaging phase.
      - Existing safe build, ABI, package metadata, dependent metadata, and regression
        checks pass.
      
      # Git Commit Requirement
      
      The implementer must commit work to git before yielding. The commit subject must start with `impl_p03_fix_packaging_install:`, and the implementer must force-update local tag `phase/impl_p03_fix_packaging_install` to that phase commit before yielding.
  - id: check_p03_packaging_install_software_tester
    type: check
    bounce_target: impl_p03_fix_packaging_install
    prompt: |
      Role: Software tester reviewer for check phase `check_p03_packaging_install_software_tester`.
      
      Review the current workspace for implement phase `impl_p03_fix_packaging_install`. Do not modify files. The fixed `bounce_target` for this check is `impl_p03_fix_packaging_install`.
      
      Workflow Topology
      - Phase order: 6
      - Phase ID: `check_p03_packaging_install_software_tester`
      - Phase type: `check`
      - Fixed top-level `bounce_target`: `impl_p03_fix_packaging_install`
      - This verifier is an explicit top-level `check` phase from `.plan/workflow-structure.yaml` and must remain in structure order immediately after the implement block it verifies.
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      
      Preexisting Inputs
      Use the existing artifacts listed here and in the phase file content below as review evidence. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `phase/impl_p02_validator_baseline_triage`
      - `phase/impl_p03_fix_packaging_install`
      - `validator/`
      - `validator-report.md`
      - `safe/scripts/check-validator-package-dev-probe.sh`
      - `safe/scripts/prepare-validator-local-port.py`
      - `safe/scripts/run-validator-libgcrypt.sh`
      - `safe/scripts/check-validator-port-evidence.py`
      - `safe/dist/safe-debs.manifest.json`
      - `validator-local/proof/local-port-debs-lock.json`
      - `validator-local/override-debs/libgcrypt/`
      - `validator-artifacts/p03-package-dev-probe/`
      - `validator-artifacts/p03-port-source/`
      - `validator-artifacts/p03-port-usage-smoke/`
      
      Specific Things To Verify
      - Verify the implementation for `impl_p03_fix_packaging_install` satisfies the phase file `.plan/phases/03-fix-packaging-install.md` for `Fix packaging and installation validator failures`.
      - Verify the implementation consumed or updated existing artifacts instead of refetching, recollecting, rediscovering, or regenerating them from scratch.
      - Verify the implementation committed the phase work and updated the required phase tag when the phase file requires it.
      - Run every command listed in the verification block below unless a command is impossible in the current environment; if a command cannot be run, treat that as a failure unless the phase file explicitly permits a documented exception.
      - Verify the success criteria below and report any missing artifact, unexpected file modification, unsafe scope expansion, or validator evidence gap.
      
      Verification Block From `.plan/phases/03-fix-packaging-install.md`
      
      - Phase ID: `check_p03_packaging_install_software_tester`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p03_fix_packaging_install`
      - Purpose: verify that package installation, development metadata, local
        override evidence, headers, linker inputs, and package metadata are fixed
        before source or usage compatibility phases depend on rebuilt packages.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p03_fix_packaging_install)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p02_validator_baseline_triage)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `bash -c 'if git diff --name-only HEAD^..HEAD | grep -E "^(validator|validator-local|validator-artifacts)(/|$)"; then exit 1; fi'`
        - `cmp -s rust-toolchain.toml safe/rust-toolchain.toml`
        - `safe/scripts/check-rust-toolchain.sh`
        - `safe/scripts/check-dependent-metadata.sh`
        - `cargo fmt --manifest-path safe/Cargo.toml -- --check`
        - `cargo build --manifest-path safe/Cargo.toml --release --locked --offline`
        - `safe/scripts/build-release-lib.sh`
        - `safe/scripts/check-abi.sh --all`
        - `safe/scripts/run-regression-tests.sh --all`
        - `safe/scripts/build-debs.sh`
        - `safe/scripts/check-deb-metadata.sh --dist safe/dist`
        - `python3 safe/scripts/prepare-validator-local-port.py --validator-dir validator --dist safe/dist --output-root validator-local`
        - `bash safe/scripts/check-validator-package-dev-probe.sh --dist safe/dist --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --artifact-root validator-artifacts/p03-package-dev-probe`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p03-port-source --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --kind source --record-casts || true`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p03-port-usage-smoke --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case usage-gpg-print-md --record-casts || true`
        - `python3 safe/scripts/check-validator-port-evidence.py --port-lock validator-local/proof/local-port-debs-lock.json --override-root validator-local/override-debs --artifact-root validator-artifacts/p03-port-source --artifact-root validator-artifacts/p03-port-usage-smoke`
        - `python3 -c 'import sys; from pathlib import Path; text=Path("validator-report.md").read_text(); required=["packaging-install","Port override installation evidence","Package/development probe","validator-artifacts/p03-package-dev-probe","phase/impl_p03_fix_packaging_install"]; missing=[item for item in required if item not in text]; sys.exit("missing report fields: "+", ".join(missing) if missing else 0)'`
        - `bash -c 'test -z "$(git status --short)"'`
      
      Success Criteria From `.plan/phases/03-fix-packaging-install.md`
      
      - Package install, header, linker, `libgcrypt-config`, pkg-config, Multi-Arch,
        dependency, symbol/SONAME, and override-evidence failures from phase 2 are
        fixed or documented as exact validator bugs.
      - The clean-container package/development probe passes with local safe
        `libgcrypt20` and `libgcrypt20-dev` installed from the validator override root,
        and the probe artifacts show successful direct-header, pkg-config, and
        `libgcrypt-config` compile/link/run paths.
      - Focused source and usage smoke validator runs produce port-mode artifacts with
        verified safe override package installation evidence for every non-skipped
        testcase. Source or usage compatibility failures with complete install
        evidence remain assigned to phase 4 or later buckets rather than blocking
        this packaging phase.
      - Existing safe build, ABI, package metadata, dependent metadata, and regression
        checks pass.
      
      Verdict Instruction
      End your response with exactly one of these lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check_p03_packaging_install_senior_tester
    type: check
    bounce_target: impl_p03_fix_packaging_install
    prompt: |
      Role: Senior reviewer for check phase `check_p03_packaging_install_senior_tester`.
      
      Review the current workspace for implement phase `impl_p03_fix_packaging_install`. Do not modify files. The fixed `bounce_target` for this check is `impl_p03_fix_packaging_install`.
      
      Workflow Topology
      - Phase order: 7
      - Phase ID: `check_p03_packaging_install_senior_tester`
      - Phase type: `check`
      - Fixed top-level `bounce_target`: `impl_p03_fix_packaging_install`
      - This verifier is an explicit top-level `check` phase from `.plan/workflow-structure.yaml` and must remain in structure order immediately after the implement block it verifies.
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      
      Preexisting Inputs
      Use the existing artifacts listed here and in the phase file content below as review evidence. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `phase/impl_p02_validator_baseline_triage`
      - `phase/impl_p03_fix_packaging_install`
      - `validator-report.md`
      - `safe/scripts/check-validator-package-dev-probe.sh`
      - `safe/debian/`
      - `safe/abi/`
      - `safe/cabi/`
      - `safe/build.rs`
      - `safe/tests/regressions/manifest.json`
      - `safe/tests/regressions/`
      
      Specific Things To Verify
      - Verify the implementation for `impl_p03_fix_packaging_install` satisfies the phase file `.plan/phases/03-fix-packaging-install.md` for `Fix packaging and installation validator failures`.
      - Verify the implementation consumed or updated existing artifacts instead of refetching, recollecting, rediscovering, or regenerating them from scratch.
      - Verify the implementation committed the phase work and updated the required phase tag when the phase file requires it.
      - Run every command listed in the verification block below unless a command is impossible in the current environment; if a command cannot be run, treat that as a failure unless the phase file explicitly permits a documented exception.
      - Verify the success criteria below and report any missing artifact, unexpected file modification, unsafe scope expansion, or validator evidence gap.
      
      Verification Block From `.plan/phases/03-fix-packaging-install.md`
      
      - Phase ID: `check_p03_packaging_install_senior_tester`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p03_fix_packaging_install`
      - Purpose: senior review that packaging/install fixes are safe-side changes,
        do not alter validator tests, and unblock all later port-mode validator runs.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p03_fix_packaging_install)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p02_validator_baseline_triage)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `git diff --stat HEAD^..HEAD`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `bash -c 'if git diff --name-only HEAD^..HEAD | grep -E "^(validator|validator-local|validator-artifacts)(/|$)"; then exit 1; fi'`
        - `test -x safe/scripts/check-validator-package-dev-probe.sh`
        - Review `validator-report.md`, `safe/debian/**`, `safe/abi/**`,
          `safe/cabi/**`, `safe/build.rs`, package scripts, the package/development
          probe, and new regression tests to confirm the phase only fixes package
          install, development metadata, override evidence, header, linker,
          pkg-config, `libgcrypt-config`, Multi-Arch, dependency, or symbol packaging
          failures.
      
      Success Criteria From `.plan/phases/03-fix-packaging-install.md`
      
      - Package install, header, linker, `libgcrypt-config`, pkg-config, Multi-Arch,
        dependency, symbol/SONAME, and override-evidence failures from phase 2 are
        fixed or documented as exact validator bugs.
      - The clean-container package/development probe passes with local safe
        `libgcrypt20` and `libgcrypt20-dev` installed from the validator override root,
        and the probe artifacts show successful direct-header, pkg-config, and
        `libgcrypt-config` compile/link/run paths.
      - Focused source and usage smoke validator runs produce port-mode artifacts with
        verified safe override package installation evidence for every non-skipped
        testcase. Source or usage compatibility failures with complete install
        evidence remain assigned to phase 4 or later buckets rather than blocking
        this packaging phase.
      - Existing safe build, ABI, package metadata, dependent metadata, and regression
        checks pass.
      
      Verdict Instruction
      End your response with exactly one of these lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl_p04_fix_source_api_failures
    type: implement
    bounce_target: null
    prompt: |
      Role: Software engineer implementing phase `impl_p04_fix_source_api_failures`.
      
      This prompt is self-contained; the agent sees only this prompt. Work from repository root `/home/yans/safelibs/pipeline/ports/port-libgcrypt`. Preserve unrelated user work and the existing dirty worktree state.
      
      Workflow Topology
      - Phase order: 8
      - Phase ID: `impl_p04_fix_source_api_failures`
      - Phase type: `implement`
      - Fixed top-level `bounce_target`: null
      - Verifier phases, in structure order immediately after this implement phase: `check_p04_source_api_software_tester`, then `check_p04_source_api_senior_tester`
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      - Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or agent-guided `bounce_targets` lists.
      - Use only fixed `bounce_target` values from `.plan/workflow-structure.yaml`.
      
      Preexisting Inputs
      Consume or update the existing artifacts listed here and in the phase file content below. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `phase/impl_p03_fix_packaging_install`
      - `validator/`
      - `validator-report.md`
      - `safe/scripts/run-validator-libgcrypt.sh`
      - `safe/scripts/check-validator-port-evidence.py`
      - `safe/scripts/validator-libgcrypt-skips.json`
      - `validator/tests/libgcrypt/tests/cases/source/aes-ctr-roundtrip.sh`
      - `validator/tests/libgcrypt/tests/cases/source/digest-sha256-smoke.sh`
      - `validator/tests/libgcrypt/tests/cases/source/hmac-sha256-smoke.sh`
      - `validator/tests/libgcrypt/tests/cases/source/mpi-arithmetic.sh`
      - `validator/tests/libgcrypt/tests/cases/source/nonce-generation.sh`
      - `safe/src/cipher/`
      - `safe/src/digest/`
      - `safe/src/mpi/`
      - `safe/src/random.rs`
      - `safe/src/global.rs`
      - `safe/tests/regressions/manifest.json`
      
      New Outputs
      Produce or update the outputs named here and in the phase file content below. Keep outputs phase-scoped, avoid unrelated edits, and commit all phase work to git before yielding.
      
      - Source API fixes in `safe/src/`.
      - Minimal regression scripts under `safe/tests/regressions/`, one per fixed
        source validator failure.
      - Updated `safe/tests/regressions/manifest.json`.
      - Updated `validator-report.md` marking fixed source API failures and the
        phase tag that fixed them.
      - If the phase 2 `source-api` bucket is empty, no safe code or regression test
        changes are required; the phase still produces a focused source-validator
        rerun, updates `validator-report.md` to mark the bucket clean with no fixes
        needed, and creates the required phase commit and tag.
      
      Git Requirement
      Commit work to git before yielding. Stage only files that are in scope for this phase. Do not commit unrelated dirty files or the nested `validator/` checkout.
      
      Phase File Content From `.plan/phases/04-fix-source-api-failures.md`
      
      # Phase Name
      
      Fix source API validator failures
      
      # Implement Phase ID
      
      `impl_p04_fix_source_api_failures`
      
      # Preexisting Inputs
      
      Consume these artifacts in place. If any listed generated artifact already exists, use it as phase input evidence instead of rediscovering or reimporting it.
      
      - Original source snapshot and safe implementation artifacts:
        `original/libgcrypt20-1.10.3/`, `safe/Cargo.toml`, `safe/Cargo.lock`,
        `safe/build.rs`, `safe/src/`, `safe/abi/`, `safe/cabi/`, and
        `safe/vendor/`.
      - Generated/package artifacts from prior phases: `safe/dist/*.deb`,
        `safe/dist/safe-debs.manifest.json`, and top-level package build outputs if
        present.
      - Existing test harnesses and scripts: `safe/scripts/*.sh`,
        `safe/tests/upstream/`, `safe/tests/compat/`, `safe/tests/dependents/`,
        and `safe/tests/regressions/`.
      - Existing metadata and documentation inputs: `dependents.json`,
        `relevant_cves.json`, `all_cves.json`, and `safe/docs/*.md`.
      - Dependent-image package locks and snapshot inputs:
        `safe/tests/dependents/metadata/install-packages.noble.lock`,
        `safe/tests/dependents/metadata/safe-debs.noble.lock`,
        `safe/tests/dependents/metadata/base-image.noble.digest`, and
        `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`.
      - Phase 3 packaging/install commit and report, plus the phase 2 baseline
        inventory and artifacts.
      - `safe/scripts/run-validator-libgcrypt.sh` and
        `safe/scripts/check-validator-port-evidence.py`.
      - `safe/scripts/validator-libgcrypt-skips.json`.
      - Validator source testcases:
        - `tests/libgcrypt/tests/cases/source/aes-ctr-roundtrip.sh`
        - `tests/libgcrypt/tests/cases/source/digest-sha256-smoke.sh`
        - `tests/libgcrypt/tests/cases/source/hmac-sha256-smoke.sh`
        - `tests/libgcrypt/tests/cases/source/mpi-arithmetic.sh`
        - `tests/libgcrypt/tests/cases/source/nonce-generation.sh`
      - Relevant safe modules:
        - `safe/src/cipher/mod.rs`
        - `safe/src/cipher/block.rs`
        - `safe/src/cipher/local.rs`
        - `safe/src/cipher/modes.rs`
        - `safe/src/cipher/registry.rs`
        - `safe/src/digest/mod.rs`
        - `safe/src/digest/algorithms.rs`
        - `safe/src/mpi/mod.rs`
        - `safe/src/mpi/arith.rs`
        - `safe/src/mpi/scan.rs`
        - `safe/src/random.rs`
        - `safe/src/global.rs`
        - `safe/abi/`
        - `safe/cabi/`
      
      # New Outputs
      
      - Source API fixes in `safe/src/`.
      - Minimal regression scripts under `safe/tests/regressions/`, one per fixed
        source validator failure.
      - Updated `safe/tests/regressions/manifest.json`.
      - Updated `validator-report.md` marking fixed source API failures and the
        phase tag that fixed them.
      - If the phase 2 `source-api` bucket is empty, no safe code or regression test
        changes are required; the phase still produces a focused source-validator
        rerun, updates `validator-report.md` to mark the bucket clean with no fixes
        needed, and creates the required phase commit and tag.
      
      # File Changes
      
      - `safe/src/cipher/*.rs` as required for AES CTR failures.
      - `safe/src/digest/*.rs` as required for digest or HMAC failures.
      - `safe/src/mpi/*.rs` as required for MPI arithmetic failures.
      - `safe/src/random.rs` or `safe/src/global.rs` as required for nonce failures.
      - `safe/abi/*` or `safe/cabi/*` only if the failure is an exported symbol,
        header, or C ABI forwarding issue.
      - `safe/tests/regressions/manifest.json`
      - `safe/tests/regressions/validator-*.sh` or
        `safe/tests/regressions/validator/*.sh`
      - `safe/scripts/validator-libgcrypt-skips.json` only for a clearly identified
        source-test validator bug, with an exact validator-commit-specific skip.
      - `safe/scripts/run-validator-libgcrypt.sh` only if the phase exposes a bug in
        the existing skip matching or result-writing behavior.
      - `validator-report.md`
      
      # Implementation Details
      
      ## Workflow-Wide Requirements For This Phase
      
      - Consume the listed preexisting inputs in place. Existing source snapshots, metadata, tests, packages, validator checkouts, and prior artifacts are inputs, not rediscovery or regeneration tasks unless this phase explicitly rebuilds generated package/validator artifacts.
      - Keep `validator/`, `validator-local/`, and `validator-artifacts/` external to the port repository. Do not commit nested validator contents or generated validator artifacts.
      - Preserve the Rust toolchain contract: root `rust-toolchain.toml` and `safe/rust-toolchain.toml` must stay identical and pinned to Rust `1.85.1` with `profile = "minimal"` and `components = ["rustfmt"]`.
      - Run `safe/scripts/check-rust-toolchain.sh` before every Cargo, release-library, Debian package, or dependent safe-image build.
      - Build any package artifacts used as validator inputs from a clean phase commit after `phase/impl_p04_fix_source_api_failures` points at `HEAD`. If tracked files or `validator-report.md` change after validation, amend the same phase commit, force-update the phase tag, rebuild packages, regenerate `validator-local`, and rerun the phase validator commands until the worktree is clean and artifacts match the final phase commit.
      - `validator-report.md` must identify the safe port by implement phase ID and `phase/impl_p04_fix_source_api_failures`, not by embedding the literal hash of the final report commit.
      
      ## Phase-Specific Details
      
      - For each source API failure in `validator-report.md`, first add a minimal
        regression test that compiles and runs a small C probe against the staged safe
        headers and library. The probe should be a reduced version of the validator
        testcase, not a copied full validator script.
      - If phase 2 recorded zero `source-api` failures, do not invent a regression or
        safe-side change. Consume the phase 2 inventory, rerun
        `safe/scripts/run-validator-libgcrypt.sh --kind source` in port mode with
        override evidence, update `validator-report.md` with the clean source-bucket
        result and artifact root, and make the required
        `impl_p04_fix_source_api_failures:` phase commit. If the report was already
        exact and no tracked file changed, create the phase commit with
        `git commit --allow-empty` before tagging it.
      - For `aes-ctr-roundtrip`, validate:
        - `gcry_cipher_open(&hd, GCRY_CIPHER_AES128, GCRY_CIPHER_MODE_CTR, 0)`
          succeeds.
        - `gcry_cipher_setkey` accepts a 16-byte AES-128 key.
        - `gcry_cipher_setctr` accepts a 16-byte counter.
        - Two encryptions with the same key/counter recover the original 16-byte
          buffer.
        - Likely code paths are `safe/src/cipher/mod.rs` lines 72-178,
          `safe/src/cipher/block.rs`, and `safe/src/cipher/local.rs`.
      - For `digest-sha256-smoke`, validate `gcry_md_hash_buffer(GCRY_MD_SHA256, ...)`
        returns the SHA-256 prefix `ba7816bf` for `abc`. Likely code paths are
        `safe/src/digest/mod.rs` lines 443-463 and
        `safe/src/digest/algorithms.rs`.
      - For `hmac-sha256-smoke`, validate `gcry_md_open(..., GCRY_MD_FLAG_HMAC)`,
        `gcry_md_setkey`, `gcry_md_write`, and `gcry_md_read` produce a stable
        HMAC-SHA256 result. Likely code paths are `safe/src/digest/mod.rs` lines
        291-616 and `safe/src/digest/algorithms.rs` HMAC helpers.
      - For `mpi-arithmetic`, validate `gcry_mpi_new`, `gcry_mpi_set_ui`,
        `gcry_mpi_add_ui`, `gcry_mpi_get_ui`, and `gcry_mpi_release`. Likely code
        paths are `safe/src/mpi/mod.rs` lines 741-814 and
        `safe/src/mpi/arith.rs` line 62.
      - For `nonce-generation`, validate `gcry_create_nonce` fills a 16-byte buffer
        with nonzero data without crashing before explicit initialization. Likely
        code path is `safe/src/random.rs` lines 87-98 and 282-283.
      - Preserve memory safety: no raw pointer expansion beyond C ABI boundaries
        unless required and locally justified.
      - Do not alter validator scripts. If a source testcase is invalid, document the
        exact validator bug in `validator-report.md` and add or update only the
        matching entry in `safe/scripts/validator-libgcrypt-skips.json`. The skip must
        name the current validator commit and source testcase ID, and the focused
        source run must still execute all unaffected source cases.
      - Commit fixes and report updates. End with one commit whose subject begins
        `impl_p04_fix_source_api_failures:` and tag it
        `phase/impl_p04_fix_source_api_failures`.
      
      # Verification Phases
      
      - Phase ID: `check_p04_source_api_software_tester`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p04_fix_source_api_failures`
      - Purpose: verify source API fixes through focused regressions and the
        validator source testcase set.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p04_fix_source_api_failures)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p03_fix_packaging_install)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `safe/scripts/check-rust-toolchain.sh`
        - `safe/scripts/check-dependent-metadata.sh`
        - `cargo fmt --manifest-path safe/Cargo.toml -- --check`
        - `cargo build --manifest-path safe/Cargo.toml --release --locked --offline`
        - `safe/scripts/build-release-lib.sh`
        - `safe/scripts/check-abi.sh --all`
        - `safe/scripts/run-regression-tests.sh --all`
        - `safe/scripts/build-debs.sh`
        - `safe/scripts/check-deb-metadata.sh --dist safe/dist`
        - `python3 safe/scripts/prepare-validator-local-port.py --validator-dir validator --dist safe/dist --output-root validator-local`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p04-port-source --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --kind source --record-casts`
        - `python3 safe/scripts/check-validator-port-evidence.py --port-lock validator-local/proof/local-port-debs-lock.json --override-root validator-local/override-debs --artifact-root validator-artifacts/p04-port-source`
      
      - Phase ID: `check_p04_source_api_senior_tester`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p04_fix_source_api_failures`
      - Purpose: review that every source API failure has a minimal safe-side
        regression and that validator files were not patched to hide failures.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p04_fix_source_api_failures)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p03_fix_packaging_install)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `git diff --stat HEAD^..HEAD`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `bash -c 'if git diff --name-only HEAD^..HEAD | grep -E "^(validator|validator-local|validator-artifacts)(/|$)"; then exit 1; fi'`
        - Review `validator-report.md`, `safe/tests/regressions/manifest.json`, and
          new `safe/tests/regressions/**` files for one regression per fixed source
          validator failure.
      
      # Success Criteria
      
      - All source API validator cases pass in safe mode.
      - All added regression tests pass through `safe/scripts/run-regression-tests.sh --all`.
      - ABI and package metadata checks still pass.
      
      # Git Commit Requirement
      
      The implementer must commit work to git before yielding. The commit subject must start with `impl_p04_fix_source_api_failures:`, and the implementer must force-update local tag `phase/impl_p04_fix_source_api_failures` to that phase commit before yielding.
  - id: check_p04_source_api_software_tester
    type: check
    bounce_target: impl_p04_fix_source_api_failures
    prompt: |
      Role: Software tester reviewer for check phase `check_p04_source_api_software_tester`.
      
      Review the current workspace for implement phase `impl_p04_fix_source_api_failures`. Do not modify files. The fixed `bounce_target` for this check is `impl_p04_fix_source_api_failures`.
      
      Workflow Topology
      - Phase order: 9
      - Phase ID: `check_p04_source_api_software_tester`
      - Phase type: `check`
      - Fixed top-level `bounce_target`: `impl_p04_fix_source_api_failures`
      - This verifier is an explicit top-level `check` phase from `.plan/workflow-structure.yaml` and must remain in structure order immediately after the implement block it verifies.
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      
      Preexisting Inputs
      Use the existing artifacts listed here and in the phase file content below as review evidence. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `phase/impl_p03_fix_packaging_install`
      - `phase/impl_p04_fix_source_api_failures`
      - `validator/`
      - `validator-report.md`
      - `safe/scripts/run-validator-libgcrypt.sh`
      - `safe/scripts/check-validator-port-evidence.py`
      - `safe/dist/safe-debs.manifest.json`
      - `validator-local/proof/local-port-debs-lock.json`
      - `validator-local/override-debs/libgcrypt/`
      - `validator-artifacts/p04-port-source/`
      
      Specific Things To Verify
      - Verify the implementation for `impl_p04_fix_source_api_failures` satisfies the phase file `.plan/phases/04-fix-source-api-failures.md` for `Fix source API validator failures`.
      - Verify the implementation consumed or updated existing artifacts instead of refetching, recollecting, rediscovering, or regenerating them from scratch.
      - Verify the implementation committed the phase work and updated the required phase tag when the phase file requires it.
      - Run every command listed in the verification block below unless a command is impossible in the current environment; if a command cannot be run, treat that as a failure unless the phase file explicitly permits a documented exception.
      - Verify the success criteria below and report any missing artifact, unexpected file modification, unsafe scope expansion, or validator evidence gap.
      
      Verification Block From `.plan/phases/04-fix-source-api-failures.md`
      
      - Phase ID: `check_p04_source_api_software_tester`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p04_fix_source_api_failures`
      - Purpose: verify source API fixes through focused regressions and the
        validator source testcase set.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p04_fix_source_api_failures)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p03_fix_packaging_install)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `safe/scripts/check-rust-toolchain.sh`
        - `safe/scripts/check-dependent-metadata.sh`
        - `cargo fmt --manifest-path safe/Cargo.toml -- --check`
        - `cargo build --manifest-path safe/Cargo.toml --release --locked --offline`
        - `safe/scripts/build-release-lib.sh`
        - `safe/scripts/check-abi.sh --all`
        - `safe/scripts/run-regression-tests.sh --all`
        - `safe/scripts/build-debs.sh`
        - `safe/scripts/check-deb-metadata.sh --dist safe/dist`
        - `python3 safe/scripts/prepare-validator-local-port.py --validator-dir validator --dist safe/dist --output-root validator-local`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p04-port-source --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --kind source --record-casts`
        - `python3 safe/scripts/check-validator-port-evidence.py --port-lock validator-local/proof/local-port-debs-lock.json --override-root validator-local/override-debs --artifact-root validator-artifacts/p04-port-source`
      
      Success Criteria From `.plan/phases/04-fix-source-api-failures.md`
      
      - All source API validator cases pass in safe mode.
      - All added regression tests pass through `safe/scripts/run-regression-tests.sh --all`.
      - ABI and package metadata checks still pass.
      
      Verdict Instruction
      End your response with exactly one of these lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check_p04_source_api_senior_tester
    type: check
    bounce_target: impl_p04_fix_source_api_failures
    prompt: |
      Role: Senior reviewer for check phase `check_p04_source_api_senior_tester`.
      
      Review the current workspace for implement phase `impl_p04_fix_source_api_failures`. Do not modify files. The fixed `bounce_target` for this check is `impl_p04_fix_source_api_failures`.
      
      Workflow Topology
      - Phase order: 10
      - Phase ID: `check_p04_source_api_senior_tester`
      - Phase type: `check`
      - Fixed top-level `bounce_target`: `impl_p04_fix_source_api_failures`
      - This verifier is an explicit top-level `check` phase from `.plan/workflow-structure.yaml` and must remain in structure order immediately after the implement block it verifies.
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      
      Preexisting Inputs
      Use the existing artifacts listed here and in the phase file content below as review evidence. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `phase/impl_p03_fix_packaging_install`
      - `phase/impl_p04_fix_source_api_failures`
      - `validator-report.md`
      - `safe/tests/regressions/manifest.json`
      - `safe/tests/regressions/`
      
      Specific Things To Verify
      - Verify the implementation for `impl_p04_fix_source_api_failures` satisfies the phase file `.plan/phases/04-fix-source-api-failures.md` for `Fix source API validator failures`.
      - Verify the implementation consumed or updated existing artifacts instead of refetching, recollecting, rediscovering, or regenerating them from scratch.
      - Verify the implementation committed the phase work and updated the required phase tag when the phase file requires it.
      - Run every command listed in the verification block below unless a command is impossible in the current environment; if a command cannot be run, treat that as a failure unless the phase file explicitly permits a documented exception.
      - Verify the success criteria below and report any missing artifact, unexpected file modification, unsafe scope expansion, or validator evidence gap.
      
      Verification Block From `.plan/phases/04-fix-source-api-failures.md`
      
      - Phase ID: `check_p04_source_api_senior_tester`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p04_fix_source_api_failures`
      - Purpose: review that every source API failure has a minimal safe-side
        regression and that validator files were not patched to hide failures.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p04_fix_source_api_failures)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p03_fix_packaging_install)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `git diff --stat HEAD^..HEAD`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `bash -c 'if git diff --name-only HEAD^..HEAD | grep -E "^(validator|validator-local|validator-artifacts)(/|$)"; then exit 1; fi'`
        - Review `validator-report.md`, `safe/tests/regressions/manifest.json`, and
          new `safe/tests/regressions/**` files for one regression per fixed source
          validator failure.
      
      Success Criteria From `.plan/phases/04-fix-source-api-failures.md`
      
      - All source API validator cases pass in safe mode.
      - All added regression tests pass through `safe/scripts/run-regression-tests.sh --all`.
      - ABI and package metadata checks still pass.
      
      Verdict Instruction
      End your response with exactly one of these lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl_p05_fix_gpg_symmetric_digest_random
    type: implement
    bounce_target: null
    prompt: |
      Role: Software engineer implementing phase `impl_p05_fix_gpg_symmetric_digest_random`.
      
      This prompt is self-contained; the agent sees only this prompt. Work from repository root `/home/yans/safelibs/pipeline/ports/port-libgcrypt`. Preserve unrelated user work and the existing dirty worktree state.
      
      Workflow Topology
      - Phase order: 11
      - Phase ID: `impl_p05_fix_gpg_symmetric_digest_random`
      - Phase type: `implement`
      - Fixed top-level `bounce_target`: null
      - Verifier phases, in structure order immediately after this implement phase: `check_p05_symmetric_digest_random_software_tester`, then `check_p05_symmetric_digest_random_senior_tester`
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      - Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or agent-guided `bounce_targets` lists.
      - Use only fixed `bounce_target` values from `.plan/workflow-structure.yaml`.
      
      Preexisting Inputs
      Consume or update the existing artifacts listed here and in the phase file content below. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `phase/impl_p04_fix_source_api_failures`
      - `validator/`
      - `validator-report.md`
      - `safe/scripts/run-validator-libgcrypt.sh`
      - `safe/scripts/check-validator-port-evidence.py`
      - `safe/scripts/validator-libgcrypt-skips.json`
      - `safe/src/cipher/`
      - `safe/src/digest/`
      - `safe/src/mac.rs`
      - `safe/src/kdf.rs`
      - `safe/src/random.rs`
      - `safe/src/drbg.rs`
      - `safe/src/os_rng.rs`
      - `safe/src/global.rs`
      - `safe/src/sexp.rs`
      - `safe/tests/regressions/manifest.json`
      
      New Outputs
      Produce or update the outputs named here and in the phase file content below. Keep outputs phase-scoped, avoid unrelated edits, and commit all phase work to git before yielding.
      
      - Safe-side fixes for GPG digest, random, symmetric encryption, S2K/KDF, and
        related packet behavior failures.
      - Regression tests for each fixed validator usage failure in this bucket.
      - Updated `validator-report.md`.
      - If the phase 2 baseline inventory plus phase 4 source outcome contains no
        unresolved failures in the `usage-symmetric-digest-random` bucket, no safe
        code or regression test changes are required; the phase still reruns the
        focused bucket subsets with
        port override evidence, updates `validator-report.md` to mark the bucket
        clean with no fixes needed, and creates the required phase commit and tag.
      
      Git Requirement
      Commit work to git before yielding. Stage only files that are in scope for this phase. Do not commit unrelated dirty files or the nested `validator/` checkout.
      
      Phase File Content From `.plan/phases/05-fix-gpg-symmetric-digest-random.md`
      
      # Phase Name
      
      Fix GPG symmetric, digest, and random usage failures
      
      # Implement Phase ID
      
      `impl_p05_fix_gpg_symmetric_digest_random`
      
      # Preexisting Inputs
      
      Consume these artifacts in place. If any listed generated artifact already exists, use it as phase input evidence instead of rediscovering or reimporting it.
      
      - Original source snapshot and safe implementation artifacts:
        `original/libgcrypt20-1.10.3/`, `safe/Cargo.toml`, `safe/Cargo.lock`,
        `safe/build.rs`, `safe/src/`, `safe/abi/`, `safe/cabi/`, and
        `safe/vendor/`.
      - Generated/package artifacts from prior phases: `safe/dist/*.deb`,
        `safe/dist/safe-debs.manifest.json`, and top-level package build outputs if
        present.
      - Existing test harnesses and scripts: `safe/scripts/*.sh`,
        `safe/tests/upstream/`, `safe/tests/compat/`, `safe/tests/dependents/`,
        and `safe/tests/regressions/`.
      - Existing metadata and documentation inputs: `dependents.json`,
        `relevant_cves.json`, `all_cves.json`, and `safe/docs/*.md`.
      - Dependent-image package locks and snapshot inputs:
        `safe/tests/dependents/metadata/install-packages.noble.lock`,
        `safe/tests/dependents/metadata/safe-debs.noble.lock`,
        `safe/tests/dependents/metadata/base-image.noble.digest`, and
        `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`.
      - Phase 4 source API fixes and report.
      - Validator wrapper and port evidence checker from phase 1.
      - `safe/scripts/validator-libgcrypt-skips.json`.
      - Validator usage failures in the
        `usage-symmetric-digest-random` bucket from `validator-report.md`.
      - Existing safe modules:
        - `safe/src/cipher/*.rs`
        - `safe/src/digest/*.rs`
        - `safe/src/mac.rs`
        - `safe/src/kdf.rs`
        - `safe/src/random.rs`
        - `safe/src/drbg.rs`
        - `safe/src/os_rng.rs`
        - `safe/src/global.rs`
        - `safe/src/sexp.rs`
      
      # New Outputs
      
      - Safe-side fixes for GPG digest, random, symmetric encryption, S2K/KDF, and
        related packet behavior failures.
      - Regression tests for each fixed validator usage failure in this bucket.
      - Updated `validator-report.md`.
      - If the phase 2 baseline inventory plus phase 4 source outcome contains no
        unresolved failures in the `usage-symmetric-digest-random` bucket, no safe
        code or regression test changes are required; the phase still reruns the
        focused bucket subsets with
        port override evidence, updates `validator-report.md` to mark the bucket
        clean with no fixes needed, and creates the required phase commit and tag.
      
      # File Changes
      
      - `safe/src/cipher/*.rs`
      - `safe/src/digest/*.rs`
      - `safe/src/mac.rs`
      - `safe/src/kdf.rs`
      - `safe/src/random.rs`
      - `safe/src/drbg.rs`
      - `safe/src/os_rng.rs`
      - `safe/src/global.rs`
      - `safe/src/sexp.rs` only if packet/S-expression behavior depends on it.
      - `safe/tests/regressions/manifest.json`
      - `safe/tests/regressions/**`
      - `safe/scripts/validator-libgcrypt-skips.json` only for a clearly identified
        validator bug in this bucket, with an exact validator-commit-specific skip.
      - `safe/scripts/run-validator-libgcrypt.sh` only if the phase exposes a bug in
        the existing skip matching or result-writing behavior.
      - `validator-report.md`
      
      # Implementation Details
      
      ## Workflow-Wide Requirements For This Phase
      
      - Consume the listed preexisting inputs in place. Existing source snapshots, metadata, tests, packages, validator checkouts, and prior artifacts are inputs, not rediscovery or regeneration tasks unless this phase explicitly rebuilds generated package/validator artifacts.
      - Keep `validator/`, `validator-local/`, and `validator-artifacts/` external to the port repository. Do not commit nested validator contents or generated validator artifacts.
      - Preserve the Rust toolchain contract: root `rust-toolchain.toml` and `safe/rust-toolchain.toml` must stay identical and pinned to Rust `1.85.1` with `profile = "minimal"` and `components = ["rustfmt"]`.
      - Run `safe/scripts/check-rust-toolchain.sh` before every Cargo, release-library, Debian package, or dependent safe-image build.
      - Build any package artifacts used as validator inputs from a clean phase commit after `phase/impl_p05_fix_gpg_symmetric_digest_random` points at `HEAD`. If tracked files or `validator-report.md` change after validation, amend the same phase commit, force-update the phase tag, rebuild packages, regenerate `validator-local`, and rerun the phase validator commands until the worktree is clean and artifacts match the final phase commit.
      - `validator-report.md` must identify the safe port by implement phase ID and `phase/impl_p05_fix_gpg_symmetric_digest_random`, not by embedding the literal hash of the final report commit.
      
      ## Phase-Specific Details
      
      - Use the failure inventory from phase 2 and any relevant phase 4 leftovers.
        Do not rediscover from scratch except by rerunning the focused validator
        subsets.
      - If the inventory has zero unresolved failures for this bucket, do not invent a
        regression or safe-side change. Rerun every focused phase 5 validator subset
        listed below in port mode with override evidence, update
        `validator-report.md` with the clean bucket result and artifact roots, and
        make the required `impl_p05_fix_gpg_symmetric_digest_random:` phase commit.
        If the report was already exact and no tracked file changed, create the phase
        commit with `git commit --allow-empty` before tagging it.
      - Add one minimal regression per validator failure before fixing it. Regression
        tests may drive `gpg` against installed safe packages only when a reduced C
        probe cannot reproduce the defect.
      - For digest failures, compare libgcrypt algorithm names, aliases, digest
        lengths, HMAC behavior, and known answer prefixes against original behavior.
      - For symmetric failures, focus on AES128/192/256, 3DES, Blowfish, CAST5,
        Camellia, Twofish, stream/block mode state resets, IV/CTR handling, padding
        expectations, and error codes used by GPG.
      - For random failures, preserve `gcry_check_version(NULL)` before first use,
        RNG initialization state, `gcry_create_nonce`, `gcry_randomize`,
        `gcry_random_bytes`, `GCRY_WEAK_RANDOM`, `GCRY_STRONG_RANDOM`, and zero-byte
        requests.
      - For S2K/KDF failures, inspect `safe/src/kdf.rs` and GPG usage around salted
        and iterated modes. Match libgcrypt error codes where GPG branches on them.
      - Focused validator coverage for this phase must include all current
        digest/random/symmetric and packet/preference case families:
        `usage-gpg-print-md`, `usage-gpg-print-md-*`,
        `usage-gpg-print-mds-multi`, `usage-gpg-gen-random-*`,
        `usage-gpg-symmetric-*`, `usage-gpg-list-config-*`,
        `usage-gpg-list-packets`, `usage-gpg-list-packets-*`, and
        `usage-gpg-personal-*-prefs-*`.
      - If a validator defect is the only reason a focused case cannot pass, document
        the exact defect in `validator-report.md` and add or update only the matching
        entry in `safe/scripts/validator-libgcrypt-skips.json`. The skip must name the
        current validator commit and testcase ID, and the focused subset run must
        still execute all unaffected cases in this bucket.
      - Update `validator-report.md` with each fixed testcase ID, regression test ID,
        changed safe module, and verification artifact path.
      - Commit fixes and report updates. End with one commit whose subject begins
        `impl_p05_fix_gpg_symmetric_digest_random:` and tag it
        `phase/impl_p05_fix_gpg_symmetric_digest_random`.
      
      # Verification Phases
      
      - Phase ID: `check_p05_symmetric_digest_random_software_tester`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p05_fix_gpg_symmetric_digest_random`
      - Purpose: verify safe fixes against GPG usage cases that exercise symmetric
        ciphers, message digests, random generation, S2K/KDF behavior, and packet
        parsing output.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p05_fix_gpg_symmetric_digest_random)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p04_fix_source_api_failures)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `safe/scripts/check-rust-toolchain.sh`
        - `cargo fmt --manifest-path safe/Cargo.toml -- --check`
        - `cargo build --manifest-path safe/Cargo.toml --release --locked --offline`
        - `safe/scripts/build-release-lib.sh`
        - `safe/scripts/check-abi.sh --all`
        - `safe/scripts/run-upstream-tests.sh random hashtest hmac t-kdf aeswrap basic`
        - `safe/scripts/run-regression-tests.sh --all`
        - `safe/scripts/build-debs.sh`
        - `safe/scripts/check-deb-metadata.sh --dist safe/dist`
        - `python3 safe/scripts/prepare-validator-local-port.py --validator-dir validator --dist safe/dist --output-root validator-local`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p05-port-print-md --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-print-md-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p05-port-print-md-base --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case usage-gpg-print-md --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p05-port-print-mds --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case usage-gpg-print-mds-multi --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p05-port-random --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-gen-random-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p05-port-symmetric --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-symmetric-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p05-port-list-config --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-list-config-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p05-port-list-packets-base --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case usage-gpg-list-packets --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p05-port-list-packets --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-list-packets-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p05-port-personal-prefs --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-personal-*-prefs-*' --record-casts`
        - `python3 safe/scripts/check-validator-port-evidence.py --port-lock validator-local/proof/local-port-debs-lock.json --override-root validator-local/override-debs --artifact-root validator-artifacts/p05-port-print-md --artifact-root validator-artifacts/p05-port-print-md-base --artifact-root validator-artifacts/p05-port-print-mds --artifact-root validator-artifacts/p05-port-random --artifact-root validator-artifacts/p05-port-symmetric --artifact-root validator-artifacts/p05-port-list-config --artifact-root validator-artifacts/p05-port-list-packets-base --artifact-root validator-artifacts/p05-port-list-packets --artifact-root validator-artifacts/p05-port-personal-prefs`
      
      - Phase ID: `check_p05_symmetric_digest_random_senior_tester`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p05_fix_gpg_symmetric_digest_random`
      - Purpose: senior review of algorithm compatibility, error semantics,
        regression coverage, and no validator-suite edits.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p05_fix_gpg_symmetric_digest_random)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p04_fix_source_api_failures)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `git diff --stat HEAD^..HEAD`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `bash -c 'if git diff --name-only HEAD^..HEAD | grep -E "^(validator|validator-local|validator-artifacts)(/|$)"; then exit 1; fi'`
        - `bash -c 'rg -n "todo!|unimplemented!|Not implemented|safe_gcry_stub_zero" safe/src safe/cabi safe/build.rs && exit 1 || true'`
        - Review `validator-report.md` and new regression tests for one safe-side
          reproduction per fixed validator failure in this bucket.
      
      # Success Criteria
      
      - Focused GPG digest/random/symmetric, packet, and preference validator
        subsets pass in safe mode with verified safe override package installation
        evidence for every port-mode testcase.
      - Existing upstream `random`, `hashtest`, `hmac`, `t-kdf`, `aeswrap`, and
        `basic` tests still pass.
      - No validator files were modified.
      
      # Git Commit Requirement
      
      The implementer must commit work to git before yielding. The commit subject must start with `impl_p05_fix_gpg_symmetric_digest_random:`, and the implementer must force-update local tag `phase/impl_p05_fix_gpg_symmetric_digest_random` to that phase commit before yielding.
  - id: check_p05_symmetric_digest_random_software_tester
    type: check
    bounce_target: impl_p05_fix_gpg_symmetric_digest_random
    prompt: |
      Role: Software tester reviewer for check phase `check_p05_symmetric_digest_random_software_tester`.
      
      Review the current workspace for implement phase `impl_p05_fix_gpg_symmetric_digest_random`. Do not modify files. The fixed `bounce_target` for this check is `impl_p05_fix_gpg_symmetric_digest_random`.
      
      Workflow Topology
      - Phase order: 12
      - Phase ID: `check_p05_symmetric_digest_random_software_tester`
      - Phase type: `check`
      - Fixed top-level `bounce_target`: `impl_p05_fix_gpg_symmetric_digest_random`
      - This verifier is an explicit top-level `check` phase from `.plan/workflow-structure.yaml` and must remain in structure order immediately after the implement block it verifies.
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      
      Preexisting Inputs
      Use the existing artifacts listed here and in the phase file content below as review evidence. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `phase/impl_p04_fix_source_api_failures`
      - `phase/impl_p05_fix_gpg_symmetric_digest_random`
      - `validator/`
      - `validator-report.md`
      - `safe/scripts/run-validator-libgcrypt.sh`
      - `safe/scripts/check-validator-port-evidence.py`
      - `safe/dist/safe-debs.manifest.json`
      - `validator-local/proof/local-port-debs-lock.json`
      - `validator-local/override-debs/libgcrypt/`
      - `validator-artifacts/p05-port-print-md/`
      - `validator-artifacts/p05-port-print-md-base/`
      - `validator-artifacts/p05-port-print-mds/`
      - `validator-artifacts/p05-port-random/`
      - `validator-artifacts/p05-port-symmetric/`
      - `validator-artifacts/p05-port-list-config/`
      - `validator-artifacts/p05-port-list-packets-base/`
      - `validator-artifacts/p05-port-list-packets/`
      - `validator-artifacts/p05-port-personal-prefs/`
      
      Specific Things To Verify
      - Verify the implementation for `impl_p05_fix_gpg_symmetric_digest_random` satisfies the phase file `.plan/phases/05-fix-gpg-symmetric-digest-random.md` for `Fix GPG symmetric, digest, and random usage failures`.
      - Verify the implementation consumed or updated existing artifacts instead of refetching, recollecting, rediscovering, or regenerating them from scratch.
      - Verify the implementation committed the phase work and updated the required phase tag when the phase file requires it.
      - Run every command listed in the verification block below unless a command is impossible in the current environment; if a command cannot be run, treat that as a failure unless the phase file explicitly permits a documented exception.
      - Verify the success criteria below and report any missing artifact, unexpected file modification, unsafe scope expansion, or validator evidence gap.
      
      Verification Block From `.plan/phases/05-fix-gpg-symmetric-digest-random.md`
      
      - Phase ID: `check_p05_symmetric_digest_random_software_tester`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p05_fix_gpg_symmetric_digest_random`
      - Purpose: verify safe fixes against GPG usage cases that exercise symmetric
        ciphers, message digests, random generation, S2K/KDF behavior, and packet
        parsing output.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p05_fix_gpg_symmetric_digest_random)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p04_fix_source_api_failures)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `safe/scripts/check-rust-toolchain.sh`
        - `cargo fmt --manifest-path safe/Cargo.toml -- --check`
        - `cargo build --manifest-path safe/Cargo.toml --release --locked --offline`
        - `safe/scripts/build-release-lib.sh`
        - `safe/scripts/check-abi.sh --all`
        - `safe/scripts/run-upstream-tests.sh random hashtest hmac t-kdf aeswrap basic`
        - `safe/scripts/run-regression-tests.sh --all`
        - `safe/scripts/build-debs.sh`
        - `safe/scripts/check-deb-metadata.sh --dist safe/dist`
        - `python3 safe/scripts/prepare-validator-local-port.py --validator-dir validator --dist safe/dist --output-root validator-local`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p05-port-print-md --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-print-md-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p05-port-print-md-base --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case usage-gpg-print-md --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p05-port-print-mds --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case usage-gpg-print-mds-multi --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p05-port-random --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-gen-random-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p05-port-symmetric --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-symmetric-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p05-port-list-config --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-list-config-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p05-port-list-packets-base --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case usage-gpg-list-packets --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p05-port-list-packets --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-list-packets-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p05-port-personal-prefs --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-personal-*-prefs-*' --record-casts`
        - `python3 safe/scripts/check-validator-port-evidence.py --port-lock validator-local/proof/local-port-debs-lock.json --override-root validator-local/override-debs --artifact-root validator-artifacts/p05-port-print-md --artifact-root validator-artifacts/p05-port-print-md-base --artifact-root validator-artifacts/p05-port-print-mds --artifact-root validator-artifacts/p05-port-random --artifact-root validator-artifacts/p05-port-symmetric --artifact-root validator-artifacts/p05-port-list-config --artifact-root validator-artifacts/p05-port-list-packets-base --artifact-root validator-artifacts/p05-port-list-packets --artifact-root validator-artifacts/p05-port-personal-prefs`
      
      Success Criteria From `.plan/phases/05-fix-gpg-symmetric-digest-random.md`
      
      - Focused GPG digest/random/symmetric, packet, and preference validator
        subsets pass in safe mode with verified safe override package installation
        evidence for every port-mode testcase.
      - Existing upstream `random`, `hashtest`, `hmac`, `t-kdf`, `aeswrap`, and
        `basic` tests still pass.
      - No validator files were modified.
      
      Verdict Instruction
      End your response with exactly one of these lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check_p05_symmetric_digest_random_senior_tester
    type: check
    bounce_target: impl_p05_fix_gpg_symmetric_digest_random
    prompt: |
      Role: Senior reviewer for check phase `check_p05_symmetric_digest_random_senior_tester`.
      
      Review the current workspace for implement phase `impl_p05_fix_gpg_symmetric_digest_random`. Do not modify files. The fixed `bounce_target` for this check is `impl_p05_fix_gpg_symmetric_digest_random`.
      
      Workflow Topology
      - Phase order: 13
      - Phase ID: `check_p05_symmetric_digest_random_senior_tester`
      - Phase type: `check`
      - Fixed top-level `bounce_target`: `impl_p05_fix_gpg_symmetric_digest_random`
      - This verifier is an explicit top-level `check` phase from `.plan/workflow-structure.yaml` and must remain in structure order immediately after the implement block it verifies.
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      
      Preexisting Inputs
      Use the existing artifacts listed here and in the phase file content below as review evidence. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `phase/impl_p04_fix_source_api_failures`
      - `phase/impl_p05_fix_gpg_symmetric_digest_random`
      - `validator-report.md`
      - `safe/src/cipher/`
      - `safe/src/digest/`
      - `safe/src/mac.rs`
      - `safe/src/kdf.rs`
      - `safe/src/random.rs`
      - `safe/src/drbg.rs`
      - `safe/src/os_rng.rs`
      - `safe/src/global.rs`
      - `safe/src/sexp.rs`
      - `safe/tests/regressions/manifest.json`
      - `safe/tests/regressions/`
      
      Specific Things To Verify
      - Verify the implementation for `impl_p05_fix_gpg_symmetric_digest_random` satisfies the phase file `.plan/phases/05-fix-gpg-symmetric-digest-random.md` for `Fix GPG symmetric, digest, and random usage failures`.
      - Verify the implementation consumed or updated existing artifacts instead of refetching, recollecting, rediscovering, or regenerating them from scratch.
      - Verify the implementation committed the phase work and updated the required phase tag when the phase file requires it.
      - Run every command listed in the verification block below unless a command is impossible in the current environment; if a command cannot be run, treat that as a failure unless the phase file explicitly permits a documented exception.
      - Verify the success criteria below and report any missing artifact, unexpected file modification, unsafe scope expansion, or validator evidence gap.
      
      Verification Block From `.plan/phases/05-fix-gpg-symmetric-digest-random.md`
      
      - Phase ID: `check_p05_symmetric_digest_random_senior_tester`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p05_fix_gpg_symmetric_digest_random`
      - Purpose: senior review of algorithm compatibility, error semantics,
        regression coverage, and no validator-suite edits.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p05_fix_gpg_symmetric_digest_random)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p04_fix_source_api_failures)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `git diff --stat HEAD^..HEAD`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `bash -c 'if git diff --name-only HEAD^..HEAD | grep -E "^(validator|validator-local|validator-artifacts)(/|$)"; then exit 1; fi'`
        - `bash -c 'rg -n "todo!|unimplemented!|Not implemented|safe_gcry_stub_zero" safe/src safe/cabi safe/build.rs && exit 1 || true'`
        - Review `validator-report.md` and new regression tests for one safe-side
          reproduction per fixed validator failure in this bucket.
      
      Success Criteria From `.plan/phases/05-fix-gpg-symmetric-digest-random.md`
      
      - Focused GPG digest/random/symmetric, packet, and preference validator
        subsets pass in safe mode with verified safe override package installation
        evidence for every port-mode testcase.
      - Existing upstream `random`, `hashtest`, `hmac`, `t-kdf`, `aeswrap`, and
        `basic` tests still pass.
      - No validator files were modified.
      
      Verdict Instruction
      End your response with exactly one of these lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl_p06_fix_gpg_pubkey_keyring
    type: implement
    bounce_target: null
    prompt: |
      Role: Software engineer implementing phase `impl_p06_fix_gpg_pubkey_keyring`.
      
      This prompt is self-contained; the agent sees only this prompt. Work from repository root `/home/yans/safelibs/pipeline/ports/port-libgcrypt`. Preserve unrelated user work and the existing dirty worktree state.
      
      Workflow Topology
      - Phase order: 14
      - Phase ID: `impl_p06_fix_gpg_pubkey_keyring`
      - Phase type: `implement`
      - Fixed top-level `bounce_target`: null
      - Verifier phases, in structure order immediately after this implement phase: `check_p06_pubkey_keyring_software_tester`, then `check_p06_pubkey_keyring_senior_tester`
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      - Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or agent-guided `bounce_targets` lists.
      - Use only fixed `bounce_target` values from `.plan/workflow-structure.yaml`.
      
      Preexisting Inputs
      Consume or update the existing artifacts listed here and in the phase file content below. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `phase/impl_p05_fix_gpg_symmetric_digest_random`
      - `validator/`
      - `validator-report.md`
      - `safe/scripts/run-validator-libgcrypt.sh`
      - `safe/scripts/check-validator-port-evidence.py`
      - `safe/scripts/validator-libgcrypt-skips.json`
      - `safe/src/pubkey/`
      - `safe/src/sexp.rs`
      - `safe/src/mpi/`
      - `safe/src/random.rs`
      - `safe/src/digest/`
      - `safe/tests/regressions/manifest.json`
      
      New Outputs
      Produce or update the outputs named here and in the phase file content below. Keep outputs phase-scoped, avoid unrelated edits, and commit all phase work to git before yielding.
      
      - Safe-side fixes for GPG key generation, import/export, list/fingerprint,
        sign/verify, recipient encryption/decryption, hidden recipient, trustdb,
        ownertrust, secret key, keygrip, revocation, and packet metadata failures.
      - Regression tests for each fixed public-key/keyring validator failure.
      - Updated `validator-report.md`.
      - If the phase 2 through phase 5 inventory contains no unresolved
        `usage-pubkey-keyring` failures, no safe code or regression test changes are
        required; the phase still reruns the focused public-key/keyring subsets with
        port override evidence, updates `validator-report.md` to mark the bucket
        clean with no fixes needed, and creates the required phase commit and tag.
      
      Git Requirement
      Commit work to git before yielding. Stage only files that are in scope for this phase. Do not commit unrelated dirty files or the nested `validator/` checkout.
      
      Phase File Content From `.plan/phases/06-fix-gpg-pubkey-keyring.md`
      
      # Phase Name
      
      Fix GPG public-key, signing, encryption, and keyring usage failures
      
      # Implement Phase ID
      
      `impl_p06_fix_gpg_pubkey_keyring`
      
      # Preexisting Inputs
      
      Consume these artifacts in place. If any listed generated artifact already exists, use it as phase input evidence instead of rediscovering or reimporting it.
      
      - Original source snapshot and safe implementation artifacts:
        `original/libgcrypt20-1.10.3/`, `safe/Cargo.toml`, `safe/Cargo.lock`,
        `safe/build.rs`, `safe/src/`, `safe/abi/`, `safe/cabi/`, and
        `safe/vendor/`.
      - Generated/package artifacts from prior phases: `safe/dist/*.deb`,
        `safe/dist/safe-debs.manifest.json`, and top-level package build outputs if
        present.
      - Existing test harnesses and scripts: `safe/scripts/*.sh`,
        `safe/tests/upstream/`, `safe/tests/compat/`, `safe/tests/dependents/`,
        and `safe/tests/regressions/`.
      - Existing metadata and documentation inputs: `dependents.json`,
        `relevant_cves.json`, `all_cves.json`, and `safe/docs/*.md`.
      - Dependent-image package locks and snapshot inputs:
        `safe/tests/dependents/metadata/install-packages.noble.lock`,
        `safe/tests/dependents/metadata/safe-debs.noble.lock`,
        `safe/tests/dependents/metadata/base-image.noble.digest`, and
        `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`.
      - Phase 5 commit and report.
      - Validator wrapper and port evidence checker from phase 1.
      - `safe/scripts/validator-libgcrypt-skips.json`.
      - Validator failures in the `usage-pubkey-keyring` bucket.
      - Existing safe modules:
        - `safe/src/pubkey/rsa.rs`
        - `safe/src/pubkey/dsa.rs`
        - `safe/src/pubkey/ecc.rs`
        - `safe/src/pubkey/elgamal.rs`
        - `safe/src/pubkey/encoding.rs`
        - `safe/src/pubkey/keygrip.rs`
        - `safe/src/sexp.rs`
        - `safe/src/mpi/*.rs`
        - `safe/src/random.rs`
        - `safe/src/digest/*.rs`
      
      # New Outputs
      
      - Safe-side fixes for GPG key generation, import/export, list/fingerprint,
        sign/verify, recipient encryption/decryption, hidden recipient, trustdb,
        ownertrust, secret key, keygrip, revocation, and packet metadata failures.
      - Regression tests for each fixed public-key/keyring validator failure.
      - Updated `validator-report.md`.
      - If the phase 2 through phase 5 inventory contains no unresolved
        `usage-pubkey-keyring` failures, no safe code or regression test changes are
        required; the phase still reruns the focused public-key/keyring subsets with
        port override evidence, updates `validator-report.md` to mark the bucket
        clean with no fixes needed, and creates the required phase commit and tag.
      
      # File Changes
      
      - `safe/src/pubkey/*.rs`
      - `safe/src/sexp.rs`
      - `safe/src/mpi/*.rs`
      - `safe/src/random.rs`
      - `safe/src/digest/*.rs`
      - `safe/abi/*` or `safe/cabi/*` only for public ABI/header issues.
      - `safe/tests/regressions/manifest.json`
      - `safe/tests/regressions/**`
      - `safe/scripts/validator-libgcrypt-skips.json` only for a clearly identified
        validator bug in this bucket, with an exact validator-commit-specific skip.
      - `safe/scripts/run-validator-libgcrypt.sh` only if the phase exposes a bug in
        the existing skip matching or result-writing behavior.
      - `validator-report.md`
      
      # Implementation Details
      
      ## Workflow-Wide Requirements For This Phase
      
      - Consume the listed preexisting inputs in place. Existing source snapshots, metadata, tests, packages, validator checkouts, and prior artifacts are inputs, not rediscovery or regeneration tasks unless this phase explicitly rebuilds generated package/validator artifacts.
      - Keep `validator/`, `validator-local/`, and `validator-artifacts/` external to the port repository. Do not commit nested validator contents or generated validator artifacts.
      - Preserve the Rust toolchain contract: root `rust-toolchain.toml` and `safe/rust-toolchain.toml` must stay identical and pinned to Rust `1.85.1` with `profile = "minimal"` and `components = ["rustfmt"]`.
      - Run `safe/scripts/check-rust-toolchain.sh` before every Cargo, release-library, Debian package, or dependent safe-image build.
      - Build any package artifacts used as validator inputs from a clean phase commit after `phase/impl_p06_fix_gpg_pubkey_keyring` points at `HEAD`. If tracked files or `validator-report.md` change after validation, amend the same phase commit, force-update the phase tag, rebuild packages, regenerate `validator-local`, and rerun the phase validator commands until the worktree is clean and artifacts match the final phase commit.
      - `validator-report.md` must identify the safe port by implement phase ID and `phase/impl_p06_fix_gpg_pubkey_keyring`, not by embedding the literal hash of the final report commit.
      
      ## Phase-Specific Details
      
      - Reproduce each validator failure with a minimal regression before fixing it.
        Prefer C API probes for `gcry_pk_*`, `gcry_sexp_*`, and `gcry_mpi_*`
        problems; use GPG command regressions when the failure depends on GPG keyring
        or packet state.
      - If the inventory has zero unresolved failures for this bucket, do not invent a
        regression or safe-side change. Rerun every focused phase 6 validator subset
        in port mode with override evidence, update `validator-report.md` with the
        clean bucket result and artifact roots, and make the required
        `impl_p06_fix_gpg_pubkey_keyring:` phase commit. If the report was already
        exact and no tracked file changed, create the phase commit with
        `git commit --allow-empty` before tagging it.
      - For signing and verification failures, inspect:
        - RSA/DSA/ECDSA signature S-expressions.
        - Hash algorithm selection and digest info encoding.
        - Random nonce/k generation behavior.
        - Libgcrypt error codes consumed by GPG.
      - For encryption/decryption failures, inspect:
        - RSA/ElGamal/ECC encryption S-expressions.
        - PKCS#1, OAEP, ECDH, and curve parameter handling where relevant.
        - MPI opaque handling in `safe/src/mpi/opaque.rs` and
          `safe/src/pubkey/encoding.rs`.
      - For keyring/listing/fingerprint failures, inspect:
        - `gcry_pk_get_keygrip` in `safe/src/pubkey/keygrip.rs`.
        - `gcry_pk_get_nbits`, curve lookup, and public parameter ordering.
        - S-expression canonical and advanced formatting in `safe/src/sexp.rs`.
      - Focused validator coverage for this phase must include sign, verify,
        encrypt, decrypt-only, key, import/export, recipient, hidden-recipient,
        trust/ownertrust/trustdb, fingerprint, revocation/revuid, list-secret,
        list-keys, quick UID/expiry, hash-algo, password, agent flag, and batch-list
        case families. The verifier commands must not rely on `usage-gpg-*key*`
        alone to cover those families.
      - If a validator defect is the only reason a focused case cannot pass, document
        the exact defect in `validator-report.md` and add or update only the matching
        entry in `safe/scripts/validator-libgcrypt-skips.json`. The skip must name the
        current validator commit and testcase ID, and the focused subset run must
        still execute all unaffected cases in this bucket.
      - Preserve allocation ownership across C ABI boundaries. All returned buffers
        must be freeable with `gcry_free` when libgcrypt expects that contract.
      - Document fixed testcase IDs, regression IDs, and modules touched in
        `validator-report.md`.
      - Commit fixes and report updates. End with one commit whose subject begins
        `impl_p06_fix_gpg_pubkey_keyring:` and tag it
        `phase/impl_p06_fix_gpg_pubkey_keyring`.
      
      # Verification Phases
      
      - Phase ID: `check_p06_pubkey_keyring_software_tester`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p06_fix_gpg_pubkey_keyring`
      - Purpose: verify safe fixes against GPG public-key, keyring, sign/verify, and
        recipient encryption/decryption validator usage cases.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p06_fix_gpg_pubkey_keyring)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p05_fix_gpg_symmetric_digest_random)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `safe/scripts/check-rust-toolchain.sh`
        - `cargo fmt --manifest-path safe/Cargo.toml -- --check`
        - `cargo build --manifest-path safe/Cargo.toml --release --locked --offline`
        - `safe/scripts/build-release-lib.sh`
        - `safe/scripts/check-abi.sh --all`
        - `safe/scripts/run-upstream-tests.sh t-sexp t-convert mpitests t-mpi-bit t-mpi-point pubkey keygen keygrip t-rsa-15 t-rsa-pss t-dsa t-ecdsa t-ed25519 t-cv25519`
        - `safe/scripts/run-regression-tests.sh --all`
        - `safe/scripts/build-debs.sh`
        - `safe/scripts/check-deb-metadata.sh --dist safe/dist`
        - `python3 safe/scripts/prepare-validator-local-port.py --validator-dir validator --dist safe/dist --output-root validator-local`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-sign --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-*sign*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-verify --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-*verify*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-encrypt --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-*encrypt*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-decrypt --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-*decrypt*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-keys --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-*key*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-import --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-import-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-export --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-export-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-recipient --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-recipient-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-hidden-recipient --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-hidden-recipient-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-trust --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-*trust*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-fingerprint --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-*fingerprint*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-revoke --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-*revoke*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-revuid --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-quick-revuid*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-list-secret --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-list-secret-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-list-keys --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-list-keys*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-quick-uid --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-quick-add-uid*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-quick-expire --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-quick-set-expire*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-hash-algo --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-hash-algo-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-passwd --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-passwd-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-use-agent --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-use-agent-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-batch-list --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case usage-gpg-no-tty-batch-list --record-casts`
        - `python3 safe/scripts/check-validator-port-evidence.py --port-lock validator-local/proof/local-port-debs-lock.json --override-root validator-local/override-debs --artifact-root validator-artifacts/p06-port-sign --artifact-root validator-artifacts/p06-port-verify --artifact-root validator-artifacts/p06-port-encrypt --artifact-root validator-artifacts/p06-port-decrypt --artifact-root validator-artifacts/p06-port-keys --artifact-root validator-artifacts/p06-port-import --artifact-root validator-artifacts/p06-port-export --artifact-root validator-artifacts/p06-port-recipient --artifact-root validator-artifacts/p06-port-hidden-recipient --artifact-root validator-artifacts/p06-port-trust --artifact-root validator-artifacts/p06-port-fingerprint --artifact-root validator-artifacts/p06-port-revoke --artifact-root validator-artifacts/p06-port-revuid --artifact-root validator-artifacts/p06-port-list-secret --artifact-root validator-artifacts/p06-port-list-keys --artifact-root validator-artifacts/p06-port-quick-uid --artifact-root validator-artifacts/p06-port-quick-expire --artifact-root validator-artifacts/p06-port-hash-algo --artifact-root validator-artifacts/p06-port-passwd --artifact-root validator-artifacts/p06-port-use-agent --artifact-root validator-artifacts/p06-port-batch-list`
      
      - Phase ID: `check_p06_pubkey_keyring_senior_tester`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p06_fix_gpg_pubkey_keyring`
      - Purpose: senior review of public-key compatibility, S-expression/MPI memory
        ownership, regression coverage, and no validator-suite edits.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p06_fix_gpg_pubkey_keyring)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p05_fix_gpg_symmetric_digest_random)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `git diff --stat HEAD^..HEAD`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `bash -c 'if git diff --name-only HEAD^..HEAD | grep -E "^(validator|validator-local|validator-artifacts)(/|$)"; then exit 1; fi'`
        - `bash -c 'rg -n "todo!|unimplemented!|Not implemented|safe_gcry_stub_zero" safe/src safe/cabi safe/build.rs && exit 1 || true'`
        - Review new tests under `safe/tests/regressions/` and the updated
          `validator-report.md`.
      
      # Success Criteria
      
      - Focused public-key/keyring, trust, fingerprint, revocation, recipient, and
        decrypt-only validator subsets pass in safe mode with verified safe override
        package installation evidence for every port-mode testcase.
      - Upstream public-key, S-expression, and MPI tests still pass.
      - No validator files were modified.
      
      # Git Commit Requirement
      
      The implementer must commit work to git before yielding. The commit subject must start with `impl_p06_fix_gpg_pubkey_keyring:`, and the implementer must force-update local tag `phase/impl_p06_fix_gpg_pubkey_keyring` to that phase commit before yielding.
  - id: check_p06_pubkey_keyring_software_tester
    type: check
    bounce_target: impl_p06_fix_gpg_pubkey_keyring
    prompt: |
      Role: Software tester reviewer for check phase `check_p06_pubkey_keyring_software_tester`.
      
      Review the current workspace for implement phase `impl_p06_fix_gpg_pubkey_keyring`. Do not modify files. The fixed `bounce_target` for this check is `impl_p06_fix_gpg_pubkey_keyring`.
      
      Workflow Topology
      - Phase order: 15
      - Phase ID: `check_p06_pubkey_keyring_software_tester`
      - Phase type: `check`
      - Fixed top-level `bounce_target`: `impl_p06_fix_gpg_pubkey_keyring`
      - This verifier is an explicit top-level `check` phase from `.plan/workflow-structure.yaml` and must remain in structure order immediately after the implement block it verifies.
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      
      Preexisting Inputs
      Use the existing artifacts listed here and in the phase file content below as review evidence. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `phase/impl_p05_fix_gpg_symmetric_digest_random`
      - `phase/impl_p06_fix_gpg_pubkey_keyring`
      - `validator/`
      - `validator-report.md`
      - `safe/scripts/run-validator-libgcrypt.sh`
      - `safe/scripts/check-validator-port-evidence.py`
      - `safe/dist/safe-debs.manifest.json`
      - `validator-local/proof/local-port-debs-lock.json`
      - `validator-local/override-debs/libgcrypt/`
      - `validator-artifacts/p06-port-sign/`
      - `validator-artifacts/p06-port-verify/`
      - `validator-artifacts/p06-port-encrypt/`
      - `validator-artifacts/p06-port-decrypt/`
      - `validator-artifacts/p06-port-keys/`
      - `validator-artifacts/p06-port-import/`
      - `validator-artifacts/p06-port-export/`
      - `validator-artifacts/p06-port-recipient/`
      - `validator-artifacts/p06-port-hidden-recipient/`
      - `validator-artifacts/p06-port-trust/`
      - `validator-artifacts/p06-port-fingerprint/`
      - `validator-artifacts/p06-port-revoke/`
      - `validator-artifacts/p06-port-revuid/`
      - `validator-artifacts/p06-port-list-secret/`
      - `validator-artifacts/p06-port-list-keys/`
      - `validator-artifacts/p06-port-quick-uid/`
      - `validator-artifacts/p06-port-quick-expire/`
      - `validator-artifacts/p06-port-hash-algo/`
      - `validator-artifacts/p06-port-passwd/`
      - `validator-artifacts/p06-port-use-agent/`
      - `validator-artifacts/p06-port-batch-list/`
      
      Specific Things To Verify
      - Verify the implementation for `impl_p06_fix_gpg_pubkey_keyring` satisfies the phase file `.plan/phases/06-fix-gpg-pubkey-keyring.md` for `Fix GPG public-key, signing, encryption, and keyring usage failures`.
      - Verify the implementation consumed or updated existing artifacts instead of refetching, recollecting, rediscovering, or regenerating them from scratch.
      - Verify the implementation committed the phase work and updated the required phase tag when the phase file requires it.
      - Run every command listed in the verification block below unless a command is impossible in the current environment; if a command cannot be run, treat that as a failure unless the phase file explicitly permits a documented exception.
      - Verify the success criteria below and report any missing artifact, unexpected file modification, unsafe scope expansion, or validator evidence gap.
      
      Verification Block From `.plan/phases/06-fix-gpg-pubkey-keyring.md`
      
      - Phase ID: `check_p06_pubkey_keyring_software_tester`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p06_fix_gpg_pubkey_keyring`
      - Purpose: verify safe fixes against GPG public-key, keyring, sign/verify, and
        recipient encryption/decryption validator usage cases.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p06_fix_gpg_pubkey_keyring)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p05_fix_gpg_symmetric_digest_random)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `safe/scripts/check-rust-toolchain.sh`
        - `cargo fmt --manifest-path safe/Cargo.toml -- --check`
        - `cargo build --manifest-path safe/Cargo.toml --release --locked --offline`
        - `safe/scripts/build-release-lib.sh`
        - `safe/scripts/check-abi.sh --all`
        - `safe/scripts/run-upstream-tests.sh t-sexp t-convert mpitests t-mpi-bit t-mpi-point pubkey keygen keygrip t-rsa-15 t-rsa-pss t-dsa t-ecdsa t-ed25519 t-cv25519`
        - `safe/scripts/run-regression-tests.sh --all`
        - `safe/scripts/build-debs.sh`
        - `safe/scripts/check-deb-metadata.sh --dist safe/dist`
        - `python3 safe/scripts/prepare-validator-local-port.py --validator-dir validator --dist safe/dist --output-root validator-local`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-sign --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-*sign*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-verify --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-*verify*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-encrypt --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-*encrypt*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-decrypt --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-*decrypt*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-keys --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-*key*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-import --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-import-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-export --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-export-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-recipient --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-recipient-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-hidden-recipient --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-hidden-recipient-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-trust --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-*trust*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-fingerprint --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-*fingerprint*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-revoke --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-*revoke*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-revuid --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-quick-revuid*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-list-secret --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-list-secret-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-list-keys --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-list-keys*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-quick-uid --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-quick-add-uid*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-quick-expire --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-quick-set-expire*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-hash-algo --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-hash-algo-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-passwd --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-passwd-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-use-agent --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case-glob 'usage-gpg-use-agent-*' --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p06-port-batch-list --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --case usage-gpg-no-tty-batch-list --record-casts`
        - `python3 safe/scripts/check-validator-port-evidence.py --port-lock validator-local/proof/local-port-debs-lock.json --override-root validator-local/override-debs --artifact-root validator-artifacts/p06-port-sign --artifact-root validator-artifacts/p06-port-verify --artifact-root validator-artifacts/p06-port-encrypt --artifact-root validator-artifacts/p06-port-decrypt --artifact-root validator-artifacts/p06-port-keys --artifact-root validator-artifacts/p06-port-import --artifact-root validator-artifacts/p06-port-export --artifact-root validator-artifacts/p06-port-recipient --artifact-root validator-artifacts/p06-port-hidden-recipient --artifact-root validator-artifacts/p06-port-trust --artifact-root validator-artifacts/p06-port-fingerprint --artifact-root validator-artifacts/p06-port-revoke --artifact-root validator-artifacts/p06-port-revuid --artifact-root validator-artifacts/p06-port-list-secret --artifact-root validator-artifacts/p06-port-list-keys --artifact-root validator-artifacts/p06-port-quick-uid --artifact-root validator-artifacts/p06-port-quick-expire --artifact-root validator-artifacts/p06-port-hash-algo --artifact-root validator-artifacts/p06-port-passwd --artifact-root validator-artifacts/p06-port-use-agent --artifact-root validator-artifacts/p06-port-batch-list`
      
      Success Criteria From `.plan/phases/06-fix-gpg-pubkey-keyring.md`
      
      - Focused public-key/keyring, trust, fingerprint, revocation, recipient, and
        decrypt-only validator subsets pass in safe mode with verified safe override
        package installation evidence for every port-mode testcase.
      - Upstream public-key, S-expression, and MPI tests still pass.
      - No validator files were modified.
      
      Verdict Instruction
      End your response with exactly one of these lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check_p06_pubkey_keyring_senior_tester
    type: check
    bounce_target: impl_p06_fix_gpg_pubkey_keyring
    prompt: |
      Role: Senior reviewer for check phase `check_p06_pubkey_keyring_senior_tester`.
      
      Review the current workspace for implement phase `impl_p06_fix_gpg_pubkey_keyring`. Do not modify files. The fixed `bounce_target` for this check is `impl_p06_fix_gpg_pubkey_keyring`.
      
      Workflow Topology
      - Phase order: 16
      - Phase ID: `check_p06_pubkey_keyring_senior_tester`
      - Phase type: `check`
      - Fixed top-level `bounce_target`: `impl_p06_fix_gpg_pubkey_keyring`
      - This verifier is an explicit top-level `check` phase from `.plan/workflow-structure.yaml` and must remain in structure order immediately after the implement block it verifies.
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      
      Preexisting Inputs
      Use the existing artifacts listed here and in the phase file content below as review evidence. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `phase/impl_p05_fix_gpg_symmetric_digest_random`
      - `phase/impl_p06_fix_gpg_pubkey_keyring`
      - `validator-report.md`
      - `safe/src/pubkey/`
      - `safe/src/sexp.rs`
      - `safe/src/mpi/`
      - `safe/tests/regressions/manifest.json`
      - `safe/tests/regressions/`
      
      Specific Things To Verify
      - Verify the implementation for `impl_p06_fix_gpg_pubkey_keyring` satisfies the phase file `.plan/phases/06-fix-gpg-pubkey-keyring.md` for `Fix GPG public-key, signing, encryption, and keyring usage failures`.
      - Verify the implementation consumed or updated existing artifacts instead of refetching, recollecting, rediscovering, or regenerating them from scratch.
      - Verify the implementation committed the phase work and updated the required phase tag when the phase file requires it.
      - Run every command listed in the verification block below unless a command is impossible in the current environment; if a command cannot be run, treat that as a failure unless the phase file explicitly permits a documented exception.
      - Verify the success criteria below and report any missing artifact, unexpected file modification, unsafe scope expansion, or validator evidence gap.
      
      Verification Block From `.plan/phases/06-fix-gpg-pubkey-keyring.md`
      
      - Phase ID: `check_p06_pubkey_keyring_senior_tester`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p06_fix_gpg_pubkey_keyring`
      - Purpose: senior review of public-key compatibility, S-expression/MPI memory
        ownership, regression coverage, and no validator-suite edits.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p06_fix_gpg_pubkey_keyring)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p05_fix_gpg_symmetric_digest_random)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `git diff --stat HEAD^..HEAD`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `bash -c 'if git diff --name-only HEAD^..HEAD | grep -E "^(validator|validator-local|validator-artifacts)(/|$)"; then exit 1; fi'`
        - `bash -c 'rg -n "todo!|unimplemented!|Not implemented|safe_gcry_stub_zero" safe/src safe/cabi safe/build.rs && exit 1 || true'`
        - Review new tests under `safe/tests/regressions/` and the updated
          `validator-report.md`.
      
      Success Criteria From `.plan/phases/06-fix-gpg-pubkey-keyring.md`
      
      - Focused public-key/keyring, trust, fingerprint, revocation, recipient, and
        decrypt-only validator subsets pass in safe mode with verified safe override
        package installation evidence for every port-mode testcase.
      - Upstream public-key, S-expression, and MPI tests still pass.
      - No validator files were modified.
      
      Verdict Instruction
      End your response with exactly one of these lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl_p07_fix_remaining_validator_failures
    type: implement
    bounce_target: null
    prompt: |
      Role: Software engineer implementing phase `impl_p07_fix_remaining_validator_failures`.
      
      This prompt is self-contained; the agent sees only this prompt. Work from repository root `/home/yans/safelibs/pipeline/ports/port-libgcrypt`. Preserve unrelated user work and the existing dirty worktree state.
      
      Workflow Topology
      - Phase order: 17
      - Phase ID: `impl_p07_fix_remaining_validator_failures`
      - Phase type: `implement`
      - Fixed top-level `bounce_target`: null
      - Verifier phases, in structure order immediately after this implement phase: `check_p07_remaining_failures_software_tester`, then `check_p07_remaining_failures_senior_tester`
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      - Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or agent-guided `bounce_targets` lists.
      - Use only fixed `bounce_target` values from `.plan/workflow-structure.yaml`.
      
      Preexisting Inputs
      Consume or update the existing artifacts listed here and in the phase file content below. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `phase/impl_p06_fix_gpg_pubkey_keyring`
      - `validator/`
      - `validator-report.md`
      - `safe/scripts/run-validator-libgcrypt.sh`
      - `safe/scripts/check-validator-port-evidence.py`
      - `safe/scripts/validator-libgcrypt-skips.json`
      - `safe/src/`
      - `safe/abi/`
      - `safe/cabi/`
      - `safe/debian/`
      - `safe/scripts/`
      - `safe/tests/regressions/manifest.json`
      - `safe/tests/regressions/`
      
      New Outputs
      Produce or update the outputs named here and in the phase file content below. Keep outputs phase-scoped, avoid unrelated edits, and commit all phase work to git before yielding.
      
      - Fixes for any remaining validator failures not covered by phases 3-6.
      - Regressions for remaining safe-side failures.
      - Updated `validator-report.md` with no unresolved safe-side failures.
      
      Git Requirement
      Commit work to git before yielding. Stage only files that are in scope for this phase. Do not commit unrelated dirty files or the nested `validator/` checkout.
      
      Phase File Content From `.plan/phases/07-fix-remaining-validator-failures.md`
      
      # Phase Name
      
      Catch-all remaining validator failures and safety review
      
      # Implement Phase ID
      
      `impl_p07_fix_remaining_validator_failures`
      
      # Preexisting Inputs
      
      Consume these artifacts in place. If any listed generated artifact already exists, use it as phase input evidence instead of rediscovering or reimporting it.
      
      - Original source snapshot and safe implementation artifacts:
        `original/libgcrypt20-1.10.3/`, `safe/Cargo.toml`, `safe/Cargo.lock`,
        `safe/build.rs`, `safe/src/`, `safe/abi/`, `safe/cabi/`, and
        `safe/vendor/`.
      - Generated/package artifacts from prior phases: `safe/dist/*.deb`,
        `safe/dist/safe-debs.manifest.json`, and top-level package build outputs if
        present.
      - Existing test harnesses and scripts: `safe/scripts/*.sh`,
        `safe/tests/upstream/`, `safe/tests/compat/`, `safe/tests/dependents/`,
        and `safe/tests/regressions/`.
      - Existing metadata and documentation inputs: `dependents.json`,
        `relevant_cves.json`, `all_cves.json`, and `safe/docs/*.md`.
      - Dependent-image package locks and snapshot inputs:
        `safe/tests/dependents/metadata/install-packages.noble.lock`,
        `safe/tests/dependents/metadata/safe-debs.noble.lock`,
        `safe/tests/dependents/metadata/base-image.noble.digest`, and
        `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`.
      - Phase 6 commit and report.
      - Validator wrapper and port evidence checker from phase 1.
      - `safe/scripts/validator-libgcrypt-skips.json`.
      - All full-run safe candidate failures still listed in `validator-report.md`.
      - All existing safe source, package, scripts, and tests.
      
      # New Outputs
      
      - Fixes for any remaining validator failures not covered by phases 3-6.
      - Regressions for remaining safe-side failures.
      - Updated `validator-report.md` with no unresolved safe-side failures.
      
      # File Changes
      
      - Any safe-side file required by remaining failures:
        - `safe/src/**`
        - `safe/abi/**`
        - `safe/cabi/**`
        - `safe/debian/**`
        - `safe/scripts/**`
        - `safe/tests/regressions/**`
      - `safe/scripts/validator-libgcrypt-skips.json` for exact
        validator-commit-specific testcase skips of clearly identified validator
        defects.
      - `safe/tests/regressions/manifest.json`
      - `validator-report.md`
      
      # Implementation Details
      
      ## Workflow-Wide Requirements For This Phase
      
      - Consume the listed preexisting inputs in place. Existing source snapshots, metadata, tests, packages, validator checkouts, and prior artifacts are inputs, not rediscovery or regeneration tasks unless this phase explicitly rebuilds generated package/validator artifacts.
      - Keep `validator/`, `validator-local/`, and `validator-artifacts/` external to the port repository. Do not commit nested validator contents or generated validator artifacts.
      - Preserve the Rust toolchain contract: root `rust-toolchain.toml` and `safe/rust-toolchain.toml` must stay identical and pinned to Rust `1.85.1` with `profile = "minimal"` and `components = ["rustfmt"]`.
      - Run `safe/scripts/check-rust-toolchain.sh` before every Cargo, release-library, Debian package, or dependent safe-image build.
      - Build any package artifacts used as validator inputs from a clean phase commit after `phase/impl_p07_fix_remaining_validator_failures` points at `HEAD`. If tracked files or `validator-report.md` change after validation, amend the same phase commit, force-update the phase tag, rebuild packages, regenerate `validator-local`, and rerun the phase validator commands until the worktree is clean and artifacts match the final phase commit.
      - `validator-report.md` must identify the safe port by implement phase ID and `phase/impl_p07_fix_remaining_validator_failures`, not by embedding the literal hash of the final report commit.
      
      ## Phase-Specific Details
      
      - Rerun the full safe validator suite and inspect every remaining failure.
        Reject the run before triage if the port artifact root lacks complete
        override-install evidence for the local safe packages.
      - If the failure is a safe compatibility problem, add a minimal regression,
        fix the underlying safe module, and rerun the relevant subset.
      - If the failure is package installation or development metadata:
        - Inspect `safe/debian/control`, `safe/debian/rules`,
          `safe/debian/libgcrypt20.install`, `safe/debian/libgcrypt20-dev.install`,
          `safe/abi/libgcrypt.pc.in`, `safe/abi/libgcrypt-config.in`, and
          `safe/build.rs`.
        - Keep package names and versions compatible with the existing safe package
          contract.
        - Do not change validator override install scripts.
      - If the failure is a validator bug:
        - Document the exact evidence in `validator-report.md`.
        - If it is a testcase execution bug, skip only that testcase through the
          port-owned `safe/scripts/validator-libgcrypt-skips.json` entry consumed by
          the wrapper.
        - Require testcase skip entries to name the current validator commit and fail
          closed on every other commit.
        - If it is a proof or inventory bug rather than a testcase execution bug,
          document the skipped proof or inventory check in `validator-report.md` and
          keep all testcase execution unchanged.
        - Keep the validator checkout unmodified.
      - Verify the report no longer lists unresolved safe-side failures before
        committing.
      - Commit fixes and report updates. End with one commit whose subject begins
        `impl_p07_fix_remaining_validator_failures:` and tag it
        `phase/impl_p07_fix_remaining_validator_failures`.
      
      # Verification Phases
      
      - Phase ID: `check_p07_remaining_failures_software_tester`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p07_fix_remaining_validator_failures`
      - Purpose: rerun the full safe validator suite and verify that all remaining
        non-skipped failures have safe-side regressions and fixes.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p07_fix_remaining_validator_failures)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p06_fix_gpg_pubkey_keyring)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `safe/scripts/check-rust-toolchain.sh`
        - `safe/scripts/check-dependent-metadata.sh`
        - `cargo fmt --manifest-path safe/Cargo.toml -- --check`
        - `cargo build --manifest-path safe/Cargo.toml --release --locked --offline`
        - `safe/scripts/build-release-lib.sh`
        - `safe/scripts/check-no-upstream-bridge.sh`
        - `safe/scripts/check-abi.sh --all`
        - `safe/scripts/run-upstream-tests.sh --all`
        - `safe/scripts/run-regression-tests.sh --all`
        - `safe/scripts/run-compat-smoke.sh --all`
        - `safe/scripts/build-debs.sh`
        - `safe/scripts/check-deb-metadata.sh --dist safe/dist`
        - `python3 safe/scripts/prepare-validator-local-port.py --validator-dir validator --dist safe/dist --output-root validator-local`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p07-port-full --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --record-casts`
        - `python3 safe/scripts/check-validator-port-evidence.py --port-lock validator-local/proof/local-port-debs-lock.json --override-root validator-local/override-debs --artifact-root validator-artifacts/p07-port-full`
      
      - Phase ID: `check_p07_remaining_failures_senior_tester`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p07_fix_remaining_validator_failures`
      - Purpose: senior review for safety, packaging, validator bug skips, artifact
        contracts, and no validator-suite modifications.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p07_fix_remaining_validator_failures)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p06_fix_gpg_pubkey_keyring)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `git diff --stat HEAD^..HEAD`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `bash -c 'if git diff --name-only HEAD^..HEAD | grep -E "^(validator|validator-local|validator-artifacts)(/|$)"; then exit 1; fi'`
        - `safe/scripts/check-no-upstream-bridge.sh`
        - `bash -c 'rg -n "safe_gcry_stub_zero|todo!|unimplemented!|Not implemented" safe/src safe/cabi safe/build.rs safe/scripts safe/tests/regressions safe/debian test-original.sh && exit 1 || true'`
        - `bash -c 'rg -n "skip|validator bug|unsupported libgcrypt" validator-report.md || true'`
        - Review `validator-report.md` to ensure every skip names a validator
          defect, affected check, and justification.
      
      # Success Criteria
      
      - Full safe validator run passes except for explicitly documented validator
        bug skips, and every non-skipped port result proves installation of the local
        safe override packages.
      - Existing safe build, ABI, upstream, compatibility, and regression checks pass.
      - No validator files were modified.
      
      # Git Commit Requirement
      
      The implementer must commit work to git before yielding. The commit subject must start with `impl_p07_fix_remaining_validator_failures:`, and the implementer must force-update local tag `phase/impl_p07_fix_remaining_validator_failures` to that phase commit before yielding.
  - id: check_p07_remaining_failures_software_tester
    type: check
    bounce_target: impl_p07_fix_remaining_validator_failures
    prompt: |
      Role: Software tester reviewer for check phase `check_p07_remaining_failures_software_tester`.
      
      Review the current workspace for implement phase `impl_p07_fix_remaining_validator_failures`. Do not modify files. The fixed `bounce_target` for this check is `impl_p07_fix_remaining_validator_failures`.
      
      Workflow Topology
      - Phase order: 18
      - Phase ID: `check_p07_remaining_failures_software_tester`
      - Phase type: `check`
      - Fixed top-level `bounce_target`: `impl_p07_fix_remaining_validator_failures`
      - This verifier is an explicit top-level `check` phase from `.plan/workflow-structure.yaml` and must remain in structure order immediately after the implement block it verifies.
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      
      Preexisting Inputs
      Use the existing artifacts listed here and in the phase file content below as review evidence. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `phase/impl_p06_fix_gpg_pubkey_keyring`
      - `phase/impl_p07_fix_remaining_validator_failures`
      - `validator/`
      - `validator-report.md`
      - `safe/scripts/run-validator-libgcrypt.sh`
      - `safe/scripts/check-validator-port-evidence.py`
      - `safe/dist/safe-debs.manifest.json`
      - `validator-local/proof/local-port-debs-lock.json`
      - `validator-local/override-debs/libgcrypt/`
      - `validator-artifacts/p07-port-full/`
      
      Specific Things To Verify
      - Verify the implementation for `impl_p07_fix_remaining_validator_failures` satisfies the phase file `.plan/phases/07-fix-remaining-validator-failures.md` for `Catch-all remaining validator failures and safety review`.
      - Verify the implementation consumed or updated existing artifacts instead of refetching, recollecting, rediscovering, or regenerating them from scratch.
      - Verify the implementation committed the phase work and updated the required phase tag when the phase file requires it.
      - Run every command listed in the verification block below unless a command is impossible in the current environment; if a command cannot be run, treat that as a failure unless the phase file explicitly permits a documented exception.
      - Verify the success criteria below and report any missing artifact, unexpected file modification, unsafe scope expansion, or validator evidence gap.
      
      Verification Block From `.plan/phases/07-fix-remaining-validator-failures.md`
      
      - Phase ID: `check_p07_remaining_failures_software_tester`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p07_fix_remaining_validator_failures`
      - Purpose: rerun the full safe validator suite and verify that all remaining
        non-skipped failures have safe-side regressions and fixes.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p07_fix_remaining_validator_failures)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p06_fix_gpg_pubkey_keyring)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `safe/scripts/check-rust-toolchain.sh`
        - `safe/scripts/check-dependent-metadata.sh`
        - `cargo fmt --manifest-path safe/Cargo.toml -- --check`
        - `cargo build --manifest-path safe/Cargo.toml --release --locked --offline`
        - `safe/scripts/build-release-lib.sh`
        - `safe/scripts/check-no-upstream-bridge.sh`
        - `safe/scripts/check-abi.sh --all`
        - `safe/scripts/run-upstream-tests.sh --all`
        - `safe/scripts/run-regression-tests.sh --all`
        - `safe/scripts/run-compat-smoke.sh --all`
        - `safe/scripts/build-debs.sh`
        - `safe/scripts/check-deb-metadata.sh --dist safe/dist`
        - `python3 safe/scripts/prepare-validator-local-port.py --validator-dir validator --dist safe/dist --output-root validator-local`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p07-port-full --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --record-casts`
        - `python3 safe/scripts/check-validator-port-evidence.py --port-lock validator-local/proof/local-port-debs-lock.json --override-root validator-local/override-debs --artifact-root validator-artifacts/p07-port-full`
      
      Success Criteria From `.plan/phases/07-fix-remaining-validator-failures.md`
      
      - Full safe validator run passes except for explicitly documented validator
        bug skips, and every non-skipped port result proves installation of the local
        safe override packages.
      - Existing safe build, ABI, upstream, compatibility, and regression checks pass.
      - No validator files were modified.
      
      Verdict Instruction
      End your response with exactly one of these lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check_p07_remaining_failures_senior_tester
    type: check
    bounce_target: impl_p07_fix_remaining_validator_failures
    prompt: |
      Role: Senior reviewer for check phase `check_p07_remaining_failures_senior_tester`.
      
      Review the current workspace for implement phase `impl_p07_fix_remaining_validator_failures`. Do not modify files. The fixed `bounce_target` for this check is `impl_p07_fix_remaining_validator_failures`.
      
      Workflow Topology
      - Phase order: 19
      - Phase ID: `check_p07_remaining_failures_senior_tester`
      - Phase type: `check`
      - Fixed top-level `bounce_target`: `impl_p07_fix_remaining_validator_failures`
      - This verifier is an explicit top-level `check` phase from `.plan/workflow-structure.yaml` and must remain in structure order immediately after the implement block it verifies.
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      
      Preexisting Inputs
      Use the existing artifacts listed here and in the phase file content below as review evidence. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `phase/impl_p06_fix_gpg_pubkey_keyring`
      - `phase/impl_p07_fix_remaining_validator_failures`
      - `validator-report.md`
      - `safe/src/`
      - `safe/abi/`
      - `safe/cabi/`
      - `safe/debian/`
      - `safe/scripts/`
      - `safe/tests/regressions/manifest.json`
      - `safe/tests/regressions/`
      
      Specific Things To Verify
      - Verify the implementation for `impl_p07_fix_remaining_validator_failures` satisfies the phase file `.plan/phases/07-fix-remaining-validator-failures.md` for `Catch-all remaining validator failures and safety review`.
      - Verify the implementation consumed or updated existing artifacts instead of refetching, recollecting, rediscovering, or regenerating them from scratch.
      - Verify the implementation committed the phase work and updated the required phase tag when the phase file requires it.
      - Run every command listed in the verification block below unless a command is impossible in the current environment; if a command cannot be run, treat that as a failure unless the phase file explicitly permits a documented exception.
      - Verify the success criteria below and report any missing artifact, unexpected file modification, unsafe scope expansion, or validator evidence gap.
      
      Verification Block From `.plan/phases/07-fix-remaining-validator-failures.md`
      
      - Phase ID: `check_p07_remaining_failures_senior_tester`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p07_fix_remaining_validator_failures`
      - Purpose: senior review for safety, packaging, validator bug skips, artifact
        contracts, and no validator-suite modifications.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p07_fix_remaining_validator_failures)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p06_fix_gpg_pubkey_keyring)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `git diff --stat HEAD^..HEAD`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `bash -c 'if git diff --name-only HEAD^..HEAD | grep -E "^(validator|validator-local|validator-artifacts)(/|$)"; then exit 1; fi'`
        - `safe/scripts/check-no-upstream-bridge.sh`
        - `bash -c 'rg -n "safe_gcry_stub_zero|todo!|unimplemented!|Not implemented" safe/src safe/cabi safe/build.rs safe/scripts safe/tests/regressions safe/debian test-original.sh && exit 1 || true'`
        - `bash -c 'rg -n "skip|validator bug|unsupported libgcrypt" validator-report.md || true'`
        - Review `validator-report.md` to ensure every skip names a validator
          defect, affected check, and justification.
      
      Success Criteria From `.plan/phases/07-fix-remaining-validator-failures.md`
      
      - Full safe validator run passes except for explicitly documented validator
        bug skips, and every non-skipped port result proves installation of the local
        safe override packages.
      - Existing safe build, ABI, upstream, compatibility, and regression checks pass.
      - No validator files were modified.
      
      Verdict Instruction
      End your response with exactly one of these lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl_p08_final_validator_report
    type: implement
    bounce_target: null
    prompt: |
      Role: Software engineer implementing phase `impl_p08_final_validator_report`.
      
      This prompt is self-contained; the agent sees only this prompt. Work from repository root `/home/yans/safelibs/pipeline/ports/port-libgcrypt`. Preserve unrelated user work and the existing dirty worktree state.
      
      Workflow Topology
      - Phase order: 20
      - Phase ID: `impl_p08_final_validator_report`
      - Phase type: `implement`
      - Fixed top-level `bounce_target`: null
      - Verifier phases, in structure order immediately after this implement phase: `check_p08_final_validator_clean_run`, then `check_p08_final_senior_review`
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      - Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or agent-guided `bounce_targets` lists.
      - Use only fixed `bounce_target` values from `.plan/workflow-structure.yaml`.
      
      Preexisting Inputs
      Consume or update the existing artifacts listed here and in the phase file content below. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `phase/impl_p07_fix_remaining_validator_failures`
      - `validator/`
      - `validator-report.md`
      - `safe/scripts/run-validator-libgcrypt.sh`
      - `safe/scripts/check-validator-port-evidence.py`
      - `safe/scripts/check-validator-package-dev-probe.sh`
      - `safe/scripts/validator-libgcrypt-skips.json`
      - `safe/dist/`
      - `safe/tests/regressions/manifest.json`
      
      New Outputs
      Produce or update the outputs named here and in the phase file content below. Keep outputs phase-scoped, avoid unrelated edits, and commit all phase work to git before yielding.
      
      - Final `validator-report.md` with complete clean-run summary.
      - Package/development probe artifacts under
        `validator-artifacts/p08-package-dev-probe/`.
      - Final original, port, optional proof, and optional site artifacts under
        `validator-artifacts/p08-final/`.
      - Any final report-only corrections.
      
      Git Requirement
      Commit work to git before yielding. Stage only files that are in scope for this phase. Do not commit unrelated dirty files or the nested `validator/` checkout.
      
      Phase File Content From `.plan/phases/08-final-validator-report.md`
      
      # Phase Name
      
      Final validator report and clean run
      
      # Implement Phase ID
      
      `impl_p08_final_validator_report`
      
      # Preexisting Inputs
      
      Consume these artifacts in place. If any listed generated artifact already exists, use it as phase input evidence instead of rediscovering or reimporting it.
      
      - Original source snapshot and safe implementation artifacts:
        `original/libgcrypt20-1.10.3/`, `safe/Cargo.toml`, `safe/Cargo.lock`,
        `safe/build.rs`, `safe/src/`, `safe/abi/`, `safe/cabi/`, and
        `safe/vendor/`.
      - Generated/package artifacts from prior phases: `safe/dist/*.deb`,
        `safe/dist/safe-debs.manifest.json`, and top-level package build outputs if
        present.
      - Existing test harnesses and scripts: `safe/scripts/*.sh`,
        `safe/tests/upstream/`, `safe/tests/compat/`, `safe/tests/dependents/`,
        and `safe/tests/regressions/`.
      - Existing metadata and documentation inputs: `dependents.json`,
        `relevant_cves.json`, `all_cves.json`, and `safe/docs/*.md`.
      - Dependent-image package locks and snapshot inputs:
        `safe/tests/dependents/metadata/install-packages.noble.lock`,
        `safe/tests/dependents/metadata/safe-debs.noble.lock`,
        `safe/tests/dependents/metadata/base-image.noble.digest`, and
        `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`.
      - Phase 7 commit and report.
      - `validator/` checkout at the commit used for final validation.
      - Validator wrapper and port evidence checker from phase 1.
      - Package/development probe from phase 3.
      - `safe/scripts/validator-libgcrypt-skips.json`.
      - Current safe packages and all regression tests.
      
      # New Outputs
      
      - Final `validator-report.md` with complete clean-run summary.
      - Package/development probe artifacts under
        `validator-artifacts/p08-package-dev-probe/`.
      - Final original, port, optional proof, and optional site artifacts under
        `validator-artifacts/p08-final/`.
      - Any final report-only corrections.
      
      # File Changes
      
      - `validator-report.md`
      
      # Implementation Details
      
      ## Workflow-Wide Requirements For This Phase
      
      - Consume the listed preexisting inputs in place. Existing source snapshots, metadata, tests, packages, validator checkouts, and prior artifacts are inputs, not rediscovery or regeneration tasks unless this phase explicitly rebuilds generated package/validator artifacts.
      - Keep `validator/`, `validator-local/`, and `validator-artifacts/` external to the port repository. Do not commit nested validator contents or generated validator artifacts.
      - Preserve the Rust toolchain contract: root `rust-toolchain.toml` and `safe/rust-toolchain.toml` must stay identical and pinned to Rust `1.85.1` with `profile = "minimal"` and `components = ["rustfmt"]`.
      - Run `safe/scripts/check-rust-toolchain.sh` before every Cargo, release-library, Debian package, or dependent safe-image build.
      - Build any package artifacts used as validator inputs from a clean phase commit after `phase/impl_p08_final_validator_report` points at `HEAD`. If tracked files or `validator-report.md` change after validation, amend the same phase commit, force-update the phase tag, rebuild packages, regenerate `validator-local`, and rerun the phase validator commands until the worktree is clean and artifacts match the final phase commit.
      - `validator-report.md` must identify the safe port by implement phase ID and `phase/impl_p08_final_validator_report`, not by embedding the literal hash of the final report commit.
      
      ## Phase-Specific Details
      
      - Rebuild safe packages from the current commit, regenerate local validator
        override metadata, and rerun the clean-container package/development probe
        before the final validator suite. The final report must include the
        `validator-artifacts/p08-package-dev-probe` result so the clean run proves the
        installed development package surface, not just testcase override evidence.
      - Run the complete original baseline and complete safe candidate validator
        suite. At the inspected validator commit, this means 5 source cases and 170
        usage cases. Later phases must consume the fixed counts recorded by phase 2
        for the actual validator checkout. The safe candidate run is not valid unless
        `safe/scripts/check-validator-port-evidence.py` confirms every non-skipped
        port testcase installed the local safe packages.
      - Detect official libgcrypt matrix support with
        `python3 validator/tools/run_matrix.py --config validator/repositories.yml --tests-root validator/tests --list-libraries --library libgcrypt`.
        If that command succeeds and prints exactly `libgcrypt`, the official proof
        path is inventory-available. The verifier must still check
        `safe/scripts/validator-libgcrypt-skips.json`; if any active skip entry
        matches the final original or port full-suite run, official proof/site is not
        proof-eligible for that artifact root because skipped testcase result JSONs
        are rejected by the current validator proof loader.
        If inventory support is available and there are no active testcase skips, the
        verifier must run the proof/site commands below.
        If it fails, the verifier must capture the selection failure output under
        `validator-artifacts/p08-final/official-libgcrypt-list.err` and confirm
        `validator-report.md` names that exact validator inventory/proof defect plus
        the fallback artifact summary.
      - If the official validator proof path supports libgcrypt by this point and
        there are no active testcase skips for the final artifact root, run:
        - Set `LIBGCRYPT_SOURCE_CASES`, `LIBGCRYPT_USAGE_CASES`, and
          `LIBGCRYPT_TOTAL_CASES` from the phase 2 counts already recorded in
          `validator-report.md`; abort if any count is missing or zero.
        - `python3 validator/tools/verify_proof_artifacts.py --config validator/repositories.yml --tests-root validator/tests --artifact-root validator-artifacts/p08-final --proof-output validator-artifacts/p08-final/proof/original-validation-proof.json --mode original --library libgcrypt --require-casts --min-source-cases "$LIBGCRYPT_SOURCE_CASES" --min-usage-cases "$LIBGCRYPT_USAGE_CASES" --min-cases "$LIBGCRYPT_TOTAL_CASES"`
        - `python3 validator/tools/verify_proof_artifacts.py --config validator/repositories.yml --tests-root validator/tests --artifact-root validator-artifacts/p08-final --proof-output validator-artifacts/p08-final/proof/port-validation-proof.json --mode port --library libgcrypt --require-casts --min-source-cases "$LIBGCRYPT_SOURCE_CASES" --min-usage-cases "$LIBGCRYPT_USAGE_CASES" --min-cases "$LIBGCRYPT_TOTAL_CASES" --ports-root /home/yans/safelibs/pipeline/ports`
        - `python3 validator/tools/render_site.py --config validator/repositories.yml --tests-root validator/tests --artifact-root validator-artifacts/p08-final --proof-path validator-artifacts/p08-final/proof/original-validation-proof.json --proof-path validator-artifacts/p08-final/proof/port-validation-proof.json --output-root validator-artifacts/p08-final/site`
        - `bash validator/scripts/verify-site.sh --config validator/repositories.yml --tests-root validator/tests --artifacts-root validator-artifacts/p08-final --proof-path validator-artifacts/p08-final/proof/original-validation-proof.json --proof-path validator-artifacts/p08-final/proof/port-validation-proof.json --site-root validator-artifacts/p08-final/site --library libgcrypt`
      - If the official proof path still cannot load libgcrypt due to validator
        inventory omissions, document the skipped proof check and include the
        port-owned wrapper's complete per-case summary as the final validation
        artifact. This skip must name the validator files and error messages that
        make the official path unavailable, include the phrase
        `Official validator inventory/proof defect`, and include a
        `Fallback artifact summary` for `validator-artifacts/p08-final`.
      - If active testcase skips exist in the final artifact root, document the
        skipped proof/site check as `Official validator proof/status limitation`, list
        each skipped `<mode>:<testcase_id>` and the validator commit, state that the
        current validator proof loader accepts only `passed` and `failed` statuses,
        and include the port-owned wrapper's complete non-skipped per-case summary as
        the `Fallback artifact summary` for `validator-artifacts/p08-final`.
      - The final report must include:
        - Validator URL and commit.
        - Safe port phase tag (`phase/impl_p08_final_validator_report`) and a note
          that verifiers resolve the exact commit with `git rev-parse`; do not embed
          the literal hash of the final report commit in the tracked report.
        - Exact commands executed.
        - Original baseline result.
        - Safe candidate result.
        - Port override installation evidence, including package names, versions,
          architectures, filenames, SHA256s, and artifact roots checked.
        - Package/development probe result and artifact root.
        - Official proof/site status. If official proof/site ran, include proof and
          site artifact paths. If it did not run because libgcrypt was not selectable
          through the official inventory, include the exact official libgcrypt
          selection error and fallback artifact summary. If it did not run because
          active testcase skips produced skipped result JSONs, include
          `Official validator proof/status limitation`, the skipped
          `<mode>:<testcase_id>` values, and the fallback artifact summary.
        - Testcase counts.
        - Failures found by phase.
        - Regression tests added.
        - Fixes applied with file/module summaries.
        - Validator bugs and skipped checks, if any.
        - Final status.
      - Commit final report updates. End with one commit whose subject begins
        `impl_p08_final_validator_report:` and tag it
        `phase/impl_p08_final_validator_report`.
      
      # Verification Phases
      
      - Phase ID: `check_p08_final_validator_clean_run`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p08_final_validator_report`
      - Purpose: final software-tester gate proving a clean original baseline,
        clean safe validator run, current local safe packages, and complete report.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p08_final_validator_report)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p07_fix_remaining_validator_failures)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `git -C validator rev-parse HEAD`
        - `safe/scripts/check-rust-toolchain.sh`
        - `safe/scripts/check-dependent-metadata.sh`
        - `cargo fmt --manifest-path safe/Cargo.toml -- --check`
        - `cargo build --manifest-path safe/Cargo.toml --release --locked --offline`
        - `safe/scripts/build-release-lib.sh`
        - `safe/scripts/check-no-upstream-bridge.sh`
        - `safe/scripts/check-abi.sh --all`
        - `safe/scripts/run-upstream-tests.sh --all`
        - `safe/scripts/run-regression-tests.sh --all`
        - `safe/scripts/run-compat-smoke.sh --all`
        - `safe/scripts/build-debs.sh`
        - `safe/scripts/check-deb-metadata.sh --dist safe/dist`
        - `python3 safe/scripts/prepare-validator-local-port.py --validator-dir validator --dist safe/dist --output-root validator-local`
        - `bash safe/scripts/check-validator-package-dev-probe.sh --dist safe/dist --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --artifact-root validator-artifacts/p08-package-dev-probe`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode original --artifact-root validator-artifacts/p08-final --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p08-final --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --record-casts`
        - `python3 safe/scripts/check-validator-port-evidence.py --port-lock validator-local/proof/local-port-debs-lock.json --override-root validator-local/override-debs --artifact-root validator-artifacts/p08-final`
        - `mkdir -p validator-artifacts/p08-final/proof validator-artifacts/p08-final/site`
        - `python3 -c 'import json, subprocess; from pathlib import Path; out=Path("validator-artifacts/p08-final"); (out/"active-testcase-skips").unlink(missing_ok=True); (out/"no-active-testcase-skips").unlink(missing_ok=True); vc=subprocess.check_output(["git","-C","validator","rev-parse","HEAD"], text=True).strip(); data=json.load(open("safe/scripts/validator-libgcrypt-skips.json")); skips=data.get("skips", []); assert data.get("schema_version")==1; assert data.get("validator_commit")==vc; assert all(entry.get("validator_commit")==vc for entry in skips); assert all(entry.get("mode") in {"original","port"} for entry in skips); active=[str(entry.get("mode"))+":"+str(entry.get("testcase_id")) for entry in skips]; (out/"active-testcase-skips").write_text("\n".join(active)+"\n") if active else (out/"no-active-testcase-skips").write_text("none\n")'`
        - `bash -c 'set -euo pipefail; rm -f validator-artifacts/p08-final/official-libgcrypt-supported validator-artifacts/p08-final/official-libgcrypt-unsupported; if python3 validator/tools/run_matrix.py --config validator/repositories.yml --tests-root validator/tests --list-libraries --library libgcrypt >validator-artifacts/p08-final/official-libgcrypt-list.txt 2>validator-artifacts/p08-final/official-libgcrypt-list.err && grep -qx libgcrypt validator-artifacts/p08-final/official-libgcrypt-list.txt; then touch validator-artifacts/p08-final/official-libgcrypt-supported; else touch validator-artifacts/p08-final/official-libgcrypt-unsupported; fi'`
        - `bash -c 'set -euo pipefail; test ! -e validator-artifacts/p08-final/official-libgcrypt-supported || test -e validator-artifacts/p08-final/active-testcase-skips || { SOURCE=$(awk -F": *" "/^libgcrypt_source_cases:/ {print \$2}" validator-report.md | tail -1); USAGE=$(awk -F": *" "/^libgcrypt_usage_cases:/ {print \$2}" validator-report.md | tail -1); TOTAL=$(awk -F": *" "/^libgcrypt_total_cases:/ {print \$2}" validator-report.md | tail -1); for value in "$SOURCE" "$USAGE" "$TOTAL"; do case "$value" in ""|*[!0-9]*) exit 1;; esac; test "$value" -gt 0; done; python3 validator/tools/verify_proof_artifacts.py --config validator/repositories.yml --tests-root validator/tests --artifact-root validator-artifacts/p08-final --proof-output validator-artifacts/p08-final/proof/original-validation-proof.json --mode original --library libgcrypt --require-casts --min-source-cases "$SOURCE" --min-usage-cases "$USAGE" --min-cases "$TOTAL"; python3 validator/tools/verify_proof_artifacts.py --config validator/repositories.yml --tests-root validator/tests --artifact-root validator-artifacts/p08-final --proof-output validator-artifacts/p08-final/proof/port-validation-proof.json --mode port --library libgcrypt --require-casts --min-source-cases "$SOURCE" --min-usage-cases "$USAGE" --min-cases "$TOTAL" --ports-root /home/yans/safelibs/pipeline/ports; python3 validator/tools/render_site.py --config validator/repositories.yml --tests-root validator/tests --artifact-root validator-artifacts/p08-final --proof-path validator-artifacts/p08-final/proof/original-validation-proof.json --proof-path validator-artifacts/p08-final/proof/port-validation-proof.json --output-root validator-artifacts/p08-final/site; bash validator/scripts/verify-site.sh --config validator/repositories.yml --tests-root validator/tests --artifacts-root validator-artifacts/p08-final --proof-path validator-artifacts/p08-final/proof/original-validation-proof.json --proof-path validator-artifacts/p08-final/proof/port-validation-proof.json --site-root validator-artifacts/p08-final/site --library libgcrypt; }'`
        - `bash -c "set -euo pipefail; test ! -e validator-artifacts/p08-final/official-libgcrypt-unsupported || python3 -c 'from pathlib import Path; report=Path(\"validator-report.md\").read_text(); err=Path(\"validator-artifacts/p08-final/official-libgcrypt-list.err\").read_text().strip(); out=Path(\"validator-artifacts/p08-final/official-libgcrypt-list.txt\").read_text().strip(); detail=(err or out).strip(); required=[\"Official validator inventory/proof defect\",\"Fallback artifact summary\",\"validator-artifacts/p08-final\"]; errors=[\"missing report fields: \"+\", \".join(item for item in required if item not in report)] if any(item not in report for item in required) else []; errors += [\"report does not include official libgcrypt selection failure output\"] if detail and detail not in report else []; raise SystemExit(\"\\n\".join(errors) if errors else 0)'"`
        - `bash -c "set -euo pipefail; test ! -e validator-artifacts/p08-final/active-testcase-skips || python3 -c 'from pathlib import Path; report=Path(\"validator-report.md\").read_text(); skips=Path(\"validator-artifacts/p08-final/active-testcase-skips\").read_text().splitlines(); required=[\"Official validator proof/status limitation\",\"Fallback artifact summary\",\"validator-artifacts/p08-final\"]; errors=[]; missing=[item for item in required if item not in report]; errors += [\"missing report fields: \"+\", \".join(missing)] if missing else []; errors += [\"report missing active testcase skip \"+item for item in skips if item and item not in report]; raise SystemExit(\"\\n\".join(errors) if errors else 0)'"`
        - `python3 -c 'import subprocess, sys; from pathlib import Path; text=Path("validator-report.md").read_text(); required=["Validator commit","Safe port phase tag","Commands executed","Testcase counts","Port override installation evidence","Package/development probe","Official proof/site status","Failures found","Regression tests","Fixes applied","Final status"]; phase="phase/impl_p08_final_validator_report"; head=subprocess.check_output(["git","rev-parse","HEAD"], text=True).strip(); errors=[]; missing=[item for item in required if item not in text]; errors += ["missing report fields: "+", ".join(missing)] if missing else []; errors += ["missing final phase tag in report"] if phase not in text else []; errors += ["validator-report.md must not embed the final safe commit hash; use the phase tag"] if head in text else []; sys.exit("\\n".join(errors) if errors else 0)'`
      
      - Phase ID: `check_p08_final_senior_review`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p08_final_validator_report`
      - Purpose: final senior review of report accuracy, workflow contract,
        safe-side-only fixes, safety posture, and artifact flow.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p08_final_validator_report)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p07_fix_remaining_validator_failures)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `git diff --stat HEAD^..HEAD`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `bash -c 'if git diff --name-only HEAD^..HEAD | grep -E "^(validator|validator-local|validator-artifacts)(/|$)"; then exit 1; fi'`
        - `safe/scripts/check-no-upstream-bridge.sh`
        - `bash -c 'rg -n "safe_gcry_stub_zero|todo!|unimplemented!|Not implemented" safe/src safe/cabi safe/build.rs safe/scripts safe/tests/regressions safe/debian test-original.sh && exit 1 || true'`
        - Review `validator-report.md`, `safe/tests/regressions/manifest.json`,
          helper scripts, and the final validator artifact summaries.
      
      # Success Criteria
      
      - Full safe validator suite passes, except for explicitly justified
        validator-side skips, with verified override-install evidence for every
        non-skipped port testcase.
      - The final clean-container package/development probe passes against the same
        local safe `.deb` files used by the final port validator run.
      - The final verifier either runs `verify_proof_artifacts.py`, `render_site.py`,
        and `scripts/verify-site.sh` for official libgcrypt support with the phase 2
        fixed case counts and no active testcase skips, or confirms the report records
        the exact official libgcrypt inventory/proof defect or proof/status limitation
        and fallback artifact summary.
      - `validator-report.md` is complete and matches final artifact summaries.
      - Worktree is clean and validator checkout/artifacts remain external ignored
        artifacts.
      
      # Git Commit Requirement
      
      The implementer must commit work to git before yielding. The commit subject must start with `impl_p08_final_validator_report:`, and the implementer must force-update local tag `phase/impl_p08_final_validator_report` to that phase commit before yielding.
  - id: check_p08_final_validator_clean_run
    type: check
    bounce_target: impl_p08_final_validator_report
    prompt: |
      Role: Software tester reviewer for check phase `check_p08_final_validator_clean_run`.
      
      Review the current workspace for implement phase `impl_p08_final_validator_report`. Do not modify files. The fixed `bounce_target` for this check is `impl_p08_final_validator_report`.
      
      Workflow Topology
      - Phase order: 21
      - Phase ID: `check_p08_final_validator_clean_run`
      - Phase type: `check`
      - Fixed top-level `bounce_target`: `impl_p08_final_validator_report`
      - This verifier is an explicit top-level `check` phase from `.plan/workflow-structure.yaml` and must remain in structure order immediately after the implement block it verifies.
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      
      Preexisting Inputs
      Use the existing artifacts listed here and in the phase file content below as review evidence. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `phase/impl_p07_fix_remaining_validator_failures`
      - `phase/impl_p08_final_validator_report`
      - `validator/`
      - `validator-report.md`
      - `safe/scripts/run-validator-libgcrypt.sh`
      - `safe/scripts/check-validator-port-evidence.py`
      - `safe/scripts/check-validator-package-dev-probe.sh`
      - `safe/scripts/validator-libgcrypt-skips.json`
      - `safe/dist/safe-debs.manifest.json`
      - `validator-local/proof/local-port-debs-lock.json`
      - `validator-local/override-debs/libgcrypt/`
      - `validator-artifacts/p08-package-dev-probe/`
      - `validator-artifacts/p08-final/`
      
      Specific Things To Verify
      - Verify the implementation for `impl_p08_final_validator_report` satisfies the phase file `.plan/phases/08-final-validator-report.md` for `Final validator report and clean run`.
      - Verify the implementation consumed or updated existing artifacts instead of refetching, recollecting, rediscovering, or regenerating them from scratch.
      - Verify the implementation committed the phase work and updated the required phase tag when the phase file requires it.
      - Run every command listed in the verification block below unless a command is impossible in the current environment; if a command cannot be run, treat that as a failure unless the phase file explicitly permits a documented exception.
      - Verify the success criteria below and report any missing artifact, unexpected file modification, unsafe scope expansion, or validator evidence gap.
      
      Verification Block From `.plan/phases/08-final-validator-report.md`
      
      - Phase ID: `check_p08_final_validator_clean_run`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p08_final_validator_report`
      - Purpose: final software-tester gate proving a clean original baseline,
        clean safe validator run, current local safe packages, and complete report.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p08_final_validator_report)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p07_fix_remaining_validator_failures)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `git -C validator rev-parse HEAD`
        - `safe/scripts/check-rust-toolchain.sh`
        - `safe/scripts/check-dependent-metadata.sh`
        - `cargo fmt --manifest-path safe/Cargo.toml -- --check`
        - `cargo build --manifest-path safe/Cargo.toml --release --locked --offline`
        - `safe/scripts/build-release-lib.sh`
        - `safe/scripts/check-no-upstream-bridge.sh`
        - `safe/scripts/check-abi.sh --all`
        - `safe/scripts/run-upstream-tests.sh --all`
        - `safe/scripts/run-regression-tests.sh --all`
        - `safe/scripts/run-compat-smoke.sh --all`
        - `safe/scripts/build-debs.sh`
        - `safe/scripts/check-deb-metadata.sh --dist safe/dist`
        - `python3 safe/scripts/prepare-validator-local-port.py --validator-dir validator --dist safe/dist --output-root validator-local`
        - `bash safe/scripts/check-validator-package-dev-probe.sh --dist safe/dist --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --artifact-root validator-artifacts/p08-package-dev-probe`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode original --artifact-root validator-artifacts/p08-final --record-casts`
        - `bash safe/scripts/run-validator-libgcrypt.sh --validator-dir validator --mode port --artifact-root validator-artifacts/p08-final --override-root validator-local/override-debs --port-lock validator-local/proof/local-port-debs-lock.json --record-casts`
        - `python3 safe/scripts/check-validator-port-evidence.py --port-lock validator-local/proof/local-port-debs-lock.json --override-root validator-local/override-debs --artifact-root validator-artifacts/p08-final`
        - `mkdir -p validator-artifacts/p08-final/proof validator-artifacts/p08-final/site`
        - `python3 -c 'import json, subprocess; from pathlib import Path; out=Path("validator-artifacts/p08-final"); (out/"active-testcase-skips").unlink(missing_ok=True); (out/"no-active-testcase-skips").unlink(missing_ok=True); vc=subprocess.check_output(["git","-C","validator","rev-parse","HEAD"], text=True).strip(); data=json.load(open("safe/scripts/validator-libgcrypt-skips.json")); skips=data.get("skips", []); assert data.get("schema_version")==1; assert data.get("validator_commit")==vc; assert all(entry.get("validator_commit")==vc for entry in skips); assert all(entry.get("mode") in {"original","port"} for entry in skips); active=[str(entry.get("mode"))+":"+str(entry.get("testcase_id")) for entry in skips]; (out/"active-testcase-skips").write_text("\n".join(active)+"\n") if active else (out/"no-active-testcase-skips").write_text("none\n")'`
        - `bash -c 'set -euo pipefail; rm -f validator-artifacts/p08-final/official-libgcrypt-supported validator-artifacts/p08-final/official-libgcrypt-unsupported; if python3 validator/tools/run_matrix.py --config validator/repositories.yml --tests-root validator/tests --list-libraries --library libgcrypt >validator-artifacts/p08-final/official-libgcrypt-list.txt 2>validator-artifacts/p08-final/official-libgcrypt-list.err && grep -qx libgcrypt validator-artifacts/p08-final/official-libgcrypt-list.txt; then touch validator-artifacts/p08-final/official-libgcrypt-supported; else touch validator-artifacts/p08-final/official-libgcrypt-unsupported; fi'`
        - `bash -c 'set -euo pipefail; test ! -e validator-artifacts/p08-final/official-libgcrypt-supported || test -e validator-artifacts/p08-final/active-testcase-skips || { SOURCE=$(awk -F": *" "/^libgcrypt_source_cases:/ {print \$2}" validator-report.md | tail -1); USAGE=$(awk -F": *" "/^libgcrypt_usage_cases:/ {print \$2}" validator-report.md | tail -1); TOTAL=$(awk -F": *" "/^libgcrypt_total_cases:/ {print \$2}" validator-report.md | tail -1); for value in "$SOURCE" "$USAGE" "$TOTAL"; do case "$value" in ""|*[!0-9]*) exit 1;; esac; test "$value" -gt 0; done; python3 validator/tools/verify_proof_artifacts.py --config validator/repositories.yml --tests-root validator/tests --artifact-root validator-artifacts/p08-final --proof-output validator-artifacts/p08-final/proof/original-validation-proof.json --mode original --library libgcrypt --require-casts --min-source-cases "$SOURCE" --min-usage-cases "$USAGE" --min-cases "$TOTAL"; python3 validator/tools/verify_proof_artifacts.py --config validator/repositories.yml --tests-root validator/tests --artifact-root validator-artifacts/p08-final --proof-output validator-artifacts/p08-final/proof/port-validation-proof.json --mode port --library libgcrypt --require-casts --min-source-cases "$SOURCE" --min-usage-cases "$USAGE" --min-cases "$TOTAL" --ports-root /home/yans/safelibs/pipeline/ports; python3 validator/tools/render_site.py --config validator/repositories.yml --tests-root validator/tests --artifact-root validator-artifacts/p08-final --proof-path validator-artifacts/p08-final/proof/original-validation-proof.json --proof-path validator-artifacts/p08-final/proof/port-validation-proof.json --output-root validator-artifacts/p08-final/site; bash validator/scripts/verify-site.sh --config validator/repositories.yml --tests-root validator/tests --artifacts-root validator-artifacts/p08-final --proof-path validator-artifacts/p08-final/proof/original-validation-proof.json --proof-path validator-artifacts/p08-final/proof/port-validation-proof.json --site-root validator-artifacts/p08-final/site --library libgcrypt; }'`
        - `bash -c "set -euo pipefail; test ! -e validator-artifacts/p08-final/official-libgcrypt-unsupported || python3 -c 'from pathlib import Path; report=Path(\"validator-report.md\").read_text(); err=Path(\"validator-artifacts/p08-final/official-libgcrypt-list.err\").read_text().strip(); out=Path(\"validator-artifacts/p08-final/official-libgcrypt-list.txt\").read_text().strip(); detail=(err or out).strip(); required=[\"Official validator inventory/proof defect\",\"Fallback artifact summary\",\"validator-artifacts/p08-final\"]; errors=[\"missing report fields: \"+\", \".join(item for item in required if item not in report)] if any(item not in report for item in required) else []; errors += [\"report does not include official libgcrypt selection failure output\"] if detail and detail not in report else []; raise SystemExit(\"\\n\".join(errors) if errors else 0)'"`
        - `bash -c "set -euo pipefail; test ! -e validator-artifacts/p08-final/active-testcase-skips || python3 -c 'from pathlib import Path; report=Path(\"validator-report.md\").read_text(); skips=Path(\"validator-artifacts/p08-final/active-testcase-skips\").read_text().splitlines(); required=[\"Official validator proof/status limitation\",\"Fallback artifact summary\",\"validator-artifacts/p08-final\"]; errors=[]; missing=[item for item in required if item not in report]; errors += [\"missing report fields: \"+\", \".join(missing)] if missing else []; errors += [\"report missing active testcase skip \"+item for item in skips if item and item not in report]; raise SystemExit(\"\\n\".join(errors) if errors else 0)'"`
        - `python3 -c 'import subprocess, sys; from pathlib import Path; text=Path("validator-report.md").read_text(); required=["Validator commit","Safe port phase tag","Commands executed","Testcase counts","Port override installation evidence","Package/development probe","Official proof/site status","Failures found","Regression tests","Fixes applied","Final status"]; phase="phase/impl_p08_final_validator_report"; head=subprocess.check_output(["git","rev-parse","HEAD"], text=True).strip(); errors=[]; missing=[item for item in required if item not in text]; errors += ["missing report fields: "+", ".join(missing)] if missing else []; errors += ["missing final phase tag in report"] if phase not in text else []; errors += ["validator-report.md must not embed the final safe commit hash; use the phase tag"] if head in text else []; sys.exit("\\n".join(errors) if errors else 0)'`
      
      Success Criteria From `.plan/phases/08-final-validator-report.md`
      
      - Full safe validator suite passes, except for explicitly justified
        validator-side skips, with verified override-install evidence for every
        non-skipped port testcase.
      - The final clean-container package/development probe passes against the same
        local safe `.deb` files used by the final port validator run.
      - The final verifier either runs `verify_proof_artifacts.py`, `render_site.py`,
        and `scripts/verify-site.sh` for official libgcrypt support with the phase 2
        fixed case counts and no active testcase skips, or confirms the report records
        the exact official libgcrypt inventory/proof defect or proof/status limitation
        and fallback artifact summary.
      - `validator-report.md` is complete and matches final artifact summaries.
      - Worktree is clean and validator checkout/artifacts remain external ignored
        artifacts.
      
      Verdict Instruction
      End your response with exactly one of these lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check_p08_final_senior_review
    type: check
    bounce_target: impl_p08_final_validator_report
    prompt: |
      Role: Senior reviewer for check phase `check_p08_final_senior_review`.
      
      Review the current workspace for implement phase `impl_p08_final_validator_report`. Do not modify files. The fixed `bounce_target` for this check is `impl_p08_final_validator_report`.
      
      Workflow Topology
      - Phase order: 22
      - Phase ID: `check_p08_final_senior_review`
      - Phase type: `check`
      - Fixed top-level `bounce_target`: `impl_p08_final_validator_report`
      - This verifier is an explicit top-level `check` phase from `.plan/workflow-structure.yaml` and must remain in structure order immediately after the implement block it verifies.
      - Do not invent, rename, reorder, skip, merge, or redirect workflow phases.
      
      Preexisting Inputs
      Use the existing artifacts listed here and in the phase file content below as review evidence. Do not refetch, recollect, rediscover, or regenerate existing artifacts from scratch when they already exist.
      
      - `dependents.json`
      - `relevant_cves.json`
      - `all_cves.json`
      - `safe/docs/*.md`
      - `safe/docs/abi-map.md`
      - `safe/docs/bridge-inventory.md`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/test-matrix.md`
      - `safe/dist/`
      - `safe/dist/*.deb`
      - `safe/dist/safe-debs.manifest.json`
      - `safe/tests/upstream/`
      - `safe/tests/compat/`
      - `safe/tests/dependents/`
      - `safe/tests/dependents/metadata/install-packages.noble.lock`
      - `safe/tests/dependents/metadata/safe-debs.noble.lock`
      - `safe/tests/dependents/metadata/base-image.noble.digest`
      - `safe/tests/dependents/metadata/ubuntu-snapshot.noble.sources`
      - `phase/impl_p07_fix_remaining_validator_failures`
      - `phase/impl_p08_final_validator_report`
      - `validator-report.md`
      - `safe/scripts/run-validator-libgcrypt.sh`
      - `safe/scripts/check-validator-port-evidence.py`
      - `safe/scripts/check-validator-package-dev-probe.sh`
      - `safe/tests/regressions/manifest.json`
      - `safe/tests/regressions/`
      - `validator-artifacts/p08-final/`
      
      Specific Things To Verify
      - Verify the implementation for `impl_p08_final_validator_report` satisfies the phase file `.plan/phases/08-final-validator-report.md` for `Final validator report and clean run`.
      - Verify the implementation consumed or updated existing artifacts instead of refetching, recollecting, rediscovering, or regenerating them from scratch.
      - Verify the implementation committed the phase work and updated the required phase tag when the phase file requires it.
      - Run every command listed in the verification block below unless a command is impossible in the current environment; if a command cannot be run, treat that as a failure unless the phase file explicitly permits a documented exception.
      - Verify the success criteria below and report any missing artifact, unexpected file modification, unsafe scope expansion, or validator evidence gap.
      
      Verification Block From `.plan/phases/08-final-validator-report.md`
      
      - Phase ID: `check_p08_final_senior_review`
      - Type: `check`
      - Fixed `bounce_target`: `impl_p08_final_validator_report`
      - Purpose: final senior review of report accuracy, workflow contract,
        safe-side-only fixes, safety posture, and artifact flow.
      - Commands:
        - `test "$(git rev-parse HEAD)" = "$(git rev-parse phase/impl_p08_final_validator_report)"`
        - `test "$(git rev-parse HEAD^)" = "$(git rev-parse phase/impl_p07_fix_remaining_validator_failures)"`
        - `bash -c 'test -z "$(git status --short)"'`
        - `bash -c 'git check-ignore -q validator/ && git check-ignore -q validator-local/ && git check-ignore -q validator-artifacts/'`
        - `git diff --stat HEAD^..HEAD`
        - `bash -c 'test -z "$(git ls-files -- validator validator-local validator-artifacts)"'`
        - `bash -c 'if git diff --name-only HEAD^..HEAD | grep -E "^(validator|validator-local|validator-artifacts)(/|$)"; then exit 1; fi'`
        - `safe/scripts/check-no-upstream-bridge.sh`
        - `bash -c 'rg -n "safe_gcry_stub_zero|todo!|unimplemented!|Not implemented" safe/src safe/cabi safe/build.rs safe/scripts safe/tests/regressions safe/debian test-original.sh && exit 1 || true'`
        - Review `validator-report.md`, `safe/tests/regressions/manifest.json`,
          helper scripts, and the final validator artifact summaries.
      
      Success Criteria From `.plan/phases/08-final-validator-report.md`
      
      - Full safe validator suite passes, except for explicitly justified
        validator-side skips, with verified override-install evidence for every
        non-skipped port testcase.
      - The final clean-container package/development probe passes against the same
        local safe `.deb` files used by the final port validator run.
      - The final verifier either runs `verify_proof_artifacts.py`, `render_site.py`,
        and `scripts/verify-site.sh` for official libgcrypt support with the phase 2
        fixed case counts and no active testcase skips, or confirms the report records
        the exact official libgcrypt inventory/proof defect or proof/status limitation
        and fallback artifact summary.
      - `validator-report.md` is complete and matches final artifact summaries.
      - Worktree is clean and validator checkout/artifacts remain external ignored
        artifacts.
      
      Verdict Instruction
      End your response with exactly one of these lines:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
