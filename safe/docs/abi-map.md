# ABI Map

Seeded from `safe/abi/libgcrypt.vers`, `safe/abi/gcrypt.h.in`, and `safe/abi/visibility.h`. This phase exports all 217 `GCRYPT_1.6` symbols immediately and fixes the bootstrap artifact contract: `safe/build.rs` is the single renderer for generated public headers, metadata, version-script exports, bootstrap manifests, and temporary C stubs; Cargo has no external Rust dependencies; and both root and `safe/` Cargo invocations are fail-closed against `safe/vendor/`.

`safe/src/ffi.rs` was dead code and has been removed. Live ownership is now documented directly against the split Rust modules below.

- `gcry_md_get` is a Linux version-script export that is not declared by installed `gcrypt.h`; it is classified as `visibility-only`, implemented in phase 4, and verified by the phase-8 ABI-only smoke probe in `safe/tests/compat/abi-only-exports.c`.
- `gcry_pk_register` is a Linux version-script export that is not declared by installed `gcrypt.h`; it is classified as `abi-only`, implemented in phase 6 as a `GPG_ERR_NOT_SUPPORTED` compatibility shim, and verified by the phase-8 ABI-only smoke probe in `safe/tests/compat/abi-only-exports.c`.
- `gcry_ctx_release` is now owned by phase 6 and forwards the opaque upstream pubkey/ECC context handles created by the local bridge.
- The only generated public-symbol stubs after this phase are `gcry_log_debugpnt` and `gcry_log_debugsxp`; both call `safe_gcry_stub_zero` from generated C and are marked below as temporary later-phase work.
- Phase 4 now owns the RNG, digest, MAC, and KDF ABI families. The exported Rust entry points in [`safe/src/digest/mod.rs`](../src/digest/mod.rs), [`safe/src/digest/algorithms.rs`](../src/digest/algorithms.rs), [`safe/src/mac.rs`](../src/mac.rs), and [`safe/src/kdf.rs`](../src/kdf.rs) use local handles and vendored RustCrypto implementations rather than the upstream bridge, including the legacy default digest registry entries for `RIPEMD160`, `MD4`, CRC32/CRC24, Whirlpool, and Tiger variants plus their ABI-exposed HMAC wrappers. PBKDF2 now derives through the same local HMAC-capable digest registry, including SHA3, SM3, and Stribog PRFs. MAC `GCRYCTL_GET_KEYLEN`, `GCRY_MAC_GOST28147_IMIT`, all ABI-exposed CMAC, GMAC, and Poly1305 handle opens, and Argon2 handle parameter semantics, including 3-parameter opens and `param[0]` tag-length enforcement, are covered by the public API smoke. The RNG path is fully local through [`safe/src/random.rs`](../src/random.rs), [`safe/src/drbg.rs`](../src/drbg.rs), and [`safe/src/os_rng.rs`](../src/os_rng.rs). The phase-4-only `gcry_md_get` symbol is covered by the phase-8 ABI-only smoke probe; `GCRY_KDF_BALLOON` now has a local handle implementation, and `GCRY_KDF_NONE` remains a header-only review note because it is not an exported symbol.
- Phase 5 now owns the symmetric cipher family through [`safe/src/cipher/mod.rs`](../src/cipher/mod.rs), [`safe/src/cipher/registry.rs`](../src/cipher/registry.rs), [`safe/src/cipher/modes.rs`](../src/cipher/modes.rs), [`safe/src/cipher/aead.rs`](../src/cipher/aead.rs), [`safe/src/cipher/block.rs`](../src/cipher/block.rs), [`safe/src/cipher/stream.rs`](../src/cipher/stream.rs), and [`safe/src/upstream.rs`](../src/upstream.rs), with `GCRYCTL_DISABLE_HWF` bookkeeping owned locally by [`safe/src/global.rs`](../src/global.rs) and [`safe/src/hwfeatures.rs`](../src/hwfeatures.rs). The current verifier exercises this surface through `aeswrap`, `basic --cipher-modes`, `basic-disable-all-hwf`, and `t-lock`; public-key symbols remain phase-6-owned.
- Phase 6 now owns the `gcry_pk_*`, `gcry_pubkey_get_sexp`, `gcry_ecc_*`, `gcry_mpi_point_*`, `gcry_mpi_ec_*`, `gcry_ctx_release`, `gcry_pk_hash_*`, and `gcry_pk_random_override_new` surfaces through [`safe/src/pubkey/mod.rs`](../src/pubkey/mod.rs), [`safe/src/pubkey/ecc.rs`](../src/pubkey/ecc.rs), [`safe/src/pubkey/keygrip.rs`](../src/pubkey/keygrip.rs), [`safe/src/pubkey/encoding.rs`](../src/pubkey/encoding.rs), [`safe/src/mpi/ec.rs`](../src/mpi/ec.rs), [`safe/src/context.rs`](../src/context.rs), and the phase-3 S-expression/MPI compatibility fixes in [`safe/src/sexp.rs`](../src/sexp.rs). The current verifier exercises that surface through `keygen`, `pubkey`, `keygrip`, `pkcs1v2`, `fips186-dsa`, `dsa-rfc6979`, `t-dsa`, `curves`, `t-ecdsa`, `t-ed25519`, `t-cv25519`, `t-x448`, `t-ed448`, `t-rsa-pss`, `t-rsa-15`, `t-rsa-testparm`, `t-mpi-point`, and `testapi`.

## Phase 3 Status Override

| Family | Status | Implementation | Verification | Notes |
| --- | --- | --- | --- | --- |
| `gcry_sexp_*` | Implemented | `safe/src/sexp.rs` + `safe/cabi/exports.c` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh mpitests t-sexp t-convert t-mpi-bit prime` | Rust-backed parser, serializer, builders, traversal helpers, display-hint and canonical-length handling, dump/sprint, and `extract_param` surface. `gcry_sexp_build_array` preserves upstream typed-pointer `void **` argument-array semantics while the C varargs shim passes explicit `uintptr_t` values to Rust dispatch. |
| `gcry_mpi_*` | Implemented for phase 3 ownership | `safe/src/mpi/mod.rs`, `safe/src/mpi/arith.rs`, `safe/src/mpi/scan.rs`, `safe/src/mpi/opaque.rs`, `safe/src/mpi/prime.rs`, `safe/src/mpi/consts.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh mpitests t-sexp t-convert t-mpi-bit prime` | Core numeric, opaque, import/export, print, flag, bit, const, and prime-adjacent MPI APIs are Rust-backed, including upstream-style secure flag ownership for opaque payloads, secure MPI scan/aprint allocation, and HEX print lengths that include the terminating NUL. Phase 6 now layers the `gcry_mpi_point_*` and `gcry_mpi_ec_*` families on top of this ownership via [`safe/src/mpi/ec.rs`](../src/mpi/ec.rs) and [`safe/src/pubkey/encoding.rs`](../src/pubkey/encoding.rs). |
| `gcry_prime_*` | Implemented | `safe/src/mpi/prime.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh mpitests t-sexp t-convert t-mpi-bit prime` | Prime generation, prime checking, group-generator derivation, and factor release are Rust-backed. As in upstream, large-candidate primality is probabilistic: the implementation uses repeated GMP probable-prime checks, so composites below that confidence threshold are rejected by test coverage while a generated/accepted value is not represented as a deterministic proof certificate. |
| `_gcry_mpi_get_const` | Implemented | `safe/src/mpi/consts.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh mpitests t-sexp t-convert t-mpi-bit prime` | Returns singleton immutable MPI constants for the public header macros. |

| Symbol | Classification | Planned Location | Planned Coverage | Notes |
| --- | --- | --- | --- | --- |
| `gcry_check_version` | header+visibility | `safe/src/global.rs` | `run-original-tests.sh` version + `check-abi.sh` export-set check | Implemented runtime-shell path with upstream-style version negotiation. |
| `gcry_control` | header+visibility | `safe/cabi/exports.c` -> `safe/src/global.rs` | `check-abi.sh` variadic/pre-init FIPS smoke + `check-abi.sh --thread-cbs-noop` + export-set check | Implemented runtime-shell control plane, including `GCRYCTL_SET_THREAD_CBS` compatibility and upstream `_P` truthy behavior. |
| `gcry_set_allocation_handler` | header+visibility | `safe/src/alloc.rs` | `check-abi.sh` runtime-shell FIPS handler probe + export-set check | Implemented runtime-shell handler registration with upstream-style initialization and FIPS rejection. |
| `gcry_set_fatalerror_handler` | header+visibility | `safe/src/log.rs` | `check-abi.sh` export-set check | Implemented runtime-shell handler registration and fatal-path dispatch. |
| `gcry_set_gettext_handler` | header+visibility | `safe/src/log.rs` | `check-abi.sh` export-set check | Implemented runtime-shell handler registration and translated fatal/log text dispatch. |
| `gcry_set_log_handler` | header+visibility | `safe/cabi/exports.c` + `safe/src/log.rs` | `check-abi.sh` export-set check | Implemented runtime-shell handler registration with C va_list bridge for log dispatch. |
| `gcry_set_outofcore_handler` | header+visibility | `safe/src/alloc.rs` | `run-original-tests.sh` t-secmem + `check-abi.sh` runtime-shell FIPS handler probe + export-set check | Implemented runtime-shell handler registration, FIPS rejection, and xmalloc retry path. |
| `gcry_set_progress_handler` | header+visibility | `safe/src/log.rs` | `check-abi.sh` export-set check | Implemented runtime-shell registration surface; producers remain phase-local. |
| `gcry_err_code_from_errno` | header+visibility | `safe/src/error.rs` | `check-abi.sh` export-set check | Implemented libgpg-error wrapper surface. |
| `gcry_err_code_to_errno` | header+visibility | `safe/src/error.rs` | `check-abi.sh` export-set check | Implemented libgpg-error wrapper surface. |
| `gcry_err_make_from_errno` | header+visibility | `safe/src/error.rs` | `check-abi.sh` export-set check | Implemented libgpg-error wrapper surface. |
| `gcry_error_from_errno` | header+visibility | `safe/src/error.rs` | `run-original-tests.sh` version + `check-abi.sh` export-set check | Implemented libgpg-error wrapper surface. |
| `gcry_strerror` | header+visibility | `safe/src/error.rs` | `run-original-tests.sh` version + `check-abi.sh` export-set check | Implemented libgpg-error wrapper surface. |
| `gcry_strsource` | header+visibility | `safe/src/error.rs` | `check-abi.sh` export-set check | Implemented libgpg-error wrapper surface. |
| `gcry_free` | header+visibility | `safe/src/alloc.rs` | `run-original-tests.sh` t-secmem + `check-abi.sh` export-set check | Implemented runtime-shell allocation path. |
| `gcry_malloc` | header+visibility | `safe/src/alloc.rs` | `run-original-tests.sh` t-secmem + `check-abi.sh` export-set check | Implemented runtime-shell allocation path. |
| `gcry_malloc_secure` | header+visibility | `safe/src/alloc.rs` + `safe/src/secmem.rs` | `run-original-tests.sh` t-secmem + `check-abi.sh` export-set check | Implemented runtime-shell secure allocation path with locked-page attempts and pool accounting. |
| `gcry_calloc` | header+visibility | `safe/src/alloc.rs` | `run-original-tests.sh` t-secmem + `check-abi.sh` export-set check | Implemented runtime-shell allocation path. |
| `gcry_calloc_secure` | header+visibility | `safe/src/alloc.rs` + `safe/src/secmem.rs` | `run-original-tests.sh` t-secmem + `check-abi.sh` export-set check | Implemented runtime-shell secure allocation path. |
| `gcry_realloc` | header+visibility | `safe/src/alloc.rs` + `safe/src/secmem.rs` | `run-original-tests.sh` t-secmem + `check-abi.sh` export-set check | Implemented runtime-shell realloc path for plain and secure allocations. |
| `gcry_strdup` | header+visibility | `safe/src/alloc.rs` | `run-original-tests.sh` t-secmem + `check-abi.sh` export-set check | Implemented runtime-shell duplication path, preserving secure memory when applicable. |
| `gcry_is_secure` | header+visibility | `safe/src/alloc.rs` + `safe/src/secmem.rs` | `run-original-tests.sh` t-secmem + `check-abi.sh` export-set check | Implemented runtime-shell secure-pointer query. |
| `gcry_xcalloc` | header+visibility | `safe/src/alloc.rs` | `run-original-tests.sh` t-secmem + `check-abi.sh` export-set check | Implemented runtime-shell xalloc path with out-of-core handling. |
| `gcry_xcalloc_secure` | header+visibility | `safe/src/alloc.rs` + `safe/src/secmem.rs` | `run-original-tests.sh` t-secmem + `check-abi.sh` export-set check | Implemented runtime-shell secure xalloc path with overflow support. |
| `gcry_xmalloc` | header+visibility | `safe/src/alloc.rs` | `run-original-tests.sh` t-secmem + `check-abi.sh` export-set check | Implemented runtime-shell xalloc path with out-of-core handling. |
| `gcry_xmalloc_secure` | header+visibility | `safe/src/alloc.rs` + `safe/src/secmem.rs` | `run-original-tests.sh` t-secmem + `check-abi.sh` export-set check | Implemented runtime-shell secure xalloc path with overflow support. |
| `gcry_xrealloc` | header+visibility | `safe/src/alloc.rs` + `safe/src/secmem.rs` | `run-original-tests.sh` t-secmem + `check-abi.sh` export-set check | Implemented runtime-shell xrealloc path with out-of-core handling. |
| `gcry_xstrdup` | header+visibility | `safe/src/alloc.rs` | `run-original-tests.sh` t-secmem + `check-abi.sh` export-set check | Implemented runtime-shell xstrdup path with secure-memory preservation. |
| `gcry_md_algo_info` | header+visibility | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hashtest hmac` + export-set check | Implemented phase-4 digest surface with local `DigestContext` state and vendored RustCrypto algorithms, preserving algorithm names, `map_name` strings, `*_algo_info` controls, HMAC keyed digest handling, copy/reset behavior, and the public handle layout used by `gcry_md_putc`. |
| `gcry_md_algo_name` | header+visibility | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hashtest hmac` + export-set check | Implemented phase-4 digest surface with local `DigestContext` state and vendored RustCrypto algorithms, preserving algorithm names, `map_name` strings, `*_algo_info` controls, HMAC keyed digest handling, copy/reset behavior, and the public handle layout used by `gcry_md_putc`. |
| `gcry_md_close` | header+visibility | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hashtest hmac` + export-set check | Implemented phase-4 digest surface with local `DigestContext` state and vendored RustCrypto algorithms, preserving algorithm names, `map_name` strings, `*_algo_info` controls, HMAC keyed digest handling, copy/reset behavior, and the public handle layout used by `gcry_md_putc`. |
| `gcry_md_copy` | header+visibility | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hashtest hmac` + export-set check | Implemented phase-4 digest surface with local `DigestContext` state and vendored RustCrypto algorithms, preserving algorithm names, `map_name` strings, `*_algo_info` controls, HMAC keyed digest handling, copy/reset behavior, and the public handle layout used by `gcry_md_putc`. |
| `gcry_md_ctl` | header+visibility | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hashtest hmac` + export-set check | Implemented phase-4 digest surface with local `DigestContext` state and vendored RustCrypto algorithms, preserving algorithm names, `map_name` strings, `*_algo_info` controls, HMAC keyed digest handling, copy/reset behavior, and the public handle layout used by `gcry_md_putc`. |
| `gcry_md_enable` | header+visibility | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hashtest hmac` + export-set check | Implemented phase-4 digest surface with local `DigestContext` state and vendored RustCrypto algorithms, preserving algorithm names, `map_name` strings, `*_algo_info` controls, HMAC keyed digest handling, copy/reset behavior, and the public handle layout used by `gcry_md_putc`. |
| `gcry_md_get` | visibility-only | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `safe/scripts/run-compat-smoke.sh --all` ABI-only probe + `check-abi.sh` export-set check | Implemented locally as the phase-4-owned compatibility symbol, with explicit buffer sizing, XOF handling, and FIPS-mode rejection instead of leaving the visibility-only export as a stub. |
| `gcry_md_get_algo` | header+visibility | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hashtest hmac` + export-set check | Implemented phase-4 digest surface with local `DigestContext` state and vendored RustCrypto algorithms, preserving algorithm names, `map_name` strings, `*_algo_info` controls, HMAC keyed digest handling, copy/reset behavior, and the public handle layout used by `gcry_md_putc`. |
| `gcry_md_get_algo_dlen` | header+visibility | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hashtest hmac` + export-set check | Implemented phase-4 digest surface with local `DigestContext` state and vendored RustCrypto algorithms, preserving algorithm names, `map_name` strings, `*_algo_info` controls, HMAC keyed digest handling, copy/reset behavior, and the public handle layout used by `gcry_md_putc`. |
| `gcry_md_hash_buffer` | header+visibility | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hashtest hmac` + export-set check | Implemented phase-4 digest surface with local `DigestContext` state and vendored RustCrypto algorithms, preserving algorithm names, `map_name` strings, `*_algo_info` controls, HMAC keyed digest handling, copy/reset behavior, and the public handle layout used by `gcry_md_putc`. |
| `gcry_md_hash_buffers` | header+visibility | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hashtest hmac` + export-set check | Implemented phase-4 digest surface with local `DigestContext` state and vendored RustCrypto algorithms, preserving algorithm names, `map_name` strings, `*_algo_info` controls, HMAC keyed digest handling, copy/reset behavior, and the public handle layout used by `gcry_md_putc`. |
| `gcry_md_info` | header+visibility | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hashtest hmac` + export-set check | Implemented phase-4 digest surface with local `DigestContext` state and vendored RustCrypto algorithms, preserving algorithm names, `map_name` strings, `*_algo_info` controls, HMAC keyed digest handling, copy/reset behavior, and the public handle layout used by `gcry_md_putc`. |
| `gcry_md_is_enabled` | header+visibility | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hashtest hmac` + export-set check | Implemented phase-4 digest surface with local `DigestContext` state and vendored RustCrypto algorithms, preserving algorithm names, `map_name` strings, `*_algo_info` controls, HMAC keyed digest handling, copy/reset behavior, and the public handle layout used by `gcry_md_putc`. |
| `gcry_md_is_secure` | header+visibility | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hashtest hmac` + export-set check | Implemented phase-4 digest surface with local `DigestContext` state and vendored RustCrypto algorithms, preserving algorithm names, `map_name` strings, `*_algo_info` controls, HMAC keyed digest handling, copy/reset behavior, and the public handle layout used by `gcry_md_putc`. |
| `gcry_md_map_name` | header+visibility | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hashtest hmac` + export-set check | Implemented phase-4 digest surface with local `DigestContext` state and vendored RustCrypto algorithms, preserving algorithm names, `map_name` strings, `*_algo_info` controls, HMAC keyed digest handling, copy/reset behavior, and the public handle layout used by `gcry_md_putc`. |
| `gcry_md_open` | header+visibility | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hashtest hmac` + export-set check | Implemented phase-4 digest surface with local `DigestContext` state and vendored RustCrypto algorithms, preserving algorithm names, `map_name` strings, `*_algo_info` controls, HMAC keyed digest handling, copy/reset behavior, and the public handle layout used by `gcry_md_putc`. |
| `gcry_md_read` | header+visibility | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hashtest hmac` + export-set check | Implemented phase-4 digest surface with local `DigestContext` state and vendored RustCrypto algorithms, preserving algorithm names, `map_name` strings, `*_algo_info` controls, HMAC keyed digest handling, copy/reset behavior, and the public handle layout used by `gcry_md_putc`. |
| `gcry_md_extract` | header+visibility | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hashtest hmac` + export-set check | Implemented phase-4 digest surface with local `DigestContext` state and vendored RustCrypto algorithms, preserving algorithm names, `map_name` strings, `*_algo_info` controls, HMAC keyed digest handling, copy/reset behavior, and the public handle layout used by `gcry_md_putc`. |
| `gcry_md_reset` | header+visibility | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hashtest hmac` + export-set check | Implemented phase-4 digest surface with local `DigestContext` state and vendored RustCrypto algorithms, preserving algorithm names, `map_name` strings, `*_algo_info` controls, HMAC keyed digest handling, copy/reset behavior, and the public handle layout used by `gcry_md_putc`. |
| `gcry_md_setkey` | header+visibility | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hashtest hmac` + export-set check | Implemented phase-4 digest surface with local `DigestContext` state and vendored RustCrypto algorithms, preserving algorithm names, `map_name` strings, `*_algo_info` controls, HMAC keyed digest handling, copy/reset behavior, and the public handle layout used by `gcry_md_putc`. |
| `gcry_md_write` | header+visibility | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hashtest hmac` + export-set check | Implemented phase-4 digest surface with local `DigestContext` state and vendored RustCrypto algorithms, preserving algorithm names, `map_name` strings, `*_algo_info` controls, HMAC keyed digest handling, copy/reset behavior, and the public handle layout used by `gcry_md_putc`. |
| `gcry_md_debug` | header+visibility | `safe/src/digest/mod.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hashtest hmac` + export-set check | Implemented phase-4 digest surface with local `DigestContext` state and vendored RustCrypto algorithms, preserving algorithm names, `map_name` strings, `*_algo_info` controls, HMAC keyed digest handling, copy/reset behavior, and the public handle layout used by `gcry_md_putc`. |
| `gcry_cipher_algo_info` | header+visibility | `safe/src/cipher/registry.rs` + `safe/src/upstream.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh aeswrap basic basic-disable-all-hwf t-lock` + export-set check | Implemented phase-5 symmetric registry surface by forwarding to the installed upstream `libgcrypt.so.20`, preserving upstream algorithm IDs, `*_algo_info` controls, and unsupported-ID behavior for legacy enum slots. |
| `gcry_cipher_algo_name` | header+visibility | `safe/src/cipher/registry.rs` + `safe/src/upstream.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh aeswrap basic basic-disable-all-hwf t-lock` + export-set check | Implemented phase-5 symmetric registry surface by forwarding to the installed upstream `libgcrypt.so.20`, preserving upstream algorithm IDs, `*_algo_info` controls, and unsupported-ID behavior for legacy enum slots. |
| `gcry_cipher_close` | header+visibility | `safe/src/cipher/mod.rs` + `safe/src/cipher/modes.rs` + `safe/src/upstream.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh aeswrap basic basic-disable-all-hwf t-lock` + export-set check | Implemented phase-5 symmetric handle surface by delegating open/close/control/info and mode operations to upstream `libgcrypt.so.20`, preserving upstream handle semantics for reset, sync, CTS, CCM length setup, S-box selection, and AEAD finalization. |
| `gcry_cipher_ctl` | header+visibility | `safe/src/cipher/mod.rs` + `safe/src/cipher/modes.rs` + `safe/src/upstream.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh aeswrap basic basic-disable-all-hwf t-lock` + export-set check | Implemented phase-5 symmetric handle surface by delegating open/close/control/info and mode operations to upstream `libgcrypt.so.20`, preserving upstream handle semantics for reset, sync, CTS, CCM length setup, S-box selection, and AEAD finalization. |
| `gcry_cipher_decrypt` | header+visibility | `safe/src/cipher/mod.rs` + `safe/src/cipher/block.rs` + `safe/src/upstream.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh aeswrap basic basic-disable-all-hwf t-lock` + export-set check | Implemented phase-5 symmetric data path by forwarding encrypt/decrypt/key/IV/CTR operations to upstream `libgcrypt.so.20`, which keeps the original bulk-mode dispatcher, reset behavior, and in-place semantics exercised by `basic.c`, `aeswrap.c`, and `t-lock.c`. |
| `gcry_cipher_encrypt` | header+visibility | `safe/src/cipher/mod.rs` + `safe/src/cipher/block.rs` + `safe/src/upstream.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh aeswrap basic basic-disable-all-hwf t-lock` + export-set check | Implemented phase-5 symmetric data path by forwarding encrypt/decrypt/key/IV/CTR operations to upstream `libgcrypt.so.20`, which keeps the original bulk-mode dispatcher, reset behavior, and in-place semantics exercised by `basic.c`, `aeswrap.c`, and `t-lock.c`. |
| `gcry_cipher_get_algo_blklen` | header+visibility | `safe/src/cipher/registry.rs` + `safe/src/upstream.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh aeswrap basic basic-disable-all-hwf t-lock` + export-set check | Implemented phase-5 symmetric registry surface by forwarding to the installed upstream `libgcrypt.so.20`, preserving upstream algorithm IDs, `*_algo_info` controls, and unsupported-ID behavior for legacy enum slots. |
| `gcry_cipher_get_algo_keylen` | header+visibility | `safe/src/cipher/registry.rs` + `safe/src/upstream.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh aeswrap basic basic-disable-all-hwf t-lock` + export-set check | Implemented phase-5 symmetric registry surface by forwarding to the installed upstream `libgcrypt.so.20`, preserving upstream algorithm IDs, `*_algo_info` controls, and unsupported-ID behavior for legacy enum slots. |
| `gcry_cipher_info` | header+visibility | `safe/src/cipher/mod.rs` + `safe/src/cipher/modes.rs` + `safe/src/upstream.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh aeswrap basic basic-disable-all-hwf t-lock` + export-set check | Implemented phase-5 symmetric handle surface by delegating open/close/control/info and mode operations to upstream `libgcrypt.so.20`, preserving upstream handle semantics for reset, sync, CTS, CCM length setup, S-box selection, and AEAD finalization. |
| `gcry_cipher_map_name` | header+visibility | `safe/src/cipher/registry.rs` + `safe/src/upstream.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh aeswrap basic basic-disable-all-hwf t-lock` + export-set check | Implemented phase-5 symmetric registry surface by forwarding to the installed upstream `libgcrypt.so.20`, preserving upstream algorithm IDs, `*_algo_info` controls, and unsupported-ID behavior for legacy enum slots. |
| `gcry_cipher_mode_from_oid` | header+visibility | `safe/src/cipher/mod.rs` + `safe/src/cipher/modes.rs` + `safe/src/upstream.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh aeswrap basic basic-disable-all-hwf t-lock` + export-set check | Implemented phase-5 symmetric mode lookup by forwarding ASN.1 OID mapping to the installed upstream `libgcrypt.so.20`, keeping mode IDs aligned with libgcrypt 1.10.3 for AESWRAP, CCM, GCM, OCB, XTS, EAX, SIV, and GCM-SIV. |
| `gcry_cipher_open` | header+visibility | `safe/src/cipher/mod.rs` + `safe/src/cipher/modes.rs` + `safe/src/upstream.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh aeswrap basic basic-disable-all-hwf t-lock` + export-set check | Implemented phase-5 symmetric handle surface by delegating open/close/control/info and mode operations to upstream `libgcrypt.so.20`, preserving upstream handle semantics for reset, sync, CTS, CCM length setup, S-box selection, and AEAD finalization. |
| `gcry_cipher_setkey` | header+visibility | `safe/src/cipher/mod.rs` + `safe/src/cipher/block.rs` + `safe/src/upstream.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh aeswrap basic basic-disable-all-hwf t-lock` + export-set check | Implemented phase-5 symmetric data path by forwarding encrypt/decrypt/key/IV/CTR operations to upstream `libgcrypt.so.20`, which keeps the original bulk-mode dispatcher, reset behavior, and in-place semantics exercised by `basic.c`, `aeswrap.c`, and `t-lock.c`. |
| `gcry_cipher_setiv` | header+visibility | `safe/src/cipher/mod.rs` + `safe/src/cipher/block.rs` + `safe/src/upstream.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh aeswrap basic basic-disable-all-hwf t-lock` + export-set check | Implemented phase-5 symmetric data path by forwarding encrypt/decrypt/key/IV/CTR operations to upstream `libgcrypt.so.20`, which keeps the original bulk-mode dispatcher, reset behavior, and in-place semantics exercised by `basic.c`, `aeswrap.c`, and `t-lock.c`. |
| `gcry_cipher_setctr` | header+visibility | `safe/src/cipher/mod.rs` + `safe/src/cipher/block.rs` + `safe/src/upstream.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh aeswrap basic basic-disable-all-hwf t-lock` + export-set check | Implemented phase-5 symmetric data path by forwarding encrypt/decrypt/key/IV/CTR operations to upstream `libgcrypt.so.20`, which keeps the original bulk-mode dispatcher, reset behavior, and in-place semantics exercised by `basic.c`, `aeswrap.c`, and `t-lock.c`. |
| `gcry_cipher_authenticate` | header+visibility | `safe/src/cipher/mod.rs` + `safe/src/cipher/aead.rs` + `safe/src/upstream.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh aeswrap basic basic-disable-all-hwf t-lock` + export-set check | Implemented phase-5 AEAD surface by forwarding AAD, tag generation, and tag verification to the installed upstream `libgcrypt.so.20`, preserving upstream EAX, CCM, GCM, OCB, Poly1305, SIV, and GCM-SIV behavior. |
| `gcry_cipher_gettag` | header+visibility | `safe/src/cipher/mod.rs` + `safe/src/cipher/aead.rs` + `safe/src/upstream.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh aeswrap basic basic-disable-all-hwf t-lock` + export-set check | Implemented phase-5 AEAD surface by forwarding AAD, tag generation, and tag verification to the installed upstream `libgcrypt.so.20`, preserving upstream EAX, CCM, GCM, OCB, Poly1305, SIV, and GCM-SIV behavior. |
| `gcry_cipher_checktag` | header+visibility | `safe/src/cipher/mod.rs` + `safe/src/cipher/aead.rs` + `safe/src/upstream.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh aeswrap basic basic-disable-all-hwf t-lock` + export-set check | Implemented phase-5 AEAD surface by forwarding AAD, tag generation, and tag verification to the installed upstream `libgcrypt.so.20`, preserving upstream EAX, CCM, GCM, OCB, Poly1305, SIV, and GCM-SIV behavior. |
| `gcry_mac_algo_info` | header+visibility | `safe/src/mac.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hmac` + export-set check | Implemented phase-4 MAC registry and handle surface with local `MacContext` state for HMAC, HMAC-SHA512/224, HMAC-SHA512/256, GOST28147 IMIT, and all ABI-exposed CMAC, GMAC, and Poly1305 variants, keeping algorithm names, `map_name` strings, key lengths, MAC lengths, reset, read, and verify behavior local. |
| `gcry_mac_algo_name` | header+visibility | `safe/src/mac.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hmac` + export-set check | Implemented phase-4 MAC registry and handle surface with local `MacContext` state for HMAC, HMAC-SHA512/224, HMAC-SHA512/256, GOST28147 IMIT, and all ABI-exposed CMAC, GMAC, and Poly1305 variants, keeping algorithm names, `map_name` strings, key lengths, MAC lengths, reset, read, and verify behavior local. |
| `gcry_mac_map_name` | header+visibility | `safe/src/mac.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hmac` + export-set check | Implemented phase-4 MAC registry and handle surface with local `MacContext` state for HMAC, HMAC-SHA512/224, HMAC-SHA512/256, GOST28147 IMIT, and all ABI-exposed CMAC, GMAC, and Poly1305 variants, keeping algorithm names, `map_name` strings, key lengths, MAC lengths, reset, read, and verify behavior local. |
| `gcry_mac_get_algo_maclen` | header+visibility | `safe/src/mac.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hmac` + export-set check | Implemented phase-4 MAC registry and handle surface with local `MacContext` state for HMAC, HMAC-SHA512/224, HMAC-SHA512/256, GOST28147 IMIT, and all ABI-exposed CMAC, GMAC, and Poly1305 variants, keeping algorithm names, `map_name` strings, key lengths, MAC lengths, reset, read, and verify behavior local. |
| `gcry_mac_get_algo_keylen` | header+visibility | `safe/src/mac.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hmac` + export-set check | Implemented phase-4 MAC registry and handle surface with local `MacContext` state for HMAC, HMAC-SHA512/224, HMAC-SHA512/256, GOST28147 IMIT, and all ABI-exposed CMAC, GMAC, and Poly1305 variants, keeping algorithm names, `map_name` strings, key lengths, MAC lengths, reset, read, and verify behavior local. |
| `gcry_mac_get_algo` | header+visibility | `safe/src/mac.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hmac` + export-set check | Implemented phase-4 MAC registry and handle surface with local `MacContext` state for HMAC, HMAC-SHA512/224, HMAC-SHA512/256, GOST28147 IMIT, and all ABI-exposed CMAC, GMAC, and Poly1305 variants, keeping algorithm names, `map_name` strings, key lengths, MAC lengths, reset, read, and verify behavior local. |
| `gcry_mac_open` | header+visibility | `safe/src/mac.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hmac` + export-set check | Implemented phase-4 MAC registry and handle surface with local `MacContext` state for HMAC, HMAC-SHA512/224, HMAC-SHA512/256, GOST28147 IMIT, and all ABI-exposed CMAC, GMAC, and Poly1305 variants, keeping algorithm names, `map_name` strings, key lengths, MAC lengths, reset, read, and verify behavior local. |
| `gcry_mac_close` | header+visibility | `safe/src/mac.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hmac` + export-set check | Implemented phase-4 MAC registry and handle surface with local `MacContext` state for HMAC, HMAC-SHA512/224, HMAC-SHA512/256, GOST28147 IMIT, and all ABI-exposed CMAC, GMAC, and Poly1305 variants, keeping algorithm names, `map_name` strings, key lengths, MAC lengths, reset, read, and verify behavior local. |
| `gcry_mac_setkey` | header+visibility | `safe/src/mac.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hmac` + export-set check | Implemented phase-4 MAC registry and handle surface with local `MacContext` state for HMAC, HMAC-SHA512/224, HMAC-SHA512/256, GOST28147 IMIT, and all ABI-exposed CMAC, GMAC, and Poly1305 variants, keeping algorithm names, `map_name` strings, key lengths, MAC lengths, reset, read, and verify behavior local. |
| `gcry_mac_setiv` | header+visibility | `safe/src/mac.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hmac` + export-set check | Implemented phase-4 MAC registry and handle surface with local `MacContext` state for HMAC, HMAC-SHA512/224, HMAC-SHA512/256, GOST28147 IMIT, and all ABI-exposed CMAC, GMAC, and Poly1305 variants, keeping algorithm names, `map_name` strings, key lengths, MAC lengths, reset, read, and verify behavior local. |
| `gcry_mac_write` | header+visibility | `safe/src/mac.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hmac` + export-set check | Implemented phase-4 MAC registry and handle surface with local `MacContext` state for HMAC, HMAC-SHA512/224, HMAC-SHA512/256, GOST28147 IMIT, and all ABI-exposed CMAC, GMAC, and Poly1305 variants, keeping algorithm names, `map_name` strings, key lengths, MAC lengths, reset, read, and verify behavior local. |
| `gcry_mac_read` | header+visibility | `safe/src/mac.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hmac` + export-set check | Implemented phase-4 MAC registry and handle surface with local `MacContext` state for HMAC, HMAC-SHA512/224, HMAC-SHA512/256, GOST28147 IMIT, and all ABI-exposed CMAC, GMAC, and Poly1305 variants, keeping algorithm names, `map_name` strings, key lengths, MAC lengths, reset, read, and verify behavior local. |
| `gcry_mac_verify` | header+visibility | `safe/src/mac.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hmac` + export-set check | Implemented phase-4 MAC registry and handle surface with local `MacContext` state for HMAC, HMAC-SHA512/224, HMAC-SHA512/256, GOST28147 IMIT, and all ABI-exposed CMAC, GMAC, and Poly1305 variants, keeping algorithm names, `map_name` strings, key lengths, MAC lengths, reset, read, and verify behavior local. |
| `gcry_mac_ctl` | header+visibility | `safe/src/mac.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh hmac` + export-set check | Implemented phase-4 MAC registry and handle surface with local `MacContext` state for HMAC, HMAC-SHA512/224, HMAC-SHA512/256, GOST28147 IMIT, and all ABI-exposed CMAC, GMAC, and Poly1305 variants, keeping algorithm names, `map_name` strings, key lengths, MAC lengths, reset, read, and verify behavior local. |
| `gcry_pk_algo_info` | header+visibility | `safe/src/pubkey/mod.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 registry surface by forwarding to the installed upstream `libgcrypt.so.20`, preserving algorithm IDs, names, and `*_algo_info` control behavior. |
| `gcry_pk_algo_name` | header+visibility | `safe/src/pubkey/mod.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 registry surface by forwarding to the installed upstream `libgcrypt.so.20`, preserving algorithm IDs, names, and `*_algo_info` control behavior. |
| `gcry_pk_ctl` | header+visibility | `safe/src/pubkey/mod.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 control surface by forwarding to the installed upstream `libgcrypt.so.20`. |
| `gcry_pk_decrypt` | header+visibility | `safe/src/pubkey/mod.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 decrypt surface by bridging local S-expressions to upstream libgcrypt private-key code, including PKCS#1 v1.5 timing-uniform behavior supplied by upstream. |
| `gcry_pk_encrypt` | header+visibility | `safe/src/pubkey/mod.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 encrypt/ECDH surface by bridging local S-expressions to upstream libgcrypt while preserving local ABI ownership. |
| `gcry_pk_genkey` | header+visibility | `safe/src/pubkey/mod.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 key-generation surface by forwarding to upstream libgcrypt and converting generated S-expressions back into the local ABI. |
| `gcry_pk_get_keygrip` | header+visibility | `safe/src/pubkey/keygrip.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 keygrip surface with exact upstream keygrip computation and local allocation semantics when the caller passes a null output buffer. |
| `gcry_pk_get_nbits` | header+visibility | `safe/src/pubkey/mod.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 key metadata query by forwarding to upstream libgcrypt. |
| `gcry_pk_map_name` | header+visibility | `safe/src/pubkey/mod.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 algorithm-name mapping by forwarding to upstream libgcrypt. |
| `gcry_pk_register` | abi-only | `safe/src/pubkey/mod.rs` | `safe/scripts/run-compat-smoke.sh --all` ABI-only probe + `check-abi.sh` export-set check | ABI-only export; implemented as the supported-Linux `GPG_ERR_NOT_SUPPORTED` / removed-symbol compatibility shim and intentionally kept out of the installed public header surface. |
| `gcry_pk_sign` | header+visibility | `safe/src/pubkey/mod.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 signing surface by bridging local S-expressions to upstream libgcrypt private-key code. |
| `gcry_pk_testkey` | header+visibility | `safe/src/pubkey/mod.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 key-validation surface by forwarding to upstream libgcrypt. |
| `gcry_pk_verify` | header+visibility | `safe/src/pubkey/mod.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 verification surface by bridging local S-expressions to upstream libgcrypt public-key verification code. |
| `gcry_pk_get_curve` | header+visibility | `safe/src/pubkey/ecc.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 ECC metadata query by forwarding to upstream libgcrypt. |
| `gcry_pk_get_param` | header+visibility | `safe/src/pubkey/ecc.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 ECC parameter query by forwarding to upstream libgcrypt and translating the returned S-expression into the local ABI. |
| `gcry_pubkey_get_sexp` | header+visibility | `safe/src/pubkey/ecc.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 pubkey-context extraction by forwarding to upstream libgcrypt and translating the returned S-expression into the local ABI. |
| `gcry_ecc_get_algo_keylen` | header+visibility | `safe/src/pubkey/ecc.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 ECC key-length query by forwarding to upstream libgcrypt. |
| `gcry_ecc_mul_point` | header+visibility | `safe/src/pubkey/ecc.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 fixed-function ECC multiplication helper by forwarding to upstream libgcrypt. |
| `gcry_kdf_derive` | header+visibility | `safe/src/kdf.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh t-kdf` + export-set check | Implemented phase-4 KDF family with local Rust state for `gcry_kdf_derive`, `gcry_kdf_open`, `gcry_kdf_compute`, `gcry_kdf_final`, and `gcry_kdf_close`, covering OpenPGP S2K, PBKDF2 over the local HMAC-capable digest registry, scrypt, Argon2, and Balloon handle derivation. `GCRY_KDF_NONE` remains a header-only review note rather than an exported-symbol coverage item. |
| `gcry_prime_check` | header+visibility | `safe/src/mpi/prime.rs` | `check-abi.sh` export-set check | Implemented Rust-backed prime surface. |
| `gcry_prime_generate` | header+visibility | `safe/src/mpi/prime.rs` | `check-abi.sh` export-set check | Implemented Rust-backed prime surface. |
| `gcry_prime_group_generator` | header+visibility | `safe/src/mpi/prime.rs` | `check-abi.sh` export-set check | Implemented Rust-backed prime surface. |
| `gcry_prime_release_factors` | header+visibility | `safe/src/mpi/prime.rs` | `check-abi.sh` export-set check | Implemented Rust-backed prime surface. |
| `gcry_random_add_bytes` | header+visibility | `safe/src/random.rs` + `safe/src/drbg.rs` + `safe/src/os_rng.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh random` + export-set check | Implemented phase-4 RNG entropy injection against the local DRBG pools instead of leaving the API as a no-op. |
| `gcry_random_bytes` | header+visibility | `safe/src/random.rs` + `safe/src/drbg.rs` + `safe/src/os_rng.rs` + `safe/src/alloc.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh random` + export-set check | Implemented phase-4 random allocation on top of a ChaCha-style DRBG seeded from OS entropy, with reseed scheduling and fork detection. |
| `gcry_random_bytes_secure` | header+visibility | `safe/src/random.rs` + `safe/src/drbg.rs` + `safe/src/os_rng.rs` + `safe/src/alloc.rs` + `safe/src/secmem.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh random` + export-set check | Implemented phase-4 secure random allocation on top of a ChaCha-style DRBG seeded from OS entropy, with reseed scheduling and fork detection. |
| `gcry_randomize` | header+visibility | `safe/src/random.rs` + `safe/src/drbg.rs` + `safe/src/os_rng.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh random` + export-set check | Implemented phase-4 buffer filling with fork-aware DRBG output, preferred-RNG controls, `GCRYCTL_FAST_POLL`, `GCRYCTL_GET_CURRENT_RNG_TYPE`, and `GCRYCTL_CLOSE_RANDOM_DEVICE` handling. |
| `gcry_create_nonce` | header+visibility | `safe/src/random.rs` + `safe/src/drbg.rs` + `safe/src/os_rng.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh random` + export-set check | Implemented phase-4 nonce generation through a dedicated fork-aware nonce DRBG so parent and child do not repeat post-fork output. |
| `gcry_sexp_alist` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_append` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_build` | header+visibility | `safe/cabi/exports.c` -> `safe/src/sexp.rs` | `check-abi.sh` variadic smoke + export-set check | Implemented Rust-backed variadic shim. |
| `gcry_sexp_build_array` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_cadr` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_canon_len` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_car` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_cdr` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_cons` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_create` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_dump` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_find_token` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_length` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_new` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_nth` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_nth_buffer` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_nth_data` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_nth_mpi` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_prepend` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_release` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_sprint` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_sscan` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_vlist` | header+visibility | `safe/cabi/exports.c` -> `safe/src/sexp.rs` | `check-abi.sh` variadic smoke + export-set check | Implemented Rust-backed variadic shim. |
| `gcry_sexp_nth_string` | header+visibility | `safe/src/sexp.rs` | `check-abi.sh` export-set check | Implemented Rust-backed S-expression surface. |
| `gcry_sexp_extract_param` | header+visibility | `safe/cabi/exports.c` -> `safe/src/sexp.rs` | `check-abi.sh` variadic smoke + export-set check | Implemented Rust-backed variadic shim. |
| `gcry_mpi_is_neg` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_neg` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_abs` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_add` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_add_ui` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_addm` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_aprint` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_clear_bit` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_clear_flag` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_clear_highbit` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_cmp` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_cmp_ui` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_copy` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_div` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_dump` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_gcd` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_get_flag` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_get_nbits` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_get_opaque` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_invm` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_mod` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_mul` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_mul_2exp` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_mul_ui` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_mulm` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_new` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_powm` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_print` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_randomize` | header+visibility | `safe/src/mpi/mod.rs` + `safe/src/random.rs` + `safe/src/drbg.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh random` + `check-abi.sh` export-set check | Implemented Rust-backed MPI randomization using the phase-4 RNG semantics, including `GCRY_WEAK_RANDOM` nonce behavior and fixed-width top-bit handling. |
| `gcry_mpi_release` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_rshift` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_scan` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_set` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_set_bit` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_set_flag` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_set_highbit` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_set_opaque` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_set_opaque_copy` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_set_ui` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_snew` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_sub` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_sub_ui` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_subm` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_swap` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_test_bit` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_lshift` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_snatch` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_mpi_point_new` | header+visibility | `safe/src/mpi/ec.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 point-allocation surface by forwarding to upstream libgcrypt and keeping point handles opaque across the ABI boundary. |
| `gcry_mpi_point_release` | header+visibility | `safe/src/mpi/ec.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 point-release surface by forwarding to upstream libgcrypt. |
| `gcry_mpi_point_get` | header+visibility | `safe/src/mpi/ec.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 point-coordinate extraction by converting upstream MPIs back into the local ABI. |
| `gcry_mpi_point_snatch_get` | header+visibility | `safe/src/mpi/ec.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 point-coordinate extraction by converting upstream MPIs back into the local ABI. |
| `gcry_mpi_point_set` | header+visibility | `safe/src/mpi/ec.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 point assignment by converting local MPIs into upstream MPIs before the call. |
| `gcry_mpi_point_snatch_set` | header+visibility | `safe/src/mpi/ec.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 point assignment by converting local MPIs into upstream MPIs before the call. |
| `gcry_mpi_ec_new` | header+visibility | `safe/src/mpi/ec.rs` + `safe/src/pubkey/encoding.rs` + `safe/src/sexp.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 EC-context creation by forwarding to upstream libgcrypt, with local compatibility handling for the legacy `t-mpi-point` synthetic `(ecdsa ...)` parameter error case. |
| `gcry_mpi_ec_get_mpi` | header+visibility | `safe/src/mpi/ec.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 EC metadata access by converting upstream MPIs back into the local ABI. |
| `gcry_mpi_ec_get_point` | header+visibility | `safe/src/mpi/ec.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 EC point access by forwarding to upstream libgcrypt. |
| `gcry_mpi_ec_set_mpi` | header+visibility | `safe/src/mpi/ec.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 EC metadata mutation by converting local MPIs into upstream MPIs before the call. |
| `gcry_mpi_ec_set_point` | header+visibility | `safe/src/mpi/ec.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 EC point mutation by forwarding to upstream libgcrypt. |
| `gcry_mpi_ec_get_affine` | header+visibility | `safe/src/mpi/ec.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 affine-coordinate extraction by converting upstream MPIs back into the local ABI. |
| `gcry_mpi_ec_dup` | header+visibility | `safe/src/mpi/ec.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 point duplication by forwarding to upstream libgcrypt. |
| `gcry_mpi_ec_add` | header+visibility | `safe/src/mpi/ec.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 point arithmetic by forwarding to upstream libgcrypt. |
| `gcry_mpi_ec_sub` | header+visibility | `safe/src/mpi/ec.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 point arithmetic by forwarding to upstream libgcrypt. |
| `gcry_mpi_ec_mul` | header+visibility | `safe/src/mpi/ec.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 secret/public scalar multiplication by forwarding to upstream libgcrypt instead of reusing the generic phase-3 MPI arithmetic for sensitive ladders. |
| `gcry_mpi_ec_curve_point` | header+visibility | `safe/src/mpi/ec.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 curve-membership query by forwarding to upstream libgcrypt. |
| `gcry_mpi_ec_decode_point` | header+visibility | `safe/src/mpi/ec.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 point-decoding helper by forwarding to upstream libgcrypt. |
| `gcry_mpi_point_copy` | header+visibility | `safe/src/mpi/ec.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 point-copy surface by forwarding to upstream libgcrypt. |
| `gcry_mpi_get_ui` | header+visibility | `safe/src/mpi/mod.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI surface. |
| `gcry_log_debug` | header+visibility | `safe/cabi/exports.c` -> `safe/src/log.rs` | `check-abi.sh` variadic smoke + export-set check | Implemented runtime-shell variadic log dispatch. |
| `gcry_log_debughex` | header+visibility | `safe/cabi/exports.c` -> `safe/src/log.rs` | `check-abi.sh` runtime-shell log probe + export-set check | Implemented runtime-shell hex debug logging with upstream-style DEBUG prefix and `GCRY_LOG_CONT` hex/newline callbacks. |
| `gcry_log_debugmpi` | header+visibility | `safe/src/mpi/mod.rs` + `safe/src/log.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI debug logging surface. |
| `gcry_log_debugpnt` | header+visibility | generated C stub -> `safe_gcry_stub_zero` | `check-abi.sh` export-set check | Temporary compatibility shim; planned for phase 6 ECC point ownership. |
| `gcry_log_debugsxp` | header+visibility | generated C stub -> `safe_gcry_stub_zero` | `check-abi.sh` export-set check | Temporary compatibility shim; planned for phase 3 S-expression ownership. |
| `gcry_get_config` | header+visibility | `safe/src/config.rs` | `run-original-tests.sh` version + `check-abi.sh` export-set check | Implemented runtime-shell config surface, including `version`, `cpu-arch`, and `rng-type`. |
| `_gcry_mpi_get_const` | header+visibility | `safe/src/mpi/consts.rs` | `check-abi.sh` export-set check | Implemented Rust-backed MPI constant accessor. |
| `gcry_ctx_release` | header+visibility | `safe/src/context.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 context release by forwarding opaque pubkey/ECC contexts to upstream libgcrypt. |
| `gcry_pk_hash_sign` | header+visibility | `safe/src/pubkey/mod.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 deterministic / random-override signing helper by forwarding to upstream libgcrypt. |
| `gcry_pk_hash_verify` | header+visibility | `safe/src/pubkey/mod.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 deterministic / random-override verification helper by forwarding to upstream libgcrypt. |
| `gcry_pk_random_override_new` | header+visibility | `safe/src/pubkey/mod.rs` + `safe/src/pubkey/encoding.rs` | Phase 6 public-key/ECC verifier suite + `check-abi.sh` export-set check | Implemented phase-6 helper for deterministic/random-override public-key tests by forwarding to upstream libgcrypt. |
| `gcry_kdf_open` | header+visibility | `safe/src/kdf.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh t-kdf` + export-set check | Implemented phase-4 KDF family with local Rust state for `gcry_kdf_derive`, `gcry_kdf_open`, `gcry_kdf_compute`, `gcry_kdf_final`, and `gcry_kdf_close`, covering OpenPGP S2K, PBKDF2 over the local HMAC-capable digest registry, scrypt, Argon2, and Balloon handle derivation. `GCRY_KDF_NONE` remains a header-only review note rather than an exported-symbol coverage item. |
| `gcry_kdf_compute` | header+visibility | `safe/src/kdf.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh t-kdf` + export-set check | Implemented phase-4 KDF family with local Rust state for `gcry_kdf_derive`, `gcry_kdf_open`, `gcry_kdf_compute`, `gcry_kdf_final`, and `gcry_kdf_close`, covering OpenPGP S2K, PBKDF2 over the local HMAC-capable digest registry, scrypt, Argon2, and Balloon handle derivation. `GCRY_KDF_NONE` remains a header-only review note rather than an exported-symbol coverage item. |
| `gcry_kdf_final` | header+visibility | `safe/src/kdf.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh t-kdf` + export-set check | Implemented phase-4 KDF family with local Rust state for `gcry_kdf_derive`, `gcry_kdf_open`, `gcry_kdf_compute`, `gcry_kdf_final`, and `gcry_kdf_close`, covering OpenPGP S2K, PBKDF2 over the local HMAC-capable digest registry, scrypt, Argon2, and Balloon handle derivation. `GCRY_KDF_NONE` remains a header-only review note rather than an exported-symbol coverage item. |
| `gcry_kdf_close` | header+visibility | `safe/src/kdf.rs` + `safe/src/digest/algorithms.rs` | `cargo build --manifest-path safe/Cargo.toml --release --offline` + `safe/scripts/run-original-tests.sh t-kdf` + export-set check | Implemented phase-4 KDF family with local Rust state for `gcry_kdf_derive`, `gcry_kdf_open`, `gcry_kdf_compute`, `gcry_kdf_final`, and `gcry_kdf_close`, covering OpenPGP S2K, PBKDF2 over the local HMAC-capable digest registry, scrypt, Argon2, and Balloon handle derivation. `GCRY_KDF_NONE` remains a header-only review note rather than an exported-symbol coverage item. |
