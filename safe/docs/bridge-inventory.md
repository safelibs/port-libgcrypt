# Bridge Inventory

This inventory records the current hybrid baseline after the Noble toolchain downgrade. The exported ABI is no longer a single implementation surface: most symbol families are owned by local Rust code, while a reduced high-level ECC bridge still loads selected helpers from the system `libgcrypt.so.20` at runtime.

## Baseline Split

- Rust-owned exported families: version/control/config/error/allocation/randomness/secure-memory in [`safe/src/global.rs`](../src/global.rs), [`safe/src/config.rs`](../src/config.rs), [`safe/src/error.rs`](../src/error.rs), [`safe/src/alloc.rs`](../src/alloc.rs), and [`safe/src/random.rs`](../src/random.rs); S-expression/MPI/prime families in [`safe/src/sexp.rs`](../src/sexp.rs) and [`safe/src/mpi/`](../src/mpi); logging in [`safe/src/log.rs`](../src/log.rs).
- Rust-owned exported families: digest, MAC, and KDF exports now run entirely from their owning Rust modules in [`safe/src/digest/mod.rs`](../src/digest/mod.rs), [`safe/src/mac.rs`](../src/mac.rs), and [`safe/src/kdf.rs`](../src/kdf.rs), with local registries, handles, and data paths instead of any private upstream digest/MAC/KDF bridge.
- Rust-owned exported families: phase 5 now owns the symmetric cipher surface in [`safe/src/cipher/mod.rs`](../src/cipher/mod.rs), [`safe/src/cipher/registry.rs`](../src/cipher/registry.rs), [`safe/src/cipher/modes.rs`](../src/cipher/modes.rs), [`safe/src/cipher/aead.rs`](../src/cipher/aead.rs), [`safe/src/cipher/block.rs`](../src/cipher/block.rs), and [`safe/src/cipher/stream.rs`](../src/cipher/stream.rs), with local block/stream/AEAD registry and handle state, local mode dispatch, AEAD/tag handling, in-place overlap semantics, and runtime hardware-disable enforcement for the AES backend.
- Rust-owned exported families: non-ECC public-key dispatch, RSA/DSA/ElGamal sign/verify/encrypt/decrypt/testkey/genkey, algorithm queries, `gcry_pk_hash_*`, local random-override contexts, non-ECC keygrip handling, ECC curve metadata helpers, `gcry_ecc_mul_point`, `gcry_mpi_point_*`, `gcry_mpi_ec_*`, and `gcry_ctx_release` now live in [`safe/src/pubkey/mod.rs`](../src/pubkey/mod.rs), [`safe/src/pubkey/rsa.rs`](../src/pubkey/rsa.rs), [`safe/src/pubkey/dsa.rs`](../src/pubkey/dsa.rs), [`safe/src/pubkey/elgamal.rs`](../src/pubkey/elgamal.rs), [`safe/src/pubkey/ecc.rs`](../src/pubkey/ecc.rs), [`safe/src/pubkey/keygrip.rs`](../src/pubkey/keygrip.rs), [`safe/src/context.rs`](../src/context.rs), [`safe/src/mpi/ec.rs`](../src/mpi/ec.rs), and the shared S-expression/MPI support in [`safe/src/sexp.rs`](../src/sexp.rs) and [`safe/src/mpi/`](../src/mpi). The exported low-level ECC entrypoints, point ownership, curve naming, and EC-context lifetime are local; `safe/src/mpi/ec.rs` now reuses upstream EC helpers only for secret-sensitive scalar multiplication and private-`q` derivation so those paths do not fall back to the local `Mpz` point loops.
- Bridge-backed exported families: the remaining ECC sign/verify/encrypt/decrypt/testkey/genkey helpers, ECC `gcry_pk_get_nbits`, ECC keygrip fallback, and foreign-context `gcry_pubkey_get_sexp` fallback still load through [`safe/src/pubkey/ecc.rs`](../src/pubkey/ecc.rs).
- Bridge-only marshalling helpers: [`safe/src/pubkey/encoding.rs`](../src/pubkey/encoding.rs) still loads upstream `gcry_sexp_*` and `gcry_mpi_*` helpers for Rust-to-upstream translation, and [`safe/src/mpi/ec.rs`](../src/mpi/ec.rs) now loads upstream low-level EC helpers for secret-scalar hardening inside the local ECC exports, even though the public S-expression/MPI exports and the low-level ECC/point/context ABI remain Rust-owned.

## Upstream-Loaded Symbol Families

| Family | Loaded in | Consumed by | Notes |
| --- | --- | --- | --- |
| `gcry_check_version` bootstrap | [`safe/src/upstream.rs`](../src/upstream.rs), [`safe/src/pubkey/encoding.rs`](../src/pubkey/encoding.rs) | Bridge library initialization in both loaders | Both bridge loaders call upstream `gcry_check_version` after `dlopen` to prove the loaded `libgcrypt.so.20` is usable before resolving additional symbols. |
| `gcry_control` for pre-init `GCRYCTL_DISABLE_HWF` forwarding | [`safe/src/upstream.rs`](../src/upstream.rs) | [`safe/src/global.rs`](../src/global.rs), [`safe/src/hwfeatures.rs`](../src/hwfeatures.rs) | Rust owns the public `gcry_control` export and the runtime bookkeeping for disabled hardware features; only the pre-initialization handoff still calls upstream control so the remaining bridge-backed ECC path sees the same feature floor. |
| ECC `gcry_pk_*` fallbacks plus foreign-context `gcry_pubkey_get_sexp` | [`safe/src/pubkey/ecc.rs`](../src/pubkey/ecc.rs) | [`safe/src/pubkey/mod.rs`](../src/pubkey/mod.rs), [`safe/src/pubkey/keygrip.rs`](../src/pubkey/keygrip.rs), [`safe/src/context.rs`](../src/context.rs) | The public `gcry_pk_*` exports are Rust-owned. ECC sign/verify/encrypt/decrypt/testkey/genkey, ECC `gcry_pk_get_nbits`, ECC keygrip fallback, and the fallback `gcry_pubkey_get_sexp` path for foreign contexts still bridge through the dedicated ECC loader. |
| Secret-sensitive low-level EC helpers: `gcry_mpi_ec_new`, `gcry_mpi_ec_get_mpi`, `gcry_mpi_ec_set_mpi`, `gcry_mpi_ec_mul`, `gcry_ctx_release`, `gcry_mpi_point_new`, `gcry_mpi_point_release`, `gcry_mpi_point_snatch_set`, `gcry_mpi_point_get`, `gcry_mpi_new`, `gcry_mpi_set_flag` | [`safe/src/mpi/ec.rs`](../src/mpi/ec.rs) | [`safe/src/mpi/ec.rs`](../src/mpi/ec.rs) | These are private hardening dependencies for the Rust-owned low-level ECC exports. `safe/src/mpi/ec.rs` keeps `gcry_ecc_mul_point`, point lifetime, point add/subtract, point decoding, curve metadata, and context ownership local, but delegates secret scalar multiplication and private-point derivation to the upstream EC core instead of the generic local `Mpz` arithmetic. |
| Bridge-only marshalling helpers: `gcry_sexp_sscan`, `gcry_sexp_sprint`, `gcry_sexp_release`, `gcry_mpi_new`, `gcry_mpi_release`, `gcry_mpi_scan`, `gcry_mpi_print`, `gcry_mpi_get_flag`, `gcry_mpi_get_opaque`, `gcry_mpi_set_opaque_copy` | [`safe/src/pubkey/encoding.rs`](../src/pubkey/encoding.rs) | [`safe/src/pubkey/ecc.rs`](../src/pubkey/ecc.rs), [`safe/src/pubkey/keygrip.rs`](../src/pubkey/keygrip.rs) | These are internal bridge dependencies used for Rust-to-upstream translation, not ownership of the public MPI/S-expression exports or the low-level ECC/point/context surface. |

## `SAFE_SYSTEM_LIBGCRYPT_PATH` Flow

| Role | Files | Usage |
| --- | --- | --- |
| Compile-time fallback path injection | [`safe/build.rs`](../build.rs) | Writes `cargo:rustc-env=SAFE_SYSTEM_LIBGCRYPT_PATH=...` when a system `libgcrypt.so.20` is discoverable, so the runtime bridge has an embedded fallback path. |
| Runtime bridge consumption | [`safe/src/upstream.rs`](../src/upstream.rs), [`safe/src/pubkey/ecc.rs`](../src/pubkey/ecc.rs), [`safe/src/pubkey/encoding.rs`](../src/pubkey/encoding.rs), [`safe/src/mpi/ec.rs`](../src/mpi/ec.rs) | Prefer the environment override before the built-in distro search paths when `dlopen`ing upstream `libgcrypt.so.20`. |
| Installed-helper bridge override | [`safe/scripts/check-installed-tools.sh`](../scripts/check-installed-tools.sh) | Accepts or discovers an upstream runtime path, then exports `SAFE_SYSTEM_LIBGCRYPT_PATH` before running the packaged helper and metadata smokes. |
| Downstream/dependent bridge override | [`test-original.sh`](../../test-original.sh) | Exports or forwards the upstream runtime path into helper smoke, GNOME Keyring, and dependent-software checks. |

## Bridge-Dependent Verifiers And Helpers

- [`safe/scripts/run-original-tests.sh`](../scripts/run-original-tests.sh): `--all` executes the original `TESTS` inventory from `original/libgcrypt20-1.10.3/tests/Makefile.am`; digest/MAC/KDF/cipher entries now go through their owning Rust modules, non-ECC pubkey coverage exercises local RSA/DSA/ElGamal ownership, and the ECC metadata/point/context tests now exercise the Rust-owned low-level ECC surface while the remaining high-level ECC bridge stays available. `--list` / inventory-only `--dry-run` stay metadata-only.
- [`safe/scripts/run-upstream-tests.sh`](../scripts/run-upstream-tests.sh): the committed imported harness now exercises Rust-owned digest/MAC/KDF/cipher exports, Rust-owned non-ECC pubkey paths, and Rust-owned low-level ECC/point/context paths alongside the remaining bridge-loaded high-level ECC helpers.
- [`safe/scripts/run-compat-smoke.sh`](../scripts/run-compat-smoke.sh): public development metadata smokes and the ABI-only `gcry_pk_register` probe still run against the hybrid shared library.
- [`safe/scripts/relink-original-objects.sh`](../scripts/relink-original-objects.sh): relinked original objects still execute the Rust-owned digest/MAC/KDF/cipher surface, Rust-owned non-ECC pubkey surface, Rust-owned low-level ECC/point/context surface, and the remaining bridge-loaded high-level ECC helpers after linking against the safe shared library.
- [`safe/scripts/check-installed-tools.sh`](../scripts/check-installed-tools.sh): packaged helper verification exports an upstream bridge target before running `dumpsexp`, `hmac256`, `mpicalc`, and the install-surface compile/link smokes.
- [`test-original.sh`](../../test-original.sh): downstream dependent verification still provisions an upstream `libgcrypt.so.20` side library and forwards `SAFE_SYSTEM_LIBGCRYPT_PATH` while exercising the packaged safe build.
