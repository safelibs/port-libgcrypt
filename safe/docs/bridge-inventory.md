# Bridge Inventory

This inventory records the current hybrid baseline after the Noble toolchain downgrade. The exported ABI is no longer a single implementation surface: most symbol families are owned by local Rust code, while a reduced ECC/EC-context bridge still loads selected helpers from the system `libgcrypt.so.20` at runtime.

## Baseline Split

- Rust-owned exported families: version/control/config/error/allocation/randomness/secure-memory in [`safe/src/global.rs`](../src/global.rs), [`safe/src/config.rs`](../src/config.rs), [`safe/src/error.rs`](../src/error.rs), [`safe/src/alloc.rs`](../src/alloc.rs), and [`safe/src/random.rs`](../src/random.rs); S-expression/MPI/prime families in [`safe/src/sexp.rs`](../src/sexp.rs) and [`safe/src/mpi/`](../src/mpi); logging in [`safe/src/log.rs`](../src/log.rs).
- Rust-owned exported families: digest, MAC, and KDF exports now run entirely from their owning Rust modules in [`safe/src/digest/mod.rs`](../src/digest/mod.rs), [`safe/src/mac.rs`](../src/mac.rs), and [`safe/src/kdf.rs`](../src/kdf.rs), with local registries, handles, and data paths instead of any private upstream digest/MAC/KDF bridge.
- Rust-owned exported families: phase 5 now owns the symmetric cipher surface in [`safe/src/cipher/mod.rs`](../src/cipher/mod.rs), [`safe/src/cipher/registry.rs`](../src/cipher/registry.rs), [`safe/src/cipher/modes.rs`](../src/cipher/modes.rs), [`safe/src/cipher/aead.rs`](../src/cipher/aead.rs), [`safe/src/cipher/block.rs`](../src/cipher/block.rs), and [`safe/src/cipher/stream.rs`](../src/cipher/stream.rs), with local block/stream/AEAD registry and handle state, local mode dispatch, AEAD/tag handling, in-place overlap semantics, and runtime hardware-disable enforcement for the AES backend.
- Rust-owned exported families: non-ECC public-key dispatch, RSA/DSA/ElGamal sign/verify/encrypt/decrypt/testkey/genkey, algorithm queries, `gcry_pk_hash_*`, local random-override contexts, and non-ECC keygrip handling now live in [`safe/src/pubkey/mod.rs`](../src/pubkey/mod.rs), [`safe/src/pubkey/rsa.rs`](../src/pubkey/rsa.rs), [`safe/src/pubkey/dsa.rs`](../src/pubkey/dsa.rs), [`safe/src/pubkey/elgamal.rs`](../src/pubkey/elgamal.rs), [`safe/src/pubkey/keygrip.rs`](../src/pubkey/keygrip.rs), [`safe/src/context.rs`](../src/context.rs), and the shared S-expression/MPI support in [`safe/src/sexp.rs`](../src/sexp.rs) and [`safe/src/mpi/`](../src/mpi).
- Bridge-backed exported families: ECC metadata/operation helpers plus point/EC-context handling still load through [`safe/src/pubkey/ecc.rs`](../src/pubkey/ecc.rs), [`safe/src/pubkey/encoding.rs`](../src/pubkey/encoding.rs), and [`safe/src/mpi/ec.rs`](../src/mpi/ec.rs).
- Bridge-only marshalling helpers: [`safe/src/pubkey/encoding.rs`](../src/pubkey/encoding.rs) still loads upstream `gcry_sexp_*`, `gcry_mpi_*`, EC-point, EC-context, and foreign-context-release helpers for translation and opaque-handle forwarding, even though the public S-expression/MPI exports and the non-ECC public-key ABI are Rust-owned.

## Upstream-Loaded Symbol Families

| Family | Loaded in | Consumed by | Notes |
| --- | --- | --- | --- |
| `gcry_check_version` bootstrap | [`safe/src/upstream.rs`](../src/upstream.rs), [`safe/src/pubkey/encoding.rs`](../src/pubkey/encoding.rs) | Bridge library initialization in both loaders | Both bridge loaders call upstream `gcry_check_version` after `dlopen` to prove the loaded `libgcrypt.so.20` is usable before resolving additional symbols. |
| `gcry_control` for pre-init `GCRYCTL_DISABLE_HWF` forwarding | [`safe/src/upstream.rs`](../src/upstream.rs) | [`safe/src/global.rs`](../src/global.rs), [`safe/src/hwfeatures.rs`](../src/hwfeatures.rs) | Rust owns the public `gcry_control` export and the runtime bookkeeping for disabled hardware features; only the pre-initialization handoff still calls upstream control so the remaining bridge-backed ECC path sees the same feature floor. |
| ECC `gcry_pk_*` fallbacks plus `gcry_pubkey_get_sexp` and `gcry_ecc_*` | [`safe/src/pubkey/ecc.rs`](../src/pubkey/ecc.rs) | [`safe/src/pubkey/mod.rs`](../src/pubkey/mod.rs), [`safe/src/pubkey/keygrip.rs`](../src/pubkey/keygrip.rs), [`safe/src/context.rs`](../src/context.rs) | The public `gcry_pk_*` exports are Rust-owned, but ECC-family dispatch still bridges to upstream helpers through the dedicated ECC loader. Non-ECC RSA/DSA/ElGamal ownership no longer depends on upstream symbols. |
| `gcry_mpi_point_*`, `gcry_mpi_ec_*`, and foreign `gcry_ctx_release` | [`safe/src/pubkey/encoding.rs`](../src/pubkey/encoding.rs) | [`safe/src/mpi/ec.rs`](../src/mpi/ec.rs), [`safe/src/context.rs`](../src/context.rs), [`safe/src/pubkey/ecc.rs`](../src/pubkey/ecc.rs) | Opaque point/context handles remain upstream-backed. [`safe/src/context.rs`](../src/context.rs) now frees local random-override contexts itself and only forwards foreign ECC contexts across the bridge. |
| Bridge-only marshalling helpers: `gcry_sexp_sscan`, `gcry_sexp_sprint`, `gcry_sexp_release`, `gcry_mpi_new`, `gcry_mpi_release`, `gcry_mpi_scan`, `gcry_mpi_print`, `gcry_mpi_get_flag`, `gcry_mpi_get_opaque`, `gcry_mpi_set_opaque_copy` | [`safe/src/pubkey/encoding.rs`](../src/pubkey/encoding.rs) | [`safe/src/pubkey/mod.rs`](../src/pubkey/mod.rs), [`safe/src/pubkey/ecc.rs`](../src/pubkey/ecc.rs), [`safe/src/pubkey/keygrip.rs`](../src/pubkey/keygrip.rs), [`safe/src/mpi/ec.rs`](../src/mpi/ec.rs) | These are internal bridge dependencies used for Rust-to-upstream translation, not ownership of the public MPI/S-expression exports. |

## `SAFE_SYSTEM_LIBGCRYPT_PATH` Flow

| Role | Files | Usage |
| --- | --- | --- |
| Compile-time fallback path injection | [`safe/build.rs`](../build.rs) | Writes `cargo:rustc-env=SAFE_SYSTEM_LIBGCRYPT_PATH=...` when a system `libgcrypt.so.20` is discoverable, so the runtime bridge has an embedded fallback path. |
| Runtime bridge consumption | [`safe/src/upstream.rs`](../src/upstream.rs), [`safe/src/pubkey/ecc.rs`](../src/pubkey/ecc.rs), [`safe/src/pubkey/encoding.rs`](../src/pubkey/encoding.rs) | Prefer the environment override before the built-in distro search paths when `dlopen`ing upstream `libgcrypt.so.20`. |
| Installed-helper bridge override | [`safe/scripts/check-installed-tools.sh`](../scripts/check-installed-tools.sh) | Accepts or discovers an upstream runtime path, then exports `SAFE_SYSTEM_LIBGCRYPT_PATH` before running the packaged helper and metadata smokes. |
| Downstream/dependent bridge override | [`test-original.sh`](../../test-original.sh) | Exports or forwards the upstream runtime path into helper smoke, GNOME Keyring, and dependent-software checks. |

## Bridge-Dependent Verifiers And Helpers

- [`safe/scripts/run-original-tests.sh`](../scripts/run-original-tests.sh): `--all` executes the original `TESTS` inventory from `original/libgcrypt20-1.10.3/tests/Makefile.am`; digest/MAC/KDF/cipher entries now go through their owning Rust modules, non-ECC pubkey coverage exercises local RSA/DSA/ElGamal ownership, and ECC/point coverage still exercises the reduced bridge families. `--list` / inventory-only `--dry-run` stay metadata-only.
- [`safe/scripts/run-upstream-tests.sh`](../scripts/run-upstream-tests.sh): the committed imported harness now exercises Rust-owned digest/MAC/KDF/cipher exports and Rust-owned non-ECC pubkey paths alongside the remaining bridge-loaded ECC families.
- [`safe/scripts/run-compat-smoke.sh`](../scripts/run-compat-smoke.sh): public development metadata smokes and the ABI-only `gcry_pk_register` probe still run against the hybrid shared library.
- [`safe/scripts/relink-original-objects.sh`](../scripts/relink-original-objects.sh): relinked original objects still execute the Rust-owned digest/MAC/KDF/cipher surface, Rust-owned non-ECC pubkey surface, and the remaining bridge-loaded ECC families after linking against the safe shared library.
- [`safe/scripts/check-installed-tools.sh`](../scripts/check-installed-tools.sh): packaged helper verification exports an upstream bridge target before running `dumpsexp`, `hmac256`, `mpicalc`, and the install-surface compile/link smokes.
- [`test-original.sh`](../../test-original.sh): downstream dependent verification still provisions an upstream `libgcrypt.so.20` side library and forwards `SAFE_SYSTEM_LIBGCRYPT_PATH` while exercising the packaged safe build.
